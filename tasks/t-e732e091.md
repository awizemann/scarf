---
id: t-e732e091
title: UI gate P1b: section root ids, sweep test, base-class hygiene, test plans
status: done
added: 2026-09-08
priority: high
---

## Description

(1) In scarf/scarf/ContentView.swift apply `.accessibilityIdentifier("\(section.id).root")` to the routed section view so all 30 AppSection cases are addressable with one edit. (2) Share the section list with the UI test target (a small Sections.json resource read by both a scarfTests unit test asserting equality with AppSection.allCases and the UI sweep). (3) ScarfUITestCase: add `launchAndSurface()` (activate, ⌘1, wait for window) replacing the copy-pasted dance; replace Thread.sleep with waits; honour an optional `SCARF_UITEST_FIXTURE` env var pointing at a prebuilt fixture home (copy it per test instead of an empty home) — P1a builds it. (4) SectionSweepUITests: for each section click sidebar.section.<rawValue>, wait for <id>.root, assert no error banner, attach a screenshot. (5) Three .xctestplans in scarf/: Smoke (sweep only), Full (whole UI target + unit), Live (sets env SCARF_UITEST_LIVE=1; Live tests XCTSkip without it) so future journeys never edit plans. Wire plans into the scarf scheme.

## Plan



## Artifacts

Commit `5fa0be77` on `worktree-agent-a5b73c90b60f70871` (off `ui-gate`) — not pushed.

**New**
- `scarf/scarfUITests/SectionSweepUITests.swift` — one-launch sweep of all 28 sections.
- `scarf/scarfUITests/LiveGateUITests.swift` — proves the Live plan's env wiring; skips under Smoke/Full.
- `scarf/scarfUITests/Resources/Sections.json` — the section list shared with the UI target.
- `scarf/scarfTests/SectionCatalogTests.swift` — unit guard: JSON == `SidebarSection.allCases`, in order.
- `scarf/Smoke.xctestplan`, `scarf/Full.xctestplan`, `scarf/Live.xctestplan`.

**Changed**
- `scarf/scarf/ContentView.swift` — `<rawValue>.root` applied once at the routing switch.
- `scarf/scarf/Features/Dashboard/Views/DashboardView.swift`, `.../Activity/Views/ActivityView.swift` — `error.banner` identifier.
- `scarf/scarf/Navigation/SidebarProjectsWell.swift` — well header is now a button carrying `sidebar.section.Projects`.
- `scarf/scarfUITests/UITestIsolation.swift` — `launchAndSurface`, `gracefulQuit`, `waitForForeground`, `requireLive`, `SCARF_UITEST_FIXTURE`.
- `scarf/scarfUITests/TemplateInstallUITests.swift` — uses the new helpers; all `Thread.sleep` gone.
- `scarf/scarf.xcodeproj/xcshareddata/xcschemes/scarf.xcscheme` — three plans attached, Full default.

**Commands**
```
xcodebuild test -project scarf/scarf.xcodeproj -scheme scarf \
  -destination 'platform=macOS' -testPlan Smoke   # or Full / Live
```
(add `-skipPackagePluginValidation -skipMacroValidation` when running from a fresh git worktree)

**Results** — Smoke: 28/28 sections rendered, no error banners, no missing rows; 143 s test time, ~2:50 wall. Unit: 747 tests / 98 suites green in 9.9 s.

**Memory** — wrote `conventions/ui-gate-section-root-identifiers-and-the-smoke-full-live` and `conventions/xcuitest-runner-gotchas-env-vars-need-test-runner`.

