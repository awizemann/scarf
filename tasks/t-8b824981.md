---
id: t-8b824981
title: Audit F6: main-actor + performance sweep
status: done
added: 2026-09-02
priority: high
---

## Description

Systemic main-actor sync CLI/transport: Gateway actions, HealthViewModel start/stop/restart/dump, Settings bitwardenStatus, Sessions rename/delete + stat fallback, MemoryView reset, GatewayBehaviorViewModel load/save (make the "detached" comment true), Personalities setActive/saveSOUL + double config read, QuickCommands addOrUpdate, CredentialPools writes, ProjectModelPresetSheet, NewProjectViewModel.commit, KanbanTenantReader → Task.detached with MainActor hand-back, following each file's own load() precedent. Perf: Logs (bounded tail read locally, entries cap, memoized filter), Sessions LazyVStack + memoized filters/counts + drop unread correlated subquery from sessionListSnapshot, Projects sidebar context-menu closures async/cached, LogTail bounded read + MiniAppSchemeHandler size cap, watcher coalescing across the three project views, FleetApplyExecutor concurrent hosts + progress, widget refresh generation guards, Bots roster LazyVStack + memoized filter/sort, Health probe TaskGroup, static NSRegularExpressions, MCP testAll bounded+cancellable+spinners. KANBAN (verified): board tick spawns 2 processes + detail tick 2 + log loop 1 every 2s with no backoff/scenePhase gating (KanbanBoardViewModel.swift:108-116, KanbanTaskDetailViewModel.swift:53-63,96-104, KanbanViewModel.swift:55-63; KanbanBoardView.swift:105-110) — add scenePhase pause + failure backoff; refreshLogOnce passes tailBytes nil despite KanbanService.log supporting --tail (:129-134) — plumb it.

## Plan



## Artifacts

Commits on main: 442080d, b5bd3d9, b60d6e0, 461a9c9, 3110ab6, 6e97ed7, b6dd14e.
Memory: .memory/decisions/section-audit-remediation-2026-09 — "F6" section.
Tests: ScarfCore 1794/111 green (new SectionAuditF6BoundedReadTests, 7); app 603/77 green; TemplateInstallUITests green. Mac + iOS (`scarf mobile`) both build.
Refuted: iOS CronListView seed-from-display (different editor shape, no parse_schedule involved) — a real but different defect fixed in its place; F4's unread-subquery drop confirmed already landed.

