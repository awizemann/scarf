---
id: t-3d4e3b34
title: Audit P17: cross-phase review remediation (ssl_verify boolish, gateway cancel, plugins banner, tests, docs)
status: done
added: 2026-09-09
priority: high
---

## Description

Remediation of the cross-phase fresh-eyes review over fix/whole-surface-audit (P9–P16). Findings, anchored to branch file:line:
HIGH
1. MCPServerEditorViewModel.swift:85 — `ssl_verify` left out of P12's boolish sweep: `ssl_verify: no` renders "Verify TLS peer" ON with "no" in the CA-path field; a later edit of that field routes through P10's yamlScalar and writes a CA path named "no" (the downgrade P10 tried to prevent). Read via boolish/path split; test with `no`/`off`/`0` fixtures.
MED
2. GatewayViewModel.swift:171 — `loadTask?.cancel()` cannot cancel a `Task.detached` body; both `Task.isCancelled` guards are dead; the P11 claim "superseded load cancelled between probes" is unsupported. Either make cancellation real (structured child task / explicit cancellation propagation) or drop the guards and the claim (commit body cannot change; fix the memory note P11 section).
3. PluginsViewModel.swift:381 — user-facing failure banner contains literal 17-space runs (multi-line string literal indentation).
4. HermesCLIExitCodeTruthTests.swift:239 — the two `plugins enable` tests pass `failureWins: true` themselves and never go through the VM; reverting the call-site leaves them green. Test through the production entry point.
5. HermesConfig+YAML.swift:641 — 39 false-default keys still use the literal `== "true"` comparer while the P13 memory note claims the boolish mirror-image was fixed (only 2 keys use boolish). Either route them through boolish (verify each upstream reader is boolish-tolerant) or correct the memory claim to the two keys actually fixed.
LOW
6. HermesCapabilities.swift:1177 + TelegramSetupView.swift:33 say `ignore_root_dm` is "written on every host" while the write is gated — fix docs.
7. HermesSkillsHubParser.swift:208 names a `.unknown` case that doesn't exist — fix doc.
8. HermesCronDoctorParser.swift:179 roster branch now precedes the traceback guard — verify a traceback line cannot be mis-resolved as a job header; add a test or reorder.
9. CronViewModel.swift:588 skill diff ignores Hermes's legacy singular `skill` field — check cron/jobs.py at v2026.9.7 for how `skill` is normalised into `skills` and mirror it in the existing-skills source.
10. ConfigYAMLWriterSafetyTests.swift:24 PyYAML lane degrades silently without PyYAML — make the skip visible (recorded/known issue) rather than silent.

## Plan



## Artifacts

## Shipped

One commit on `fix/whole-surface-audit`: **`ac61b5f7`** (on top of `aec8f8b0 … bd59bd9c`, none rewritten).

All ten findings addressed; nine fixed, one verified as a non-bug (finding 8) and pinned with tests.

| # | Verdict | What shipped |
|---|---|---|
| 1 | fixed | `MCPServerEditorViewModel` hydrates the bool-OR-path `ssl_verify` scalar through a boolish split (`boolishSSLVerify`). Verified against PyYAML that bare `y`/`n` are NOT bools, so they stay paths — same as Hermes sees them. Cited: `tools/mcp_tool_transport.py:410` @ v2026.9.7 passes `config.get("ssl_verify", True)` straight into httpx's `verify=`. |
| 2 | fixed — **claim made true** | `GatewayViewModel.load` now uses `Task.detached` for the WHOLE body and commits via `await MainActor.run`. Cancelling a `Task` never propagated into the inner `Task.detached`, so both `Task.isCancelled` guards were dead. Cancellation is now real; the P11 memory claim stands unedited. C10 preserved (probes still off-main, asserted). |
| 3 | fixed | Plugins consent banner collapsed to a single-line `String(localized:)` — the multi-line literal was shipping 17-space runs. |
| 4 | fixed | `PluginsViewModel` takes an injectable `HermesCLIRunner` (the P11 seam); the `plugins enable`/`disable` verdict tests now drive `vm.enable(...)` / `vm.disable(...)` and read the real banner. Verified red when `failureWins: true` is reverted at the call site. |
| 5 | fixed — **claim made true** | All 39 false-default keys routed through `boolish`; the literal `bool(_:default:)` reader **deleted** so it cannot return. Upstream verification is universal, not per-key: config.yaml goes through `yaml.safe_load`, so `yes`/`on`/`no`/`off` are already Python bools and `1`/`0` truthy/falsy ints before any per-key reader runs — the coercion is in the LOADER. Swept the tag for `is True` / `is False` config readers that could break that: none touch these keys. `telegram.require_mention` keeps its deliberately-Scarf `true` default (reader only was changed). P13's memory section left as written (it was accurate about P13); P17 section records the completion. |
| 6 | fixed | Three doc sites corrected (`HermesCapabilities.hasTelegramIgnoreRootDM`, `TelegramSetupView`, `HermesConfig.TelegramSettings.ignoreRootDM`): the value is PARSED on every host but the WRITE is gated (`TelegramSetupViewModel.save:100`). |
| 7 | fixed | `HermesSkillsHubParser.parseUpdateList` doc no longer names a `.unknown` case — an unknown status fails `init(rawValue:)` and the ROW is skipped (`:225`). Test pins both. |
| 8 | **DISPUTED / no-op + tests** | Verified against the emitter: `cron_doctor` (`hermes_cli/cron.py:517-536` @ v2026.9.7) prints exactly one header shape, `  {id} {name}` at indent 2, issues at indent 4. The roster branch requires an EXACT known job id at a token boundary — strictly stronger evidence than the `isPlausibleJobID` shape heuristic that follows it — so reordering changes nothing. `File` (4 letters) and `Traceback` (no digit/separator, non-hex) both fail `isPlausibleJobID`. Reorder declined; pinned with a traceback fixture instead. |
| 9 | fixed | `HermesCronJob` now mirrors `cron/jobs.py::_normalize_skill_list` (`:384-397`): `skills` present wins even when empty, `skills: null` is `None` and falls back to `skill`, a bare STRING `skills` is a one-element list, trim/dedupe/order-preserving. Root cause is that Scarf reads `cron/jobs.json` DIRECTLY, so `list_jobs` → `_normalize_job_record` → `_apply_skill_fields` never runs. The legacy key is read via its OWN `CodingKey` type so it stays in `extra` and round-trips (adding it to `CodingKeys` would have silently stripped it on every write). |
| 10 | fixed | `ConfigYAMLWriterSafetyTests.pyYAMLRoundTripLaneIsPresent` wraps `#expect(pyYAMLAvailable)` in `withKnownIssue(…, isIntermittent: true)`: green when the lane ran, an explicit named known-issue line when PyYAML is missing, never a false failure. |

## Tests

New: `ScarfCoreTests/HermesP17RemediationTests.swift` (11 tests / 15 parameterised cases), `scarfTests/HermesP17AppRemediationTests.swift` (6 tests / 17 cases), plus the rewritten `plugins enable`/`disable` tests in `HermesCLIExitCodeTruthTests`.

- ScarfCore: **2490 tests / 165 suites — all passed** (20.4s).
- App target `scarfTests`: **917 tests / 119 suites — all passed** (9.1s).
- No known flake fired (`ACPClientStartIdempotenceTests`, `ScarfMiniAppBridgeTests`, `BotConversationTests`, `M0bTransportTests` all green in the full run); no isolation reruns needed.

Fails-without-the-fix verified by temporary revert for the three behavioural fixes:
- #5 (`display.compact` back to `== "true"`) → 4 expectations red.
- #2 (`git show HEAD:GatewayViewModel.swift`) → `supersededGatewayLoadStopsBetweenItsProbes` red (2 pairing probes).
- #4 (`failureWins: false` at the `enable` call site) → 3 expectations red.

## Fresh-eyes findings on my own diff (all resolved before commit)

1. Adding `skill` to `HermesCronJob.CodingKeys` would have removed it from the `extra` sweep and silently STRIPPED it from every jobs.json Scarf writes back — switched to a private `LegacySkillKey` container.
2. First cut treated `skills: null` as an explicit empty list; upstream `skills is None` is the arm that falls back to `skill`. Fixed with a `decodeNil` check + test.
3. First cut included `y`/`n` in the ssl_verify boolish set; verified against PyYAML that they are strings, so they stay CA paths. Test pins it.
4. Checked the boolish sweep did not silently flip any TRUE-default key: only `telegram.require_mention` carries `default: true`, and its default is unchanged.
5. Verified the gateway rewrite did not move the spawn onto the main actor — `gatewayProbesStillRunOffTheMainActor` asserts `Thread.isMainThread == false` inside the injected runner.

## Charter

C1: every change is reader- or doc-level except the gateway task shape; no capability floor moves, and no pre-target host renders differently except where it previously rendered a value the host disagreed with. C5/C10 preserved. C7 respected — production files staged explicitly by path; `.memory/`, `tasks/`, `TASKS.md`, `documents/` left dirty.

## Memory

Appended `## Whole-surface remediation — P17` to `scarf/decisions/hermes-v0-21-1-compatibility-decisions` (loader-vs-reader boolish rule, ssl_verify split, `Task.cancel` not reaching a detached child, direct jobs.json reads bypassing Hermes normalisation, the legacy-alias `CodingKey` convention, the two testing conventions, and the finding-8 no-op). No new note created — no separate durable Hermes contract warranted one.

## Tasks created

None — nothing out of scope surfaced.

