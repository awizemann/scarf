---
id: t-2d258871
title: Config parity: derived test over setSetting keys (convert the convention into a build gate)
status: done
added: 2026-07-14
---

## Description

Drift-audit HIGH finding (2026-07-14). The 3e0184d parser fix removed the drift, but NOTHING enforces write/read parity going forward — a new SettingsViewModel.setSetting("x.y") added without a matching HermesConfig+YAML reader saves-but-reloads-stale silently (the web_tools.* / dropdown class, already bit twice). Current guard scarfTests/HermesFileServiceConfigParityTests.swift is a FIXED-FIXTURE test (~20 hardcoded keys) — it proves the parser swap works but does NOT cover the 120 setSetting writers. FIX: a derived parity test that enumerates the setSetting(...) dotted-key literals (reflection or a code-gen/scan step) and asserts each is non-default-readable through HermesConfig(yaml:). Converts "remember to add the key to ScarfCore" from documentation into a failing build. Files: SettingsViewModel.swift (120 writers), HermesConfig+YAML.swift (reader), HermesFileServiceConfigParityTests.swift. Risk: LOW (test-only). HIGH value — closes the recurrence surface for the class we spent this session fixing.

## Plan



## Artifacts



