---
id: t-d714835d
title: UI gate P2d journey: Chat over ACP (Live plan, real key)
status: done
added: 2026-09-08
---

## Description

Live-plan XCUITest (XCTSkip unless SCARF_UITEST_LIVE=1): open Chat, send a short deterministic prompt ("Reply with exactly: PONG"), assert the reply bubble renders and the session appears in Sessions. Bounded timeout, screenshot on failure. Uses the fixture home's copied credentials. Depends on P1a + P1b.

## Plan



## Artifacts

**Commit:** `1812c92e` — *test(ui-gate): Live chat journey — send over ACP, assert the reply and the session* (branch `worktree-agent-a8ee8ef8be457bf23`, based on `ui-gate` @ `8240bd66`; not pushed).

**Files**
- `scarf/scarfUITests/ChatJourneyUITests.swift` (new, 1 test) — `requireLive()` → `hermes -z` provider probe → Sessions baseline → Chat → type + verify → send → assistant bubble carrying "PONG" → Sessions gained exactly one session. Screenshots: `keepAlways` on failure (plus a labels dump and an AX-tree dump), `deleteOnSuccess` on the two green shots. tearDown compares `~/.hermes/state.db` mtime.
- `scarf/scarf/Features/Chat/Views/RichChatInputBar.swift` — two identifiers only, no behaviour change: `chat.composer.input` (on the `TextEditor` itself, before the background/overlay modifiers) and `chat.composer.send`.

**Runs** (serialized; `pgrep` checked free before each)
| plan | fixture | result | wall |
|---|---|---|---|
| Full | — | **skipped** (clean XCTSkip) | 13 s |
| Live | no | pass | 43 s (63 s incl. build) |
| Live | yes | pass | 43 s |
| Live | yes | pass | 42 s |
| Live | yes (after the a11y-label revert) | pass | 44 s |

Command: `TEST_RUNNER_SCARF_UITEST_FIXTURE=<path> xcodebuild test -project scarf/scarf.xcodeproj -scheme scarf -destination 'platform=macOS' -testPlan Live -only-testing:scarfUITests/ChatJourneyUITests -skipPackagePluginValidation -skipMacroValidation`. Fixture built by `scripts/ui-fixture/make-ui-fixture.sh` in 27 s.

**Tokens per run:** two real turns — the `hermes -z` probe and the chat turn (info bar reported ~9.7K in / 44 out, `$0.0000 est.` on the fixture's free model). Plus the fixture build's own seeded sessions (a few cents, once).

**Isolation:** `~/.hermes/state.db` mtime `Sep 4 17:37:33` before and after all runs — untouched. No `Thread.sleep`. Every wait is a chunked `waitForExistence` or an `XCTNSPredicateExpectation`.

**Product bugs:** none. The two failures along the way were both test-side (AX `value` vs `label`; enumerating matches while the transcript streams). Filed **t-353099de** (low) for the icon-only send button's missing accessibility label — written, then reverted out of this commit because it needs six-locale catalog entries to keep `LocalizationCatalogTests` green.

**Memory:** appended five observations to *XCUITest input and click reliability on macOS: what actually works* (AX value-not-label + truncation, role-scoped bubble matching, never enumerate while streaming, empty home has no stats line, probe the provider first).

