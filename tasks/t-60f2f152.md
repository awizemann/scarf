---
id: t-60f2f152
title: Chat fix 2 — engagement gate + dedup guard (deaf transcript, merged duplicate sends)
status: done
added: 2026-07-13
---

## Description

S2+S4 root causes (CONFIRMED). (a) Pre-engagement gate (RichChatViewModel.swift:1324-1334) must NEVER drop promptComplete — turn accounting, isAgentWorking, and failure bubbles ride on it — and the gate should open wherever a prompt is actually sent, not only in addUserMessage. (b) sendViaACP's dedup guard (ChatViewModel.swift:841-843) matches re-sent text against DB history; scope it to the autoStart optimistic echo only (negative-id local messages or explicit flag) so re-sending prior text is neither invisible nor double-delivered (Hermes alternation-repair merges consecutive user messages into one row — the "duplicate bubble" was one merged row).

## Plan



## Artifacts



