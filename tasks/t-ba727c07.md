---
id: t-ba727c07
title: Audit P39: managed-install refusals — config set / save_config exit-0 verdicts
status: doing
added: 2026-09-10
priority: high
---

## Description

Round-4 whole-surface audit (documents/hermes-v0.21.1-whole-surface-audit-round4.md, CLI-verdicts section). Needs round-4 product decision 1 (managed Hermes host support: per-verb markers, connect-time detection + read-only surfaces, or both).

- HIGH · PRE · `hermes config set` managed arm refuses at exit 0: `set_config_value` opens `if is_managed(): managed_error("set configuration values"); return` (`hermes_cli/config.py:3450-3452`, `managed_error` `:445-455` @ v2026.9.7, stderr). Every write through `enqueueConfigWrite`'s default arm (`SettingsViewModel.swift:223-224,269-273,295-296,210-212`) and `PlatformSetupHelpers.commitSave`'s per-key loop (`:104-108`, all 15 forms) banners "Saved" over an untouched file. Success lines `✓ Set {key} = … in {path}` / `✓ Set {key} in {env}` (`:3468`, `:3521`) exist at all 32 tags; managed arm from v2026.3.28. Fix shape: `HermesCLIMarkers.configSetSuccess = ["Set "]` anchored, `configSetFailure = ["Cannot set", "✗ Invalid config key", "Config key not set:"]`, `HermesConfigSet.judge` beside `HermesConfigUnset.judge`, `enqueueConfigWrite` verdict non-optional for `config set`, `commitSave` routed through it. P38 corrected the two doc comments that claimed the opposite.
- HIGH · PRE · `save_config` refuses the same way (`config.py:2315-2318`) and its callers print success afterwards: `cmd_enable`/`cmd_disable` → `✓ Plugin … enabled.` / `⊘ Plugin … disabled.` (`plugins_cmd.py:115-120,1022-1023,1196-1198`), `cmd_mcp_remove` → `✓ Removed '<name>' from config` (`mcp_config.py:110-121,523`), `_cmd_skills_trust` → `Trusted: <root>` (`main_agent_cmds.py:218-235`). Scarf: `PluginsViewModel.swift:368-380,391-397`, `MCPServersViewModel.swift:147-166`, `ProjectSkillsViewModel.swift:47-78`. Fix: one shared `HermesCLIMarkers.managedRefusal` (`is managed by`, or the three `Cannot save/set/unset configuration` spellings) in every config-mutating verdict with `failureWins: true`.
- MED · PRE · `BotAgentConfigService.unsetValue` (`:514-524`) hand-builds `["config","unset",key]`, no `hasConfigUnset` gate, and `BotAgentViewModel.isBenignUnset` (`:385-388`) treats exit 0 as success — the seventh `config unset` door outside P37's helper. Needs round-4 decision 11. Fix: route through `HermesConfigUnset.argv/judge`, thread capabilities, test `Config key not set:` on both exit codes.
- LOW · PRE · `BotAgentViewModel.swift:380` cites `config.py:6009,6041,6059` and `BotAgentConfigService.swift:290` cites `:6058-6060`; file is 3891 lines @ v2026.9.7; the three `Config key not set:` sites are `:3542`, `:3561`, `:3579`.
- LOW · PRE · Missing `--` on `config set <key> <value>` / `config unset <key>` (`SettingsViewModel.swift:211`, `HermesCLIOutcome.swift:656`; both positionals `nargs="?"`, `subcommands/config.py:24-34`) — a value like `-1` exits 2.
Related: t-32794e9f (set_config_value/config check/migrate managed audit — the enumeration above scopes it; fold in).

## Plan



## Artifacts



