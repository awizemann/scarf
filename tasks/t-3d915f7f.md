---
id: t-3d915f7f
title: Projects P5: scarf-projects MCP server (bundled, auto-registered)
status: done
added: 2026-09-03
priority: high
---

## Description

Phase 5 of projects-first-class (branch feat/projects-first-class; depends on P1-P4). A stdio MCP server shipped inside Scarf.app giving agents structured project CRUD, wrapping the SAME ScarfCore services the UI uses (no parallel write paths): project_list, project_get, project_register (mints uuid, writes project.json, upserts registry — replaces skill step 8), project_update_dashboard (validates against the real widget schema, returns actionable errors instead of writing bad JSON), project_set_config (preserves keychain:// secret routing), project_add_slash_command, project_validate (Project Doctor as a tool). Implementation: new Swift executable target (SPM, e.g. ScarfProjectsMCP) bundled in the app, speaking MCP over stdio; auto-registered unconditionally into the LOCAL Hermes mcpServers config (per Alan: no toggle — first-class part of Scarf), pointing at the bundle binary path; remote SSH hosts are out of scope (skill fallback). Registration update must survive app relocation (re-point path at launch). Tests: tool-level tests against TempHermesHome; end-to-end smoke via stdio harness.

## Plan



## Artifacts



