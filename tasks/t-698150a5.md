---
id: t-698150a5
title: A11y P2: label macOS list rows + zoom button check
status: done
added: 2026-08-28
priority: high
---

## Description

From Walkabout shakedown W22: every AXRow in the macOS Projects sidebar (ProjectsSidebar.swift), Manage Servers (ManageServersView.swift), and Skills (SkillsView.swift) lists exposes label: "". Add meaningful .accessibilityLabel (project/server/skill name, plus salient state) to row content, using .accessibilityElement(children: .combine) where appropriate. Also investigate the zoom-button finding ("AX label is tooltip prose"); no "zoom" string exists in Swift sources, so determine whether it's the system window widget (document, don't patch) or an app control (fix).

## Plan



## Artifacts

Uncommitted edits: ProjectsSidebar.swift (project rows combined + projectRowLabel: "Name, in Folder, archived"), ManageServersView.swift (rowLabel(for:) "Name, user@host:port (home)"; Local row "Local, this Mac"; star/ellipsis buttons labelled "Default server"/"Set as default server", "Server actions"), SkillsView.swift (skillRowLabel "Name, pinned, disabled"; hub rows + "Install <name>"; update rows "<id>, update available, <old> to <new>"; kept-local-edits rows). Zoom button verdict: system NSWindow zoom widget (no "zoom" anywhere in app sources; plain WindowGroup, no standardWindowButton use) — AppKit-owned label, no patch warranted. Build succeeded (isolated DerivedData dd-p2).

