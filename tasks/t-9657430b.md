---
id: t-9657430b
title: Local models follow-up — route residual provider writers through LocalModelConfigPlan
status: done
added: 2026-07-13
---

## Description

T4 audit design finding (reported, not fixed): `alignProviderToModelPrefix` (chat mismatch banner) and the CredentialPools post-auth provider swap still write via `setModelAndProvider`, which never clears local keys — switching away from a local provider through those two paths can strand stale `model.base_url`/`model.api_key`/`model.api_mode`. Route both through LocalModelConfigPlan. Also from the audit: api_key round-trip renders in a plain TextField (consider SecureField), and no test pins that hosts route Saves through the plan (UI↔plan drift guard).

## Plan



## Artifacts

commit 35e901e — alignProviderToModelPrefix + CredentialPools swap plan-routed via new LocalModelConfigPlan.operations(selectingRemoteModel:provider:current:) HermesConfig overload; stripPrefixFromModelDefault plan-routed with no-clear semantics preserved; ModelPickerSheet api_key -> SecureField; setModelAndProvider marked LEGACY (iOS-only caller remains). 6 new plan tests; ScarfCore 940/940, scarfTests 244/244, Mac Debug build green.

