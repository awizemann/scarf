---
id: t-5324c6fc
title: Audit P21: Output-verdict correctness
status: done
added: 2026-09-09
priority: high
---

## Description

Round-2 whole-surface audit phase P21. Source: `documents/hermes-v0.21.1-whole-surface-audit-round2.md`; brief: `documents/hermes-v0.21.1-parity-agent-brief.md`. Opus agent, tests that fail without the fix, fresh-eyes review, memory note.

Class: verdicts that judge the wrong thing — including one P9 introduced.

- HIGH · A SUCCESSFUL `mcp login` can be reported as a failure: `finish()` judges `output`, but the termination handler nils the pipe's `readabilityHandler` and runs the verdict with no guarantee the `✓ Authenticated …` chunk was read or hopped to the MainActor. Pre-P9 the verdict was output-independent, so P9 made a latent drain race load-bearing, and it makes P12's EOF `decoder.flush()` (`:122`) unreachable when termination wins. Fix shape: drain to EOF before judging · `scarf/scarf/Features/MCPServers/ViewModels/MCPLoginController.swift:137-147` (esp. `:140`, `:302`) · `hermes_cli/mcp_config.py:695,697`
- HIGH · `pairing list`'s two hint lines parse as pending pairings with a live "Approve" button (`Approve with: hermes pairing approve …` → platform `Approve`, code `with:`; `The code the bot DM'd…` → `The`/`code`) · `scarf/scarf/Features/Gateway/ViewModels/GatewayViewModel.swift:339-355` · `hermes_cli/pairing.py:38-39`
- HIGH · `cmd_install` and `cmd_update` BOTH call `_run_capability_consent(...)` and discard the bool — round 1 only caught `enable`. A capability-declaring plugin installed/updated from Scarf reports success while its capabilities were fail-closed · `scarf/scarf/Features/Plugins/ViewModels/PluginsViewModel.swift:288-296,302-304,391-396` · `hermes_cli/plugins_cmd.py:764,822,1092-1098`
- MED · `hermes skills update` still judged by exit code; `do_update` is `-> None`, prints `No updates available.` at exit 0, and `Updated N skill(s).` counts ATTEMPTS · `scarf/Packages/ScarfCore/Sources/ScarfCore/ViewModels/SkillsViewModel.swift:922-951` · `hermes_cli/skills_hub.py:831,848-850,871-872`
- MED · A paired user whose `user_name` is empty is dropped (`parts.count >= 3` rejects the 2-token row) and cannot be revoked · `GatewayViewModel.swift:346` · `pairing.py:48`
- MED · `--fail-on critical` means exit 0 also covers high/moderate/low advisories; the `.clean` arm prints the tail of a report that IS listing vulnerabilities with no distinguishing label · `scarf/scarf/Features/Health/ViewModels/HealthViewModel.swift:752,777-781` · `hermes_cli/security_audit.py:311-312`
- LOW · A success marker matched as a bare substring can outrank a real failure when `failureWins:false` — `_print_tier1_advisory` quotes SKILL.md content into the report before `install_from_quarantine` can raise · `scarf/Packages/ScarfCore/Sources/ScarfCore/Services/HermesCLIOutcome.swift:104-112,129` · `skills_hub.py:704,714-720`
- LOW · `runAndReload`'s exit-code arm builds `HermesCLIOutcome(succeeded:detail:nil)`, discarding the `_fail` message on a failed `plugins update`/`remove` · `PluginsViewModel.swift:415,421-422` · `plugins_cmd.py:80-83`
- LOW · `pluginsDisableFailure` carries `"was removed."`, which `cmd_disable` can never print (`_refuse_legacy_relay` is wired only into `cmd_enable`) — dead marker · `HermesCLIOutcome.swift:274-277` · `plugins_cmd.py:1182-1198`

## Plan



## Artifacts

Commit `4995e7c0` on `fix/whole-surface-audit-r2`. All nine findings verified against the current Scarf code AND the tagged Hermes source before fixing; **all nine were real** — no NO-OPs.

**Shipped**
1. HIGH `mcp login` drain race — `MCPLoginController` restructured: the reader sequences decoded text into a lock-guarded `OutputInbox` (the `Task { @MainActor }` hops from the pipe queue are not mutually ordered), the termination handler LEAVES `readabilityHandler` installed and only records the exit status, and `pump()` judges once EOF **and** the exit status have both arrived. A 2 s grace deadline (armed on termination) covers a pipe still held by a grandchild. `defaultProcess` extracted; an injectable `ProcessFactory` makes the interleaving testable. P22's `proc.run()` move is untouched and unobstructed.
2. HIGH `pairing list` hint lines — `parsePairing(output:)` extracted from the spawn and the two `pairing.py:38-39` hints filtered by their own leading text (they arrive at v2026.8.3; absent below → no-op on older hosts, C1).
3. HIGH `plugins install`/`update` consent discard — `update` now output-judged with `pluginsUpdateSuccess`/`Failure` + `failureWins: true` (both success lines print AFTER the consent screen, :826/:828); `install` gets `HermesPluginInstallOutcome.capabilitiesNotGranted` and reports the refusal instead of "Installed and enabled". `_run_capability_consent` in `cmd_install`/`cmd_update` first appears at v2026.8.13; the success lines go back to v2026.6.19.
4. MED `skills update` — new `HermesSkillsUpdateReport` fields (`noUpdatesAvailable`, `attemptedCount`, `installedCount`, `failureDetail`). `Updated N skill(s).` counts ATTEMPTS (`len(updates) - len(skipped_local)`, printed after the loop); the honest per-skill success is `do_install`'s `Installed:` (`:720`). `finishUpdateAll` says "Update attempted — <reason>" when nothing landed, "No updates available" for `:831`, and "Update reported nothing" for exit 0 with no recognised line.
5. MED empty `user_name` — approved rows accept 2 tokens (`a.get("user_name") or ""`, `:48`, same since v2026.6.19); the nameless user was dropped from the list and could not be revoked.
6. MED `security audit` — new `HermesSecurityAuditReport.parse` (report head `:255`/`:257` + severity rows `:264`, byte-identical back to v2026.5.29, the `hasHermesAudit` floor). The `.clean` arm now says "No critical advisories · N finding(s): 2 high · 1 moderate."
7. LOW anchored success markers — `judge(successAnchored:)` matches `hasPrefix` after ANSI-strip, trim and a leading status glyph (`unglyphed`). On for skills install/uninstall, sessions export, mcp login, cron run, skills audit; the `plugins` markers stay substrings (mid-sentence clauses). `failureWins` untouched, per site.
8. LOW `_fail` detail forwarded — `PluginsViewModel.failLine` prefers the `Error:` line over the last line (`_require_installed_plugin` :415 appends `Installed plugins: …` after the reason).
9. LOW dead `"was removed."` marker deleted from `pluginsDisableFailure`, with the floor walk in the doc comment: over every `v2026.*` tag carrying `hermes_cli/plugins_cmd.py` it first appears at v2026.8.19 (:1424/:1439, both inside `cmd_enable` :1405, above `cmd_disable` :1710) and at v2026.9.7 lives in `_refuse_legacy_relay`, called only from `cmd_enable`.

**Beyond the finding list (in scope, load-bearing)**: `reloadSkills()`'s exit-code verdict fixed (`do_audit` is `-> None`; judged by `Auditing <n> skill(s)...` / `No hub-installed skills to audit.`) — copy left to P25 per product decision 2. And `HermesCLIVerdict.stripANSI`'s pattern was a raw string, so `\u{1B}` reached ICU verbatim and it stripped nothing; anchoring depends on it.

**Tests** — `scarf/Packages/ScarfCore/Tests/ScarfCoreTests/HermesCLIVerdictP21Tests.swift` (14) and `scarf/scarfTests/AuditP21VerdictTests.swift` (9). The drain test drives the REAL `start()` with a real `Process`/`Pipe` whose fake `hermes` backgrounds its last write into a subshell, so the parent exits strictly first; verified it FAILS (3 issues) when the old nil-handler-and-judge termination arm is restored. The EOF-alone test blocks the child on a gate file rather than a clock. ScarfCore 2551/2551. Mac target: only pre-existing failures (`SettingsWriteReadParityTests`, `AllConfigWritersParityTests` — confirmed failing with this work stashed) plus parallel-load flakes that pass in isolation.

**Tasks filed**: `t-bd119897` (same drain race in `OAuthFlowController`, where it fails the other way — a missed failure marker reads as success), `t-c9895ecb` (give `HealthViewModel` a `HermesCLIRunner` seam so the audit label branch is testable).

