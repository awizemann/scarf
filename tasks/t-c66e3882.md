---
id: t-c66e3882
title: Audit P14: Kanban dead gate surface, goal/diagnostics, sessions rename, HermesDataService dead code
status: todo
added: 2026-09-08
---

## Description

From documents/hermes-v0.21.1-whole-surface-audit.md. Brief: documents/hermes-v0.21.1-parity-agent-brief.md. Read memory on the kanban CLI contracts first (search "kanban").
1. `hallucination_gate_status` / `auto_blocked_reason` are emitted by NO Hermes version (kanban_output.py:18-24 _TASK_DICT_FIELDS; kanban_db.py:700-940 has no such columns; absent at v2026.8.31 too). Either delete the fields and every consumer (HermesKanbanTask.swift:46-55,193-194,237-238; KanbanInspectorPane.swift:376-478,862-869; KanbanCardView.swift:78-82,176-183,230-234,254-260,374-384; KanbanHallucinationGate; board VM optimistic override) or re-derive from a real signal (task_events / completion_blocked_hallucination, kanban_db.py:2629). Decide from the source; record the decision in memory.
2. `goal_mode`/`goal_max_turns`/`diagnostics` never reach `list --json`/`show --json`; diagnostics only via `hermes kanban diagnostics --json` (kanban.py:627-680). Either fetch and merge by task id (gated, one extra call per board load, off-main) or drop the decode paths + gates (HermesKanbanTask.swift:56-59,86-93,195,201-202,243,256-257; KanbanCardView.swift:293-297,322-329,411-416; KanbanInspectorPane.swift:446-448,813-817).
3. SessionsViewModel.swift:488-490 `sessions rename`: `["sessions","rename","--", id, title]` (title nargs="+").
4. KanbanCreateSheet.swift:50,176-179 + KanbanCreateRequest.swift:24-32: DEFAULT_FAILURE_LIMIT = 2 (kanban_db_dispatch.py:33), 1 = no retries; fix copy/default, resolve TODO(WS-3-Q6).
5. KanbanInspectorPane.swift:896 tooltip: Hermes has `archive --rm`; board ships Delete permanently.
6. HermesDataService.swift: delete dead fetchMessageCount/fetchUserMessageCount/fetchToolUsage/fetchSessionStartHours/fetchSessionDaysOfWeek (:1272-1279,1606-1686); resolve `api_call_count` by column name (:2093-2097).

## Plan

Decision (Alan, 2026-09-09): DELETE the hallucination-gate surface rather than re-derive it. Rationale: Hermes never emits `hallucination_gate_status`/`auto_blocked_reason` on any version; it only appends a `completion_blocked_hallucination` task_event (kanban_db.py:2629 at v2026.9.7) and the stall already surfaces through `last_failure_error` (added Phase 6). If richer diagnosis is wanted later, design it from the real event payload as its own feature. Goal/diagnostics: drop decode paths unless `kanban diagnostics --json` is a one-call off-main addition; decide from source.

## Artifacts



