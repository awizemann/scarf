---
id: t-d8992262
title: v0.21.1 P8: remediate cross-phase fresh-eyes audit findings
status: done
added: 2026-09-08
priority: high
---

## Description

Phase 8 of the Hermes v0.21.1 parity plan (brief: documents/hermes-v0.21.1-parity-agent-brief.md). Fix every finding from the whole-branch adversarial review of `feat/hermes-v0211-parity`. Each fix needs a test that fails without it.

HIGH
H1. ModelCatalogService.swift:712 `imageGenModels` was replaced with the fal-only FAL_MODELS table, dropping ids other backends still resolve from `image_gen.model` at v2026.9.7 (plugins/image_gen/krea/__init__.py:32,38 krea-2-medium/large; check openai, xai, openrouter, deepinfra, meta-ai, openai-codex plugins likewise). Ungated → changes pre-target rendering (C1). Fix: per-backend ids merged with the fal table (verify each plugin's accepted ids at the tag), or filter the list by the configured `image_gen.provider`. Delete the M0cServicesTests "stale-model negative list" that enshrines the regression.
H2. HermesConfig+YAML.swift:44-49 `boolTrueDefault` treats any unrecognised scalar as true; HermesYAML.swift:225 stores raw `afterColon` so `false  # comment` or `"false"` → true, and one save writes it back. Affects display.resume_last_session, gateway.trust_env, updates.check, model.streaming, tool_loop_guardrails.non_interactive_hard_stop_enabled, and pre-target `<platform>.gateway_restart_notification` (:498). Fix: one normaliser (strip quotes, drop ` #…` suffix, trim) applied before the falsy-set test, shared by bool()/int(); parameterised tests with comment and quoted spellings.
H3. Three new JSON parsers slice the payload from stdout+stderr merged (HermesSkillsHubParser.swift:116-120, HermesComputerUseStatus.swift:110-114, HermesPluginCompat.swift:100-104; merge at HermesFileService.swift:2448 and SkillsViewModel.swift:853). A stderr line containing `]`/`}` breaks the slice, and for skills search the fallback is the broken table parser. Fix: parse stdout only via the split runner (runHermesCLISplit or equivalent), keep the slice as fallback; test with a stderr warning containing `[skipped]`.
H4. HermesV0211CronParityTests.swift:313-326: the `Dispatch:` / `⚠ Delivery UNVERIFIED:` fixture is assigned to `_` and never asserted; the test passes with CronView.dispatchSummary / deliveryUnverifiedBanner deleted. Fix: assert the renderers' output against the fixture strings.

MED
M1. Unconsumed flags: `hasGatewayMultiplexerStatus` (HermesCapabilities.swift:1174) and `hasComputerUseDoctorJSON` (:1067). Drop both plus their tests (the multiplexer badge is output-detected, which is correct; doctor --json was deliberately not consumed) — unless consuming is a one-liner that improves copy.
M2. Missing `--` before positional user strings: SkillsViewModel.swift:432 (skills search query), MCPLoginController.swift:61, CredentialPoolsViewModel.swift:543/550/556, ProjectTemplateInstaller.swift:324,336, FleetApplyPlan.swift:401-402. Verify each parser accepts `--` (argparse does) and add it; tests with a `-`-leading value.
M3. ProjectTemplateInstaller.swift:307-337 hand-rolls the cron create argv that FleetApplyPlan.cronCreateArguments(…, paused:) builds; unify on one builder; drop the unconditional `existingBefore` at :292 when the paused path is taken.
M4. HermesFileService.swift:1687-1745 `replaceOrInsertNestedScalar` matches only an exact `oauth:` header; an inline-flow `oauth: {client_id: x}` gets a duplicate block inserted. Fix: detect `block:` prefix with non-empty remainder and bail out untouched (surface an error), never insert a second header; test it.
M5. HermesCronDoctorParser.swift:162,220-226 `isPlausibleJobID` rejects digit-less ids (legal for dict-shaped stores, cron/jobs.py:1271), swallowing a job and misattributing its issue. Fix the plausibility rule using the header grammar (two-space indent + id + name) rather than digit presence; test with `nightly-backup`.
M6. HealthViewModel.swift:331-336 / HermesComputerUseStatus.swift:22 render any non-"ok" probe status as a red error. Treat only a known failure vocabulary as problems (verify tools/computer_use/permissions.py:51 and the cua-driver values at the tag); unknown → neutral.
M7. MCPLoginController.swift:96 drops a chunk on a UTF-8 boundary split (use an incremental decoder / buffer bytes until newline); :54-56,101-107,123-129 stop() must clear terminationHandler so a retried login is not marked failed by run A.
M8. AgentTab.swift:87-108 Fast Mode: on hosts without `hasServiceTierBoundedModes` (incl. unknown version) render the original toggle, picker only on v0.21.1+ (C1 rendering parity). For GatewayBehaviorViewModel.swift:39 restart-notification default and HermesTool.swift:65,136-146 roster/rename: establish the first tag each added platform id and the PlatformConfig default appear (walk tags back to v0.6.0); gate any row whose floor is above v0.6.0 on the matching isV0xx flag; record floors in doc comments.
M9. HermesV0211SearchIndexTests.swift:49-67 uses a hand-stripped 14-column messages DDL; use the full v0.21.1 DDL from HermesV0211SchemaTests so everyColumnScarfSelectsExists covers searchMessages.
M10. Tests that pass with the feature deleted: GatewayViewModelTests.swift:150-156, HermesV0211SchemaTests.swift:219-230, HermesStateDBReplacementTests.swift:72-92 (observe the gh#102 short-circuit: assert the handle identity is retained). Rewrite each to exercise production code.

LOW (do them; they are small)
L1. SkillsViewModel.swift:430/:122 read capabilities before load() may resolve; make hub search/browse await or re-run on capability change (see the CronView .onChange pattern).
L2. HermesDataService.swift:898 `activeClause.replacingOccurrences("m.", "")` — build the un-aliased clause from a shared helper instead of string surgery.
L3. SQLValueInliner.swift:13-18 restore the guard-rail wording and state precisely why raw search terms are safe (quote-doubling + char() splitting), rather than listing them as an accepted source.
L4. AgentTab.swift:137 unreachable `case nil: raw`. L5. CronView.swift:130 gate the past-one-shot pre-check on isV0211OrLater directly, not hasCronCreatePaused. L6. SkillsViewModel.swift:589-596 stale claim: `skills uninstall --yes` exists at v2026.9.7 (subcommands/skills.py:89-92); gate it and drop the stdin "y\n" hack on hosts that have it (find its floor). L7. HermesV0211SearchIndexTests.swift:180-181 fix the rationale comment. L8. check-hermes-tables.py: also match `register_provider` as an attribute call and report it in skipped. L9. test the `catch_up` dispatch kind; delete dead HermesPluginCompatHit.init(from:); hoist the duplicated `caps(_:)` test helper.

Findings judged sound by the reviewer (do not redo): exit-code contracts, C5, argv correctness elsewhere, capability floors, pre-target traces for failure-deliver/kanban/oauth.flow, C4 FTS detection, C3, GatewayConfigWriter sibling safety, oauth.flow patcher choice, device-prompt fidelity, roster set assertions, config defaults split, Computer Use probe gating/timeout.

## Plan



## Artifacts

All 23 findings addressed on `feat/hermes-v0211-parity`; none disputed. 9 commits, 23867474..e80ce2f2.

| # | Verdict | Commit | Test |
|---|---|---|---|
| H1 | fixed | 633f76f6 | `M0cServicesTests.imageGenModelAllowlistShape` (rewritten; the stale-model negative list is gone) — union of the four backends that read top-level `image_gen.model`; meta-ai's `muse-image-1.0` deliberately out (v2026.9.7-only, ungated list, C1) |
| H2 | fixed | 079ef4ef | `HermesV0211ConfigTests.falsySpellingsTurnOffATrueByDefaultKey` (11 spellings) + truthy/numeric/`#`/platform-restart cases; new `HermesYAML.normalizedScalar` |
| H3 | fixed | a879d285 | `HermesJSONStreamPurityTests` (slice-over-merged-streams fails; `hubSearchParsesTheJSONDespiteAWarningOnStderr` drives the real `searchHub` through a split-stream transport) |
| H4 | fixed | a879d285 | `HermesV0211CronParityTests.cronListDispatchAndUnverifiedRowsMatchScarfsRendering` asserts against the CLI fixture; renderers moved to `CronDispatchStamp.summary` / `HermesCronJob.deliveryUnverifiedNote`; `dispatchSummaryCoversOnTimeAndCatchUp` adds the catch_up shape (also L9) |
| M1 | fixed | a32422b4 | deletion — both flags and their assertions removed |
| M2 | fixed | a32422b4 | `cronCreateArgsEndsOptionsBeforeTheUserPositionals`, `hubSearchEndsOptionsBeforeTheQuery`, updated `CredentialPoolsAdminArgvTests`; mcp login server also behind `--` |
| M3 | fixed | a32422b4 | `templateAndFleetCreatesShareTheBuilder`; `FleetApplyPlan.cronCreateArgs` gained a primitive overload, installer delegates, `existingBefore` only taken on the create-then-pause path |
| M4 | fixed | 12486247 + e80ce2f2 | `writingFlowRefusesAnInlineFlowOAuthMappingAndChangesNothing`, `writingFlowAcceptsAHeaderWithATrailingComment` |
| M5 | fixed | 12486247 | `HermesV021CronParityTests.digitLessSlugIDsAfterATracebackAreStillHeaders` — rule is now charset + (digit / `-` / `_` / long hex); a bare single word mid-traceback stays rejected (documented) |
| M6 | fixed | 12486247 | `HermesComputerUseStatusTests.probeSeverityOnlyReadsKnownFailureSpellingsAsFaults` (11 spellings); unknown → warning, never red |
| M7 | fixed | a32422b4 | `IncrementalUTF8DecoderTests` (4 tests, every byte boundary); `stop()` bumps a generation and clears `terminationHandler` |
| M8 | fixed | 1f84c73d + e80ce2f2 | `fastModeRendersTheToggleBelowV0211AndThePickerAtOrAbove`, `addedPlatformRowsCarryTheirVerifiedFloors`, `aRowIsHiddenBelowItsFloorAndOnAnUndetectedHost`. Floors walked over every tag: weixin 0.9, qqbot 0.10, irc 0.12, msgraph_webhook 0.14, photon 0.17; dingtalk/sms/api_server 0.4 and wecom 0.6 need none. `bluebubbles` floor is 0.9 but is deliberately NOT enforced (pre-existing row; gating it would hide a row on an undetected host). `gateway_restart_notification` floor v0.13 confirmed (b71f80e6ce, first tagged v2026.5.7), default True since its first commit — the existing flag was already right, doc comment now cites it |
| M9 | fixed | 8633bc95 | search-index fixture uses the release DDL from `HermesV0211SchemaTests`; `everyColumnScarfSelectsExists` now runs `searchMessages` over a real `messages_fts` |
| M10 | fixed | 8633bc95 | `serviceLoadednessIsDecidedByTheRealMarkers`, `messagesDDLIsUnchangedFromV0210` (now PRAGMA over a real DB), `anUntouchedDBIsNotReopened` (TEMP-table handle probe; verified failing with the gh#102 short-circuit removed) |
| L1 | fixed | cb27ee04 | `resolvedCapabilities()` awaited before the `--json` decision (no isolated test — the probe is a real subprocess; covered by inspection + the H3 test's explicit snapshot) |
| L2 | fixed | cb27ee04 | `neitherSearchPassEmitsAHalfRewrittenPredicate`; `searchActiveClause(alias:)` is the one definition |
| L3 | fixed | cb27ee04 | doc-only: says why the raw LIKE terms are inert (quote doubling + `char()` splitting) instead of listing them as an accepted source |
| L4 | fixed | cb27ee04 | unreachable `case nil` removed |
| L5 | fixed | cb27ee04 | `CronView` gates the past-one-shot pre-check on `isV0211OrLater` directly |
| L6 | fixed | cb27ee04 | `skillsUninstallSendsYesOnlyFromItsFloor`; floor v2026.8.19 = 0.20.5 (`hasSkillsUninstallYes`), stdin hack dropped above it |
| L7 | fixed | cb27ee04 | rationale comment corrected (it is the fixture's property, not Hermes's) |
| L8 | fixed | cb27ee04 | `check-hermes-tables.py` matches attribute-form `register_provider`; script still OK |
| L9 | fixed | a879d285 + cb27ee04 | catch_up covered, dead `HermesPluginCompatHit.init(from:)` deleted, `caps(_:)` hoisted into `HermesHost` (CapabilityFixtures.swift) |

Tests: ScarfCore 2401 + 42 pass; app target 841 pass; Debug build clean. `ACPClientStartIdempotenceTests` flaked once under full parallel load and passes in isolation (known).

Fresh-eyes audit of my own diff found two real bugs, both fixed in e80ce2f2: (1) the M4 refusal decided on the raw remainder, so `oauth:  # note` was treated as an inline mapping and fell through to inserting the duplicate header it exists to prevent; (2) gating `bluebubbles` on its true 0.9 floor would remove a row users already see whenever the version probe has not answered.

