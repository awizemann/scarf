---
id: t-1559ec68
title: Audit P40: gateway/mcp/plugins/skills verdicts and the OAuthFlowController drain
status: todo
added: 2026-09-10
priority: high
---

## Description

Round-4 whole-surface audit (documents/hermes-v0.21.1-whole-surface-audit-round4.md, CLI-verdicts section). Needs round-4 product decisions 2 (gateway banner copy; "nothing running" as success or failure), 3 (plugins update security-disable as a third state), 4 (config migrate offerable non-interactively).

- HIGH · PRE · `gateway start|stop|restart` judged by exit code, each with an exit-0 failure arm: `_cmd_stop` prints `✗ No gateway processes found` (`hermes_cli/gateway.py:5993`) / `✗ No gateway running for this profile` (`:5998`, from v2026.4.30) and returns; `launchd_start` returns without `✓ Service started` when bootstrap fails (`:3926-3928,3938-3939`); `_no_backend_exit` entries with code `None` (`:5828,5844,5873-5874`); `gateway_command` returns None (`:5659-5667,6164-6167`), `cmd_gateway` discards (`main.py:1736-1742`). Sites: `GatewayViewModel.swift:491,498-513`, `HealthViewModel.swift:512-516,537-542`, `HermesFileService.swift:2487-2491` (`stopHermes`), `scarfApp.swift:613-615` (two feed Analytics `succeeded:`). Fix: one `HermesGatewayServiceVerdict` (per-verb markers, `successAnchored: true`), `stopHermes()` returns the verdict.
- HIGH · PRE · `mcp remove` judged by exit code; `_lookup_server` prints `✗ Server '<name>' not found in config.` and `cmd_mcp_remove` returns (`mcp_config.py:98-107,515-532`); `mcp_command` returns None (`:878-913`), `cmd_mcp` forward_return=False (`main.py:1755-1762,1798`). `deleteServer` flashes "Removed" on exit 0 (`MCPServersViewModel.swift:150-158`, `HermesFileService.swift:880-883`). Markers: `✓ Removed '<n>' from config` (`:524`) vs `✗ Server '…' not found in config.` (`:104`). Fix: `HermesMCPRemoveVerdict`, anchored.
- HIGH · PRE · t-bd119897 still reproduces: `OAuthFlowController` (`:129-151,232-252`) judges before EOF on unordered `Task { @MainActor }` hops and nils the handler at termination, so a late `did not return credentials` / `Token exchange failed` chunk is dropped → false success. Cure is `MCPLoginController`'s `OutputInbox`/`markEOF`/`pump()`/`scheduleDrainDeadline`/`ProcessFactory` (`:101-167`). Port or hoist into a shared `DrainedProcessRun` (`NousAuthFlow.swift:118`, `SpotifyAuthFlow.swift:109`, `HermesProxyService.swift:102` share the shape). Close t-bd119897 with it.
- MED · PRE · `plugins update` whose post-pull scan disables the plugin still reports "Updated": `_rescan_after_update` prints `Plugin '<name>' has been disabled.` then `cmd_update` prints `✓ Plugin <name> updated.` (`plugins_cmd.py:810,828,832-851`), both exit 0; `pluginsUpdateFailure` lacks the marker and `pluginsUpdateSuccess`'s `"updated."` is unanchored (`HermesCLIOutcome.swift:362-390`, `PluginsViewModel.swift:334-341`).
- MED · PRE · `mcp test` verdict is a bare `output.contains("✗")` over plugin/server-authored text (`HermesFileService.swift:906-925`; emitter `mcp_config.py:33-36,49-52,583-621`). Fix: anchored `Connected (` / `Tools discovered:` vs `Connection failed (` / `not found in config.`.
- MED · PRE · `hermes config migrate` prompts on stdin (`migrate_config(interactive=True)` → `_prompt_and_save_env` → `line_input` → bare `input()`, no EOFError guard; `config.py:1289-1297,1354-1369,1375-1382,3653-3690`, `cli_output.py:29-37`); Scarf gives no stdin, so it can abort after applying migrations and before stamping `_config_version`. `runConfigMigrate` (`SettingsViewModel.swift:1249-1271`, button `AdvancedTab.swift:178`).
- LOW · PRE · `ProjectSkillsViewModel.setTrusted` (`:53-78`) discards CLI output; `_cmd_skills_trust`'s `Not a directory:` / `Not inside a git checkout` arms exit 0 (`main_agent_cmds.py:179-205`).
- LOW · PRE · Missing `--`: `mcp remove/test <name>` (`HermesFileService.swift:882,889`; `subcommands/mcp.py:44-50`), `mcp add <name> --command …` positional before flags (`HermesMCPAdd.swift:255,284`; `:27-42`), `skills trust|untrust <root>` (`ProjectSkillsScanner.swift:142`; `subcommands/skills.py:28-35`).

## Plan



## Artifacts



