---
id: t-d518c6c6
title: F1: Audit fixes — cron resume semantics + unread accuracy + remote probe tests
status: done
added: 2026-08-20
---

## Description

Fresh-eyes audit fixes (branch feat/hermes-v0204-parity). (a) iOS cron resume: withEnabled(true) must recompute next_run_at like Hermes resume_job (cron/jobs.py:2212-2233) — else a stale past next_run_at causes a spurious immediate catch-up fire consuming repeat.times; also refuse/flag re-enabling a past-deadline one-shot (Hermes raises). Prefer running `hermes cron resume <id>` via the transport from IOSCronViewModel if feasible (mirrors macOS CronViewModel.swift:130); else port compute_next_run for the schedule forms Scarf supports and guard one-shots. (b) stateIcon/state display: port effective_job_state (cron/jobs.py:585-602) — enabled=true is authoritative, never display paused for an enabled job. (c) Unread indicator: HermesSession.isUnread compares lastActivityAt??startedAt but Hermes session_unread (hermes_state.py:8452-8466) uses MAX(last_activity_at, MAX(messages.timestamp)) — heartbeat is rate-limited/best-effort so the messages-max term dominates; add the correlated subquery term to the session SELECT (Hermes runs the same per-row) and update isUnread + tests pinning the OLD semantics. (d) Add remote-backend tests for the v0.20.4 probes (RemoteSQLiteBackend.swift:327-342 hasHidden/hasLastReadAt/hasListableChildSupport incl. SQLite-3.37 fallback) — currently zero coverage.

## Plan



## Artifacts



