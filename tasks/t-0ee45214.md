---
id: t-0ee45214
title: Audit P37: cross-phase review remediation, round 3 (P30–P36)
status: done
added: 2026-09-10
priority: high
---

## Description

Remediate the 14 findings of the cross-phase review of branch fix/whole-surface-audit-r3 (P30–P36). Findings list with decisions: scratchpad file p37-findings.md (copied into this task's artifacts on completion). Headline: P36's cron re-anchorings never landed in HermesCapabilities/CronViewModel and one is wrong at v2026.8.31 (HIGH); /steer ungated below 0.13 in the ACP menu; ProfileRoutesYAML reader cannot decode what YAMLScalar.doubleQuoted now emits; six unsetSetting rows shell config unset below hasConfigUnset; refused .env read still blanks setup-form fields; AuxiliaryReasoningEffort duplicates the effort vocabulary with a stale provenance; plus eight LOWs (tab escape comment, /clear doc, localization of new strings, P22 scan test for main-actor waits, parseNestedYAML purge scope, skills_hub.py path, hex decoder sign, route-editor copy).

## Plan



## Artifacts

## Commits (branch `fix/whole-surface-audit-r3`)

| hash | subject |
|---|---|
| `43781219` | re-anchor the cron citation P36 got wrong and qualify every skills_hub.py path |
| `d2d6f904` | gate /steer on its real ACP floor, and collapse three YAML unquoters into one |
| `63d78f37` | put the `config unset` floor inside the one helper, so no row can miss it |
| `7d5aa818` | a refused read no longer blanks the form it could not read |
| `c4626b94` | retire the second reasoning-effort vocabulary, and sweep for main-actor waits |
| `f9be1120` | `#expect`'s comment argument takes a `Comment`, not a `String` |
| `3d49415e` | localize the strings P30–P35 added, and match the route editor's copy |
| `867f1462` | translate the nine new catalogue keys — the repo has a gate for this |
| `8a41879a` | the three defects the fresh-eyes pass found in P37's own diff |

## Shipped, per finding

**1 (HIGH) — cron citations.** Every blob opened. `_is_recoverable_error_job` is `cron/jobs.py:664-692` @ `v2026.8.31` (`is_terminal_job` :659); `:509-522` is its **v2026.9.7** range, which is why P36's number looked plausible. The v2026.8.27 `update_job` blocks are `:2270-2278` and `:2367-2375` — the cited `:2272` / `:2369` both started mid-condition. `HermesCapabilities.swift:1347,1350` and `CronViewModel.swift:390` corrected; `:2583-2595` / `:2684-2696` @ v2026.8.31 verified correct and left. `43781219` + `d2d6f904`.

**2 (MED) — `/steer` ungated.** New `hasACPSteer` (v0.13.0) with the four-test pattern; roster gates on it; `hasACPSteerOnIdle` expressed AS it rather than restating the floor. Walked: `acp_adapter/server.py:170` @ `v2026.5.7` (line above `queue` at `:171`), nothing in `acp_adapter/` @ `v2026.4.30`. **Two existing tests asserted the bug** and were inverted.

**3 / 13 (MED/LOW) — writer/reader asymmetry + hex sign.** ONE `YAMLScalar.unquote` carrying PyYAML's whole table; `HermesFileService.unquote` and `HermesBotProfileYAML.unquote` are forwarders; the routes reader uses it. `HermesYAML.stripYAMLQuotes` deliberately untouched (arbitrary Hermes-written values). Round-trip + idempotence over 19 scalars.

**4 (MED) — six `config unset` rows below the floor.** Gate moved INSIDE `unsetSetting(_:capabilities:)`, `capabilities` required, so a seventh row cannot miss it. Six setters + four tabs threaded; `setApprovalMode`'s redundant guard removed.

**5 (MED) — refused read blanked the form.** `apply` skipped on `envFailure`. (First attempt guarded on `loadFailure` and suppressed a PROVEN `.env` when config.yaml was unreadable — caught by this phase's own fresh-eyes pass, fixed in `8a41879a`.)

**6 (MED) — effort vocabulary.** `AuxiliaryReasoningEffort` retired; picker reads `HermesReasoningEffort.levels(capabilities:)`. Gating is MOOT not missing, and the doc says so with the walked tags: `hasAuxiliaryReasoningEffort` is 0.19.0, the very tag that added `ultra`; `max` is 0.18.1.

**7 (LOW) — tab escape.** `case "\t": out += "\\t"`, comment made truthful. Probed: `yaml.safe_dump("a\tb")` → `"a\tb"`, so this is PyYAML's own spelling. Both unquoters decode it.

**8, 12, 14 (LOW) — docs and copy.** `/clear` dropped; 40 bare `skills_hub.py` citations qualified (a sibling `tools/skills_hub.py` exists at the same tag, so it was genuinely ambiguous); route-editor copy matched to "a tab or a control character", which is what the validator actually refuses.

**9 (LOW) — localization.** `CronRecoveryOffer`'s two hints onto `String(localized:)`; nine keys added to `Localizable.xcstrings` **with all six locales** — `LocalizationCatalogTests` is a gate and English-only failed it.

**10 (LOW) — P22 scan test.** `noNewSynchronousWaitRunsOnTheMainActor`. First version was a no-op (`SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` means no `@MainActor` lines exist in the app targets); corrected version found a REAL second violation, `HealthViewModel.dashboardListenerPID` → **`t-cd9fd829`**, allowlisted with its id beside `AppRelauncher` (`t-b15ba4c3`).

**11 (LOW) — purge scope.** Purge fires only on an already-WRITTEN path. (First version tracked only headers and made a flow list + block list concatenate — caught by the self-audit, fixed in `8a41879a`.)

## Not done, deliberately
- `HermesYAML.stripYAMLQuotes` not folded into the shared decoder — it reads arbitrary Hermes-written values.
- `ProfileRoutesYAML`'s `multiplex_profiles` read stays on `normalizedScalar`: no boolish/null token carries an escape.
- `DispatchGroup.wait`/`DispatchSemaphore.wait` left out of the C10 sweep's primitives — nine sites, most legitimately off-main; triage noted in `t-cd9fd829`.
- The nine new strings' six translations were authored here; no translation-review pass was possible.

## Tests
ScarfCore **2667**; Mac `scarfTests` **1062/1062 serial**; `scarf mobile` (iOS) builds. Known load flakes, each green in isolation: `ACPClientStartIdempotenceTests` (`t-f3820038`) and once `M0bTransportTests.localTransportRunProcessDrainsLargeStdoutAndStderr`.

Watched fail before the fix (revert-in-place, then restore): `aSignedHexBodyIsNotAValidEscape`, `afreshSectionHeaderDoesNotPurgeAFlatDottedSibling`, `profileRoutesRoundTripEveryHostileScalar`, `availableCommandsHidesBothSteerAndQueueOnV012` (8 issues together); `aTabIsEscapedAsBackslashTNotAsAHexByte`; `noClearRowShellsConfigUnsetBelowTheFloor` + `theSharedHelperIsTheGate` (17); `telegramKeepsAPrimedTokenAfterARefusedEnvRead` (2); `theAuxiliaryTabHasNoSecondVocabulary`; the C10 sweep against a planted probe; and for the self-audit round `aFlowListFollowedByABlockListIsStillLastWins` and `aProvenEnvStillRendersWhenOnlyConfigYamlIsRefused`.

`whatsAppCloudKeepsPrimedCredentialsAfterARefusedRead` does NOT fail against the pre-fix code and says so in its doc comment — it pins the boundary of the bug (the config half was already nil-guarded), not the fix.

## Memory
- Appended `## Whole-surface remediation — P37` to `scarf/decisions/hermes-v0-21-1-compatibility-decisions`.
- New: `scarf/conventions/a-source-scan-test-must-be-calibrated-against-the-target-s`.
- Corrected: `scarf/decisions/bot-mode-phase-a-decisions` — its "a local `unquote` (NOT the shared …)" claim is now three-into-one.

## Tasks created
- `t-cd9fd829` — move `HealthViewModel.dashboardListenerPID`'s `lsof` wait off the main actor (found by the corrected C10 sweep).

## The findings file, verbatim

1. HIGH — P36 claims 3 cron re-anchorings that never landed, and one replacement is wrong. `HermesCapabilities.swift:1347,1350` and `CronViewModel.swift:390` still carry `cron/jobs.py:504-522`, `:2272-2278`, `:2369-2375`. At v2026.8.31 `_is_recoverable_error_job` is at `:664` (`is_terminal_job` :659); `:509-522` is correct only at v2026.9.7. At v2026.8.27 the real ranges are `:2270-2278` and `:2367-2375`. Fix: cite `cron/jobs.py:664-687 @ v2026.8.31` (or re-anchor the block to v2026.9.7 `:509-522`); `:1350` → `:2270-2278`, `:2367-2375`; `CronViewModel.swift:390` → `:2367-2375`. Open each tagged blob to confirm before writing.
2. MED — `/steer` is a dead ACP row on pre-v0.13 hosts: `RichChatViewModel.swift:964-976` gates `queue` on `hasACPQueue` but `steer` falls to `default: return true`; `steer` first appears at `v2026.5.7:acp_adapter/server.py:170`, absent at v2026.4.30. Fix: gate `steer` on the same 0.13.0 floor (reuse an existing flag if one is exactly 0.13.0, else add one with the four-test pattern); test that a v0.12 host does not offer `/steer`.
3. MED — writer/reader asymmetry on the `profile_routes` path: `ProfileRoutesWriter.swift:106/108/116` now emit via `YAMLScalar.quoteIfNeeded` (can double-quote with `\n`/`\\`/`\"`/`\xNN` escapes) but the reader `ProfileRoutesYAML.swift:95,326` goes through `HermesYAML.normalizedScalar`/`stripYAMLQuotes` (`HermesYAML.swift:495-497`) which returns a double-quoted body verbatim. Fix: give the routes reader the same decoder `HermesFileService.unquote` has (one shared decoder, not a third copy), plus a round-trip idempotence test (`a\\b`, `a\nb`, `a\u{01}b`, `"quoted"`).
4. MED — six pre-existing `unsetSetting` rows shell `config unset` below its floor: `SettingsViewModel.swift:656,719,802,845,943,954`; only `setApprovalMode` (`:607`) has the `hasConfigUnset` gate. DECISION: gate all six the same way (`guard capabilities.hasConfigUnset else { showSaveFailure(HermesConfigUnset.belowFloorHint(key:)) }`), through one shared helper so a seventh row cannot miss it; test with a v0.18.x host that no `config unset` argv is issued.
5. MED — a refused `.env` read still blanks the form's credential fields: `PlatformSetupHelpers.swift:345` calls `apply(snapshot)` unconditionally; on `envFailure` `loadEnv` returns `([:], failure)` so every `env["…"] ?? ""` overwrites live values. DECISION: skip `apply` entirely when `snapshot.loadFailure != nil` (the form keeps whatever it showed; the latched refusal already blocks Save); test that a WhatsApp/Telegram form with a primed token keeps it after a refused env read.
6. MED — `AuxiliaryReasoningEffort` (`HermesConfig.swift:47-55`) hard-codes `max, ultra` with a stale "v0.20.0" provenance doc, offered ungated at `AuxiliaryTab.swift:268`. Fix: express its option list via `HermesReasoningEffort.levels(capabilities:)` (or equivalent shared source) so there is one vocabulary; if the surface's `hasAuxiliaryReasoningEffort` (v0.19) floor makes gating moot for `ultra`, say so in the doc with the walked floors (0.18.1/0.19.0).
7. LOW — `YAMLScalar.swift:228-231` comment says tab stays raw; the `< 0x20` arm escapes it as `\x09`. Fix: add `case "\t": out += "\\t"` and make the comment truthful (tab is escaped); confirm both `unquote`s decode `\t`.
8. LOW — `RichChatViewModel.swift:1349` doc comment still names `/clear`. Drop it.
9. LOW — new user-facing strings outside the localization catalogue: `CronRecoveryOffer.swift:44,51` (plain `String` literals), `HermesConfigUnset.belowFloorHint`, `"Cleared \(key) — the host default applies"`, `"Couldn't clear \(key)…"`, `BotEditorSheet.swift:273`, `ProfileRoutesSection.swift:392`. Fix: `String(localized:)` for each (check how ScarfCore strings are localized — bundle/table — and follow the existing pattern) and add the keys to `Localizable.xcstrings`.
10. LOW — `AppRelauncher.swift:91` does a bounded 20 s wait on the main actor (already filed as t-b15ba4c3). Fix here: extend `MainActorSpawnDisciplineP22Tests` so a synchronous `waitDraining`/`waitUntilExit` inside a `@MainActor` type is flagged, with `AppRelauncher` as the one documented allowance until t-b15ba4c3 lands.
11. LOW — `HermesYAML.swift:216-227` purge fires on every section header, not only a duplicate; comment says otherwise. Fix: guard on "this path already has descendants" (preferred, with a test) or reword.
12. LOW — bare `skills_hub.py` citations at `HermesCLIOutcome.swift:412,420,423-425` and `HermesCLIOutcomeTests.swift:70-106`; qualify as `hermes_cli/skills_hub.py`.
13. LOW — `HermesFileService.swift:2422` `UInt32(hexDigits, radix: 16)` accepts a leading `+`; add the `allSatisfy(\.isHexDigit)` guard `HermesBotProfileYAML.unquote:766` has (better: share one decoder — see finding 3).
14. LOW — `ProfileRoutesSection.swift:392` says "a tab or line break"; match `BotEditorSheet.swift:273`'s "a tab or a control character".

