---
id: t-6679d648
title: Audit P20: Config-read defaults and platform read paths
status: todo
added: 2026-09-09
priority: high
---

## Description

Round-2 whole-surface audit phase P20. Source: `documents/hermes-v0.21.1-whole-surface-audit-round2.md`; brief: `documents/hermes-v0.21.1-parity-agent-brief.md`. Opus agent, tests that fail without the fix, fresh-eyes review, memory note. See product decision 5 in the report (approvals.mode picker semantics) before scoping that item.

All paths under `scarf/Packages/ScarfCore/Sources/ScarfCore/Parsing/HermesConfig+YAML.swift` unless noted.

- HIGH · `approvals.mode` parses to `"manual"`; Hermes's schema default has been `"smart"` since v0.19.0 — a default that changed INSIDE the window needs an absence sentinel + capability resolution · `:656`, `scarf/scarf/Features/Settings/Views/Tabs/AgentTab.swift:48-52` · `config_defaults.py:1534`
- HIGH · `memory.memory_char_limit`/`user_char_limit`/`nudge_interval` parse to `0` vs Hermes 2200/1375/10; steppers range `500...10_000`/`1...50`, so rows render outside their own range · `:632-634`, `MemoryTab.swift:16-18` · `config_defaults.py:1200,1203`; `agent/agent_init.py:1263,1266`
- HIGH · `voice.auto_tts` read with `boolTrueDefault`; Hermes defaults False in both layers · `:649-652`, `VoiceTab.swift:27` · `config_defaults.py:1121`; `cli_voice_mixin.py:516`
- HIGH · `memory.memory_enabled` reads `boolish(default:false)`; schema default is True · `:631` · `config_defaults.py:1194`
- MED · `display.show_reasoning` default false; Hermes flipped False→True at v2026.7.20 · `:648`, `DisplayTab.swift:83` · `config_defaults.py:784`, `cli.py:2584`
- MED · `approvals.timeout` 60 vs 300; `agent.gateway_notify_interval` 600 vs 180 · `:688,663`, `AgentTab.swift:53,65` · `config_defaults.py:1535,196`
- MED · Four `terminal.container_*` defaults 0/0/0/false vs Hermes 1 CPU / 5120 MB / 51200 MB / persistent True · `:172-175`, `TerminalTab.swift:25-28` · `config_defaults.py:318-321`
- MED · `secrets.bitwarden.override_existing` default false vs schema True · `:427` · `config_defaults.py:2174`
- MED · `agent.reasoning_effort` defaults `"medium"` but the key is in no schema layer; absent means provider default · `:654`, `AgentTab.swift:30` · `cli.py:2777`, `hermes_constants.py:876-889`
- MED · `telegram.require_mention` default true vs Hermes's only reader defaulting false · `:379` · `plugins/platforms/telegram/adapter.py:5030`
- MED · Slack `require_mention` precedence inverted — `platform_section` prefers a top-level `slack:` block outright · `:498-502` · `gateway/config_loader.py:171-180,226-236,283`
- MED · `platforms.slack.extra.reply_in_thread` read from `extra:` only, but it is a `_SHARED_KEYS` member and a top-level `slack.reply_in_thread` is bridged in and overwrites · `:503` · `config_loader.py:200,283`
- MED · Top-level `slack.reply_to_mode` is a path Hermes never reads (not `_SHARED_KEYS`; `merge_platform_sections` never merges a bare top-level block) — same class as the mattermost bug P13 fixed · `:474` · `config_loader.py:149-152,197-215`; `gateway/config.py:437`
- MED · `setMultiplexProfiles` always writes `gateway.multiplex_profiles`, but a non-null TOP-LEVEL `multiplex_profiles` wins in both Hermes and Scarf's parser — reports "Saved", changes nothing · `scarf/scarf/Features/Settings/ViewModels/SettingsViewModel.swift:919-921`, `ProfileRoutesSection.swift:179` · `gateway/config.py:708-710`
- MED · Ten picker-driven closed enums still read through `str()` (no trailing-comment strip): `agent.reasoning_effort`, `agent.tool_use_enforcement`, `logging.level`, `memory.provider`, `display.personality`, `tts.openai.voice`, `tts.neutts.device`, `stt.local.model`, `stt.groq.model`, `wake_word.capture`, `terminal.modal_mode` · `:654,680,339,658,629,199,201,210,231,245,177`
- LOW · `permanent_allowlist` preferred over `command_allowlist`, a key no Hermes version reads · `:142` · `tools/approval.py:327-332`
- LOW · `display.busy_ack_enabled` is env-bridged with `str(value)` and compared `!= "true"`, so `1` disables it in Hermes while `boolTrueDefault` reads ON · `:726` · `gateway/run.py:1816-1820`, `run_busy.py:727`
- LOW · `multiplexProfileAllowlist` maps a present-but-null top-level key to `[]` (fail-closed) but Hermes's `pick` returns None = serve-all · `:832-839` · `gateway/config.py:45-70,668-670,734`

## Plan



## Artifacts



