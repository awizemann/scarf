---
id: t-f163150a
title: Audit P27: check-hermes-tables.py hardening
status: done
added: 2026-09-09
---

## Description

Round-2 whole-surface audit phase P27. Source: `documents/hermes-v0.21.1-whole-surface-audit-round2.md`; brief: `documents/hermes-v0.21.1-parity-agent-brief.md`. Opus agent, tests that fail without the fix, fresh-eyes review, memory note. No HIGH findings — medium priority.

The drift-detection script has two fail-open holes that make its `OK` verdict untrustworthy — the same trap `ALIASES` sprang at v0.21.1, which lane 1 guards with `sys.exit` (`:113`) and lane 5 does not.

- MED · Lane 5 fails OPEN on a table-shape change: `parse_models_dev_map` returns `{}` for anything that isn't a plain `ast.Dict`, the lane then only WARNs "not found", and the script still exits 0 · `scripts/check-hermes-tables.py:131-135,444,465-467`
- MED · The overall verdict is vacuous on any machine without `~/.hermes/models_dev_cache.json`: lanes 3 and 4 WARN-and-skip and the script still prints `OK` and exits 0 — 2 of 5 lanes silently disabled. It also reads the hermes WORKING TREE rather than a tag (reviewer's checkout was `v2026.9.7-385-g9e6c4100cb` and it printed OK regardless) · `scripts/check-hermes-tables.py:371,389-390,393,436-437,476`

Note (verified sound, do not redo): all five lanes DO fail correctly when an entry is deleted (empirically checked by deleting `grok`, `kilo`, renaming `moa`, deleting `meta-ai`/`gemini`), and the doc-comment masking bug in `swift_block` is genuinely fixed — commenting out `"meta-ai": "meta"` still FAILs. The two off-by-a-few citations in the script (`:139,222,422`) are covered by P26.

## Plan



## Artifacts

Commits on `fix/whole-surface-audit-r2`:
- `8b1df76d` fix(scripts): make the provider-table check fail closed and read the target tag
- `479886cf` chore(git): ignore `__pycache__` from the new script tests

**Both findings verified real and FIXED** (no NO-OPs).

1. MED · lane 5 fails OPEN on a table-shape change (`:131-135,444,465-467`) — FIXED. `parse_models_dev_map` now distinguishes absent FILE (→ `None` → SKIP, legitimate on a pre-v0.21 tag) from present-but-reshaped: a non-`ast.Dict` value, a missing/renamed `PROVIDER_TO_MODELS_DEV`, or a dict that parses to zero literal entries all `sys.exit` non-zero, matching lane 1's posture for `ALIASES`.
2. MED · vacuous verdict with no models.dev cache + working-tree read (`:371,389-390,393,436-437,476`) — FIXED.
   - Skips: lanes 3/4/5 append to a `skipped` list, printed as `SKIPPED lane N: <reason>`; the script exits **2** unless `--allow-skip`. Verdict line carries `lanes=N/5`.
   - Tag pinning: new `HermesSource` class reads every Hermes file via `git -C <checkout> show <tag>:<path>` (and `git ls-tree` for the lane-4 plugin walk). `--tag` defaults to the new `HERMES_TARGET_TAG = "v2026.9.7"` module constant — the one machine-readable place the repo records the target (HermesCapabilities has per-flag floors but no single current target; README's "Current target" line was stale at v0.20.4; the memory/wiki record is a managed tier a script must not grep). `--worktree` opts back in and prints `git describe --dirty`. `--tag`/`--worktree` are mutually exclusive. The read mode is printed on entry and in the verdict line. The Hermes checkout is only read — verified clean + HEAD unmoved after every run, and a test asserts it.

**Exact verification command** (recorded in the memory notes too):
```
./scripts/check-hermes-tables.py --tag v2026.9.7
→ OK    aliases=87 aggregators=8 overlays=25 lanes=5/5 read from tag v2026.9.7   (exit 0)
```
with the 3 expected dormant-overlay WARNs (arcee, lmstudio, tencent-tokenhub) and the kimi-coding non-literal-plugin WARN. Also exercised: default (no args) → exit 0; `--worktree` → exit 0 + `working-tree version: v2026.9.7-385-g9e6c4100cb`; `--tag v2026.4.30` → exit 1 with 18 FAILs (old tag, expected); `--tag v2026.1.1` → hard error, unknown tag.

**Tests** — new `scripts/tests/test_check_hermes_tables.py` (+ `__init__.py`, required or discovery refuses the dir), `python3 -m unittest discover -s scripts/tests -t .`: **13 tests, all pass**, 3.3s, stable across reruns. Revert verification (each fix reverted in turn, suite re-run, then restored):
- lane-5 fail-open restored → 4 failures
- skip gating (`if skipped and not args.allow_skip` → `if False`) → 1 failure
- tag read replaced with a working-tree read → 1 failure

`xcodebuild -scheme scarf -configuration Debug build` → **BUILD SUCCEEDED** (no Swift touched).

**Fresh-eyes findings on my own diff, all resolved before commit:**
- the skip hint said "run `hermes models list` once" — an unverified CLI verb, charter C5. Reworded to cite `agent/models_dev.py` as the writer instead.
- `--tag` + `--worktree` together silently let `--worktree` win → now an argparse mutually-exclusive group (the `--tag` default doesn't trip it).
- tag mode dropped the old `git describe` provenance line; harmless at a tag, but it was the only record of what `--worktree` measured → restored as a public `HermesSource.describe()`, printed in worktree mode only.
- `main()` now clears the module-level `failures`/`warnings`/`skipped` lists so a second in-process call can't inherit the first's findings (the test suite calls it twice).

**Also updated** (not committed — `.claude/` is gitignored): `.claude/skills/hermes-release-audit/SKILL.md`, both invocations of the script, to the `--tag` flag form with the `lanes=5/5` requirement and the "don't use --allow-skip to clear the gate" warning. This is the operational counterpart to the charter guardrail at `.memory/charter.md:63`; there is no `operations/` runbook note for the script.

**Memory:** appended `## Whole-surface remediation — P27` to `decisions/hermes-v0-21-1-compatibility-decisions` (6 durable items) and a `[runbook]` line with the exact command + expected verdict to `integration/Hermes Version Compatibility Target`.

**Tasks created:** none — no out-of-phase findings.

