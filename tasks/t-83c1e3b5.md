---
id: t-83c1e3b5
title: Audit P44: chat/settings residue — typed sub-floor /steer //queue, effort picker widening, idle-steer arm, Slack shared-key write
status: todo
added: 2026-09-10
---

## Description

Round-4 whole-surface audit (documents/hermes-v0.21.1-whole-surface-audit-round4.md, capabilities/chat + CLI sections). Needs round-4 product decisions 12 (typed sub-floor `/steer` `/queue`), 13 (effort picker widening for a stored value above the floor), 14 (retire the unreachable idle-steer grey-out).

- MED · PRE · Typed-slash optimistic mirrors for `/queue` and `/steer` are ungated: `ChatViewModel.swift:1267,1293-1299` and iOS `ChatView.swift:1679-1686,1705-1707` paint "Queued — runs after current turn." + chip and `isNonInterruptiveSlash` suppresses the working indicator on a pre-v0.13 host where the adapter has neither (`acp_adapter/server.py:170-171` @ v2026.5.7; unknown → LLM `acp_adapter/commands.py:88-95`). Fix per decision 12: gate on `hasACPQueue`/`hasACPSteer`.
- MED · PRE · Top-level reasoning-effort pickers (`AgentTab.swift:40`, `AuxiliaryTab.swift:277`) offer `HermesReasoningEffort.levels(capabilities:)` with no widening for the stored value, so a 0.18.x host with `ultra` on disk renders blank; `ReasoningOverridesSection` (`AgentTab.swift:248,280-283`) and `WebToolsBackendRoster.finalize` have the fix. Decision 13: `levels(capabilities:selected:)` for all three.
- MED · PRE · t-6fa3fc84 still reproduces: `SlackSetupViewModel.save()` (`:64-66`) writes `platforms.slack.require_mention` / `extra.reply_in_thread` unconditionally; `platform_section` bridges shared keys from ONE section, a top-level `slack:` block replaces the nested one (`gateway/config_loader.py:171-180,197-200`). Read side models it (`sharedPlatformScalar`); write does not. Fix: `multiplexProfilesKey(isTopLevel:)` shape. Close t-6fa3fc84.
- LOW · PRE · `/queue` offered on an idle-but-open session; `_cmd_queue` on idle appends to a queue nothing drains (`acp_adapter/commands.py:285-289`) · `RichChatViewModel.swift:1373-1378`. Add `queue` to the idle grey-out or reword the hint when `!isAgentWorking`.
- LOW · NEW(P37) · `hasACPSteerOnIdle` arm of `disabledSlashCommandNames` (`RichChatViewModel.swift:1376,1420`) is unreachable now that `hasACPSteerOnIdle == hasACPSteer` and the roster hides `steer` below the floor; the localized reason string (six locales) can never render. Decision 14.

## Plan



## Artifacts



