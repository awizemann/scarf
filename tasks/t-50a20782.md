---
id: t-50a20782
title: Projects R1: chokepoint guards — registry, dashboard clamp, identity salt
status: done
added: 2026-09-03
priority: urgent
---

## Description

Remediation batch 1 from the fresh-eyes branch audit (full findings in the audit agent's report; orchestrator has details). Fix: H1 — move the lossy-registry refusal INTO ProjectDashboardService.saveRegistry/ProjectStore.indexInRegistry chokepoints so the 5 unguarded callers (ProjectCockpitViewModel.swift:130 try? save, ProjectUpgradeService.swift:100, ProjectTemplateInstaller.swift:41+314, FleetApplyExecutor.swift:237) cannot write back a salvaged/short list; delete or repoint the now-redundant call-site guards; note ProjectDoctorService.repairBlockReason() at :676 is dead code cited as load-bearing. H2 — ProjectTemplateUninstaller.swift:241 allowEmpty:true with no removed-count check can blank the registry (incl. quarantined-load variant); mirror removeProject's presence check. H3 — clamp DashboardSection.columnCount (ProjectDashboard.swift:213) to 1...12 on the READ path (both renderers crash on negative; confirmed SIGTRAP). M1 — unify the definition of "lossy" across P2 VM (:274 refuses field-salvage) vs doctor/MCP (row-loss only): pick row-loss/quarantine as the blocking definition everywhere, and make the doctor EMIT a finding for salvaged fields so field damage is visible+repairable rather than blocking. M2 — field salvage silently defaults archived/folder (no second copy exists on ScarfProject): preserve unknown/invalid archived/folder as a doctor finding rather than silently normalizing (minimum: doctor finding listing the dropped fields per row). M3 — ProjectIdentity.hostKey (ProjectIdentity.swift:90-97) diverges across host-alias/user-nil/port-nil spellings; canonicalize inputs (documented, frozen) BEFORE release since the format ships now. M8 — zero-byte/read-failed registry returns clean-empty (ProjectDashboardService.swift:65-66), indistinguishable from no-registry; make it lossy/blocked. Tests for every fix; revert-verify the chokepoint guard.

## Plan



## Artifacts



