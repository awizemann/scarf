---
id: t-628227d4
title: Audit P36: round-3 citation sweep + README Hermes-target lines
status: done
added: 2026-09-10
priority: low
---

## Description

Round-3 whole-surface audit (`documents/hermes-v0.21.1-whole-surface-audit-round3.md`). All C2, all PRE unless noted; P29 fixed the NEW ones. Verify every replacement by opening the tagged file.

- `ProfileRoutesSection.swift:164` cites `gateway/run.py:23923` (file is 5475 lines; gate at `:4211`); `:102` cites `gateway/profiles.py:987` (no such file; `hermes_cli/profiles.py:703-713`, `:712`).
- `PowerSettingsWriter.swift:96` cites `model_switch.py:2007` (real: `hermes_cli/model_switch_providers.py:1063`).
- `HermesBotProfileYAML.swift:23,72` cite `tui_gateway/methods_profiles.py:780-863`/`:789-791` (file is 651 lines; size guard `:443`, revisions `:450-473`); `:281` cites `profiles.py:980-986` (real `hermes_cli/profiles.py:635-640`).
- `VoiceTab.swift:69` cites `tools/tts_tool.py:2100-2170` for `_generate_xai_tts` (file is 682 lines; imported at `:55`).
- `HermesCapabilities.swift:330-334` `hasYOLOSlashCommand` "Available in ACP" is false at every tag (fold into P34 if that lands first).
- README.md Hermes-target lines are stale at v0.20.4: `:24` badge `Hermes-v0.20`; `:46` "v0.6 through v0.20"; `:132` "Current target: v0.20.4 (v2026.8.18)"; `:139` credits `/compress` to v0.20.0 (CLI: 0.3.0; ACP: 0.19.1); `:140` "Verified — current target"; table `:134-140` has no rows for v0.20.5–v0.21.1. Machine-readable target is `HERMES_TARGET_TAG = "v2026.9.7"` in `scripts/check-hermes-tables.py`. Fold into the next release-prep README bump.

## Plan



## Artifacts

Commits `3e68bdad` (24 citations re-anchored at v2026.9.7) and `ca6ae1e8` (README target -> v0.21.1 + `scripts/tests/test_readme_hermes_target.py`) on `fix/whole-surface-audit-r3`.

SHIPPED — all seven citations this task named, plus eleven P30–P35-introduced ones found by sweeping `git diff 3e64448e..HEAD`, plus three pre-existing siblings carrying the identical wrong number (`HermesBotIdentity.swift:55`, `ProfileRoutesWriter.swift:111`, `BotModePhaseAB0Tests.swift` header). README: badge, range line, "Current target", four new table rows (v0.20.5 / v0.20.6 / v0.21.0 / v0.21.1), `/compress` credit moved to v0.19.x (ACP `compact` -> `compress` at 0.19.1; CLI since 0.3.0). New 2-test script suite pinning the README's "Current target" to `HERMES_TARGET_TAG`; watched it fail first.

NOT SHIPPED (deliberate) — `hasYOLOSlashCommand` doc (already fixed by P34, verified); `gateway/config.py:773-792` (generous, not wrong); citations outside the two sets (the larger sweep); no Scarf version bump / release notes (release-prep). One rationale corrected in passing: `_coerce_route_id` does rescue plain unquoted ints, so the old "never matches" claim was false.

Tests: ScarfCore 2654 (only the known `ACPClientStartIdempotenceTests` load flake, t-f3820038, green in isolation); Mac `CronRecoveryOfferP30Tests` + `GatewayPairingVerdictP31Tests` 10/10; `scripts/tests` 20/20; `check-hermes-tables.py` OK lanes=5/5; Debug build succeeds.

Memory: P36 section appended to `scarf/decisions/hermes-v0-21-1-compatibility-decisions`.

