---
id: t-32794e9f
title: Give `unsetSetting`'s exit-0 sibling verbs the same output verdict sweep
status: todo
added: 2026-09-10
---

## Description

P35 discovered that `hermes config unset`'s managed-install refusal prints and `return`s (`hermes_cli/config.py:3550-3552` @ v2026.9.7), so it arrives at exit 0, and fixed all `SettingsViewModel.unsetSetting` call sites by moving the verdict onto the helper (`HermesConfigUnset.judge`).

The same `is_managed()` → `managed_error(...)` → bare `return` shape exists on other config-mutating handlers in `hermes_cli/config.py` that Scarf may shell (`set_config_value`'s managed arm, `config migrate`, `config check`). `set_config_value` was believed to exit non-zero on every refusal — verify that claim against the tag rather than inheriting it, and audit `runConfigCheck` / `runConfigMigrate` in `scarf/scarf/Features/Settings/ViewModels/SettingsViewModel.swift` (they ride `enqueueConfigWrite`'s sibling paths and still judge by exit code).

Deliverable: for each, cite the handler at v2026.9.7, say whether it can refuse at exit 0, and either add markers to `HermesCLIMarkers` + a `verdict:` at the call site or record in the doc comment why the exit code is sufficient.

## Plan



## Artifacts



