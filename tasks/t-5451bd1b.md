---
id: t-5451bd1b
title: Chat fix 3 — start-path watchdog, teardown hygiene, non-blocking pipe readers
status: done
added: 2026-07-13
---

## Description

S3 cluster (mechanism CONFIRMED, wedge PLAUSIBLE). (a) Bound the entire resume/start pipeline (incl. spawn + pre-spawn awaits) with a watchdog that clears isStartingSession into an error banner with retry; let a fresh sidebar click supersede a wedged start via sessionStartGeneration instead of disabling rows (ChatSessionListPane.swift:75 currently self-locks). (b) Send session/cancel (or paint a cancellation bubble) before stopACP kills a mid-turn process; call client.stop() in both start-failure catch paths (ChatViewModel.swift:799-806, :1361-1368 currently leak the process + 2 blocked reader threads each). (c) Move ProcessACPChannel readers (:237-277) off the cooperative pool (Thread/readabilityHandler) so blocked pipe reads can't starve Swift concurrency.

## Plan



## Artifacts



