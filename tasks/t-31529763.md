---
id: t-31529763
title: Bots P0: per-bot agent-config foundation
status: done
added: 2026-09-01
priority: urgent
---

## Description

ScarfCore layer for configuring a bot's AGENT (not just identity): verify in v2026.8.31 source how per-profile config works (each profile home has its own config.yaml; `hermes -p <bot> config set/get/unset` should target it — verify _apply_profile_override + config paths), then expose per-bot config reads/writes through the pinnedToProfile context: model/provider pin, skills.disabled, toolsets, MCP server enablement. Reuse existing config parsing/writers against the pinned context — no forked YAML paths. SOUL.md read/write via transport (verify filename/location per profile at the tag). Tests: pinned-context config round-trip local+SSH fixtures, never touching the root profile's config. Branch feat/bot-mode-phase-b.

## Plan



## Artifacts



