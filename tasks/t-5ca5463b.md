---
id: t-5ca5463b
title: Audit F7: localization systemic fix
status: done
added: 2026-09-02
priority: high
---

## Description

Root cause: String-typed display params bind Text's verbatim overload. Convert to LocalizedStringKey (with verbatim escape hatch where call sites pass runtime values, per the ScarfDesign precedent): SettingsComponents.swift (9 params incl. ToggleRow/PickerRow), LoadingOverlay.swift:72, CockpitEmptyState.text, WidgetErrorCard title/reason/hint, CronView's three helpers + placeholders, MCP section helpers, InsightsView sectionHeader/emptyRow/InsightCard.label, ActivityView sectionLabel/FilterChip.label, SessionsView QuickFilter.label/projectFilterLabel, Dashboard StatusCard/StatCard.label, ToolsView menuLabel, GatewayAllowlistKind vocab, BotRosterSort.label, BotPresence → token enum localized app-side, Kanban labels, NSOpenPanel titles, PeersView selected-ternary dropped. Then: high-visibility VM strings to String(localized:) (MiniAppPermission.summary consent sheet, WidgetPathResolver.userMessage, FleetApplyExecutor FieldResult.message, gateway/health status banners) — full VM sweep stays a policy follow-up. Plural-hack keys → real plural variations (Showing %lld sessions, health issues, incidents, Rewound, host/hosts sites); formatDuration/formatTokens locale-aware. Finish with catalog reconciliation: verify previously-dead translated keys are reachable again, add+translate newly-extractable keys ×6, rerun tools/validate-catalog.py + the zero-missing tests.

## Plan



## Artifacts

Commits 6b629f1, e623c29, b17271c, 6a62bf3 on main.

- Components → LocalizedStringKey (+ verbatim escape hatches): SettingsComponents (9 params),
  LoadingOverlay, CockpitEmptyState, WidgetErrorCard, CronView (3 helpers + placeholders +
  headerText), MCP summaryRow, Insights sectionHeader/emptyRow/InsightCard, Activity
  sectionLabel/FilterChip, Sessions QuickFilter/projectFilterLabel, Dashboard
  StatusCard/StatCard, ToolsView menuLabel, GatewayAllowlistKind vocab (Mac + iOS),
  BotRosterSort, BotPresence (ScarfCore token → app-side copy), Kanban sort options,
  NSOpenPanel titles ×7 sheets, PeersView selected-ternary dropped.
- VM strings: MiniAppPermission consent sheet, WidgetPathResolver.userMessage,
  FleetApplyExecutor.FieldResult.message, Health banners. Full VM sweep left as policy follow-up.
- Plurals: 7 hack keys → automatic grammar agreement; the 2 deliberate exceptions untouched.
- formatDuration/formatTokens locale-aware (3 disagreeing formatTokens impls unified).
- Catalog: +216 keys, 215 translated ×6, 7 stale hack keys deleted, 2165 → 2374.
  validate-catalog.py zero errors; fallback list 59 → 60 ("Nous Portal").
- Tests: new LocalizationF7RecoveryTests (4) pins the recovered keys. App 607/78 green,
  ScarfCore 1794/111 green, Mac + iOS build.

Extra defect found and fixed: ActivityView day-group "Today"/"Yesterday" were plain Strings
used as both label and group id.

