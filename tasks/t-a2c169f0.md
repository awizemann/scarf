---
id: t-a2c169f0
title: Projects S2/D2: security mediums + lifecycle integrity batch
status: todo
added: 2026-09-03
priority: high
---

## Description

Combined batch from the P7 audits (documents/reports/2026-09-03-projects-full-surface-audit.md). Security mediums: image widget URL policy (https+host like webview; no file://; ImageWidgetView.swift:46,83-101); mini-app generated flag must not key consent defaults (MiniAppLaunchView.swift:258-263) and grant self-certification via agent-writable miniapp_grants.json needs a Scarf-held secret or default-to-prompt; project_register refuses /, $HOME, ~/.hermes as roots (ProjectMCPTools.swift:169-214); slash-command name validated on LOAD and delete(named:) path-safe (ProjectSlashCommandService.swift:126-134,226-255); cron argv flag injection from template jobs.json; zip inspect size cap; exporter schema note. Lifecycle integrity (data audit H6-H9, M10-M16): rename propagates to project.json (record.name) + doctor emits name/path-divergence findings at diagnose time; uninstall preserves untracked .scarf/ content locally and finishes-or-reports the remote ghost instead of leaving it adoptable; name-keyed removal must not delete duplicate rows blindly; grants revocation on project removal + path-reuse grant check (mirror the cron pathReuseSuspicion); session-map pruning (1MB cap is a live trigger); AGENTS.md managed block stripped on remove-from-list; archive semantics decision (at minimum mirror into record + stop watchers); normalization uniformity (ProjectStore.swift:154, scaffolder, chat lookups).

## Plan



## Artifacts



