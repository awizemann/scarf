---
id: t-7cc4a105
title: v0.21.1 P7: Memory, wiki, version-target reconciliation
status: done
added: 2026-09-08
---

## Description

Phase 7 of the Hermes v0.21.1 parity plan (brief: documents/hermes-v0.21.1-parity-agent-brief.md; findings: documents/hermes-v0.21.1-audit-report.md). Runs after P0–P6 land. Documentation and memory only; no Swift changes unless a doc comment is wrong.

Scope (memophant MCP, project scarf):
1. Write `decisions/hermes-v0-21-1-compatibility-decisions` (what shipped per phase and why, the floors chosen, the deliberate NO-OPs: ACP wire, CLI surface, provider tables, MCP catalog, platform roster, managed scope, security commits, sessions retention UI declined, deliveries.db) and `integration/hermes-v0-21-1-audit-findings` (source-grounded findings incl. the Tavily re-add, the FTS 8 KB ceiling, service_tier bounded modes, gateway multiplexer branch). Link both with [[...]] to Hermes Version Compatibility Target, Hermes Capability Gating Pattern, Hermes v0.21 Compatibility Decisions, Hermes v0.21.0 Audit Findings. Pass source_paths.
2. Update `integration/Hermes Version Compatibility Target` and `integration/Hermes Version Targeting Strategy` (current target v0.21.1 / v2026.9.7; keep-in-sync checklist gains: web backend roster, plugin-provider lane 4, gateway multiplexer branch, cron dispatch rows). Update `conventions/hermes-release-audit-process` with the lesson that a patch tag can re-add a removed surface (Tavily) so removal flags need an upper bound too.
3. wiki/Hermes-Version-Compatibility.md: add the v0.21.1 row to the Verified table and update the headline target line. Update the skill file .claude/skills/hermes-release-audit/SKILL.md "done N times" line and add the removal-window lesson to its Two laws section (this file is NOT a managed tier; commit it).
4. Review every memory note written by P0–P6 agents (recent_activity / search by tag v0.21.1): merge duplicates, retire wrong ones, ensure each has source_paths and lives in one of the six folders. Run memory_health and fix what it flags for the touched notes.
5. Report the list of notes created/edited/retired with permalinks.

## Plan



## Artifacts

Documentation/memory only — no Swift changes, nothing pushed.

## Notes created
- `scarf/integration/hermes-v0-21-1-audit-findings` — **Hermes v0.21.1 Audit Findings** (new). The source-grounded verdict, the seven corrections the phases made to `documents/hermes-v0.21.1-audit-report.md`, what was forced vs adopted, and the deliberate NO-OPs so the next audit does not re-litigate them. `source_paths` on HermesCapabilities / HermesSearchIndex / WebToolsBackendRoster / ModelCatalogService / check-hermes-tables.py / the report. Linked to the target, gating-pattern, decisions, v0.21.0 findings, and FTS notes. Filed under `integration/` to match every prior audit-findings note (the tool nudges for the six canonical folders; the sibling convention won).

## Notes edited
- `scarf/decisions/hermes-v0-21-1-compatibility-decisions` — **restructured, not forked**. Was a Phase-0/1 observation block with `## Relations` stranded mid-note and Phases 2–6 appended after it. Now: a lead paragraph, a 5-bullet `## Observations` carrying the cycle's durable lessons (window-not-floor, walk-the-symbol, true-floor policy, presence-gating, the `bool()` true-default trap), then `## Phase 0` … `## Phase 6` sections, then `## Relations` at the end. Every durable observation kept; repeats merged (the `boolTrueDefault` trap appeared in Phases 1 and 2, the floor-enumeration lesson in Phases 0, 4 and 6).
- `scarf/architecture/hermes-capability-gating-pattern` — +2 gotchas: a removal needs an UPPER bound (Tavily), and "absent at the old tag" must be established by walking the symbol across every tag over every file location (the modularization trap), never by `git show <old-tag>:<new-path>`.
- `scarf/conventions/hermes-release-audit-process` — same two lessons as first-class law-2 corollaries; "done six times (v0.11 → v0.17)" → "done thirteen times (v0.11 → v0.21.1)"; fixed a broken relation (`[[Hermes v0.17.0 Audit Findings]]`, a note that does not exist) → `[[Hermes v0.21.1 Audit Findings]]`.
- `scarf/integration/hermes-version-compatibility-target` — new lead `[target]` observation: current target v0.21.1 (v2026.9.7), landed on `feat/hermes-v0211-parity`, **Scarf version TBD at release prep**; the old bullet now reads as "the last SHIPPED release targets v0.21.0". Keep-in-sync checklist gains the web backend roster (incl. re-checking modelled removals), plugin-provider lane 4, the gateway multiplexer branch, and the cron dispatch rows.
- `scarf/integration/hermes-version-targeting-strategy` — current-target and flag-grouping observations moved to v0.21.1 (v2026.9.7); same four checklist additions.

## Notes retired / merged
None. No duplicates found: the phase agents appended to one decisions note by design, and the only two new notes they created (`architecture/hermes-messages-fts-contract-an-8-kb-tool-prefix-and-two`, the `R3` section on `architecture/hermes-mcp-config-is-a-yaml-mcp-servers-block-scarf`) are correctly foldered, source-anchored, and non-overlapping with the decisions note.

## Wiki
`wiki/Hermes-Version-Compatibility.md`: headline rewritten to "Scarf targets Hermes v0.21.1 (v2026.9.7) … Scarf version TBD, set at release prep" with the v0.21.0 paragraph demoted to "Previously"; new **v0.21.1 / 2026-09-07** Verified-table row (forced work, adopted surfaces, the pre-existing bugs, and the note that several report-labelled v0.21.1 surfaces are really v0.15–v0.18); the v0.21.0 row loses "— current target".

## Skill file — NOT committed (blocked)
`.claude/skills/hermes-release-audit/SKILL.md` is updated on disk: "done thirteen times (v0.11 → v0.21.1)", law 2 retitled "capability-gated at its TRUE floor", the two corollaries added under it, and Step 6's "gate on the minor, not a patch" softened to "establish the real floor first; a removal is a window, a moved surface is not new". It could NOT be committed: `.gitignore:50` ignores `.claude/` wholesale and `git ls-files .claude` is empty, so committing would require `git add -f` against the repo's own ignore policy. Left uncommitted for the user to decide. **No commit hash for this task.**

## memory_health
Scoped scan over the 7 touched notes: 3 flagged, all benign — `outsideTaxonomy` on the two version-target notes and the new findings note (the deliberate `integration/` folder, shared by every Hermes note in the repo) and `codeChanged` on the two version-target notes (anchored to HermesCapabilities/ModelCatalogService/HermesDataService, which this branch changed; reviewed as `content`, so the drift stays honestly visible for a code-claim review at release prep). The `brokenReference` on the audit-process note was fixed. `review_memory` stamped: `claim: "code"` on the v0.21.1 decisions note, the v0.21.1 findings note, and the gating-pattern note (verified against the `// MARK: v0.21.1 (v2026.9.7) flags` group and the "Older floors corrected/added in the v0.21.1 pass" block in HermesCapabilities.swift); `claim: "content"` on the other four.

