---
id: t-2d3584ba
title: Audit P29: round-3 pre-merge regression fixes
status: done
added: 2026-09-10
priority: high
---

## Description

Round-3 pre-merge remediation of the regressions introduced by branch `fix/whole-surface-audit-r2` (P18–P28). Scope: 11 grouped items — ACP `/compress` spelling re-floor (P23), `hasSessionsExportNoRedact` re-floor to v0.18.1 (P25), iOS sentinel write guard (P28), `skills update --force` verdict via report (P21), `yamlScalar` tab guard (P19), `HermesApprovalMode.normalize` boolish sets (P20), `pluginsUpdateFailure` bare `Error:` (P21), `ProcessTimeout` overrun SIGKILL (P22), `detectSignalCLI` off the main actor (P22), test gaps (P23), and a batch of stale citations/comments. Verify each against the tagged Hermes source before fixing; NO-OP with evidence where a reviewer is wrong.

## Plan



## Artifacts

Branch `fix/whole-surface-audit-r2`, 7 commits on top of `bff03f0f`:

- `7a835680` fix(chat): send the compress command the spelling this host's ACP adapter knows — restores the gate as `hasACPCompressSpelling` @ v0.19.1 (ACP `_SLASH_COMMANDS`: `compact` ≤ v2026.7.20/0.19.0 `server.py:459`, `compress` ≥ v2026.7.30/0.19.1 `server.py:574`, no alias either way); both send sites go through `RichChatViewModel.compressSlashName`; `supportsCompress` accepts either spelling; `.empty` → `compact` (C1). Same commit re-floors `hasSessionsExportNoRedact` v0.21.1 → v0.18.1 (`hermes_cli/main.py:13567` @ v2026.7.7, absent v2026.7.1) and fixes the `:394`→`:395` / `:425-440`→`:425-436` cites.
- `ca5b2444` fix(ios-settings): the "Host default" row writes nothing. Save's policy extracted to `SettingEditorSheet.valueToWrite` (a View's `private func save()` over `@State` is untestable, which is why both iOS sentinel bugs shipped green); 3 new tests incl. a source scan pinning the one `saveValue` behind the guard.
- `218c6aec` fix(cli-verdicts): `finishForceUpdate` routed through `HermesSkillsUpdateReport` via a pure `forceUpdateVerdict`; dropped bare `Error:` from `pluginsUpdateFailure`; corrected the seven stale `skills_hub.py` offsets in 3 files. 7 new tests.
- `e5031590` fix(spawns): `waitUntilExit(timeout:)` escalates SIGTERM → bounded poll → SIGKILL → bounded poll (never a bare wait), `detectSignalCLI` is `nonisolated` + its own detached hop, 5 `HealthViewModel` `runHermes` sites name their timeout, Kanban diagnostics comment corrected.
- `edbf2761` fix(config-reads): `yamlScalar` tab guard (real-PyYAML round trip with a tab VALUE in an MCP `env:` row); `HermesApprovalMode.normalize` models PyYAML's full bool sets via `HermesYAML.boolishValue`.
- `14500118` test(capabilities): four-way tests for `hasGeminiKittenTTS` / `hasElevenLabsDeepInfraSTT` / `isV011OrLater`; `hasGeminiKittenTTS` doc re-grounded on the dispatch arms; four inline roster floors → shared `…PlatformFloor` constants + boundary tests; `sessionsRenameIsUngated`'s consumer pin moved to the Mac target.
- `940dbaf1` fix(mcp): transport discriminator reads `url` before `transport`; remaining stale citations (`_has_pause_marker` → v0.20.1, `resume_job` → `:1986-2003`, `pairing.py` `:39-40`/`:49`, `PROVIDER_TO_MODELS_DEV` `:108`, `overlayMetadata` drops the `ai-gateway` example, `photonPlatformFloor` doc `known` → `KnownPlatforms.all`).
- `28799050` fix(p29): fresh-eyes fixes to this phase's own diff — the compress helpers were orphaning `alwaysAvailableCommands`'s doc comment; guarded the pid before `kill(…, SIGKILL)`.

CORRECTIONS to the brief, each with evidence:
- Item 6 said `0` → `.off`. WRONG: round-tripped through the Hermes venv's PyYAML, `0`/`1` load as **ints**, so neither of `_normalize_approval_mode`'s `isinstance` arms matches and Hermes returns `manual`. The numeric spellings are carved out of the boolish read and the reason is in the doc + test.
- Reviewer claim that `tools/tts_tool.py` "does not exist at all at v2026.4.16" is wrong — it exists and contains neither `gemini` nor `kittentts`. The floor is still correct; the doc says the accurate thing.
- `tui_gateway/server.py` `_TUI_EXTRA` decl is `:3845` and the `/compact` tuple `:3846` (line 3844 is blank) — reviewer correct, cite fixed.

NO-OPs: none; every item verified real against the tagged source before fixing.

Tests: ScarfCore 2603/2603 green. Mac: the 9 suites touching this diff (88 tests) green; full `scarfTests` re-run serially. iOS: `scarf mobile` builds; `SettingsEditorSentinelP28Tests` 8/8 green on the iPhone 17 Pro simulator. `scripts/tests` 13/13.

Flaky (pre-existing, NOT P29): `ACPClientStartIdempotenceTests` and `ChatViewModelStartLifecycleTests` fail their 3 s `waitFor` under full-parallel load and pass in isolation both with and without this diff.

