---
id: t-b7d43521
title: UI gate P3: release.sh runs unit + Smoke + Full + Live before archive
status: done
added: 2026-09-08
---

## Description

release.sh builds the fixture (scripts/ui-fixture/make-ui-fixture.sh), runs unit tests and the Smoke, Full and Live test plans against the same DerivedData before archiving; `--skip-ui-tests` escape hatch logged loudly; writes a gate summary (pass counts, wall time, result bundle path) into releases/v<VERSION>/UI-GATE.md. Also a standalone scripts/ui-gate.sh that release.sh calls, so the gate can run alone. Depends on P1a + P1b (plans exist); journeys can land later without changes.

## Plan



## Artifacts

- scripts/ui-gate.sh (new, executable) — standalone UI release gate: --smoke-only / --skip-live / --derived-data / --keep-fixture / --summary
- scripts/release.sh — runs ui-gate.sh before archive; writes releases/v<VERSION>/UI-GATE.md; --skip-ui-tests bypass with loud warning
- BUILDING.md — new "UI release gate" section
- Commit: dee9acb8 on branch worktree-agent-afae8f25e56ef300c (merged forward from ui-gate @ 4dd744dc, which this worktree was NOT originally based on — see note below)
- Memory: operations/ui-gate-sh-contract-and-how-release-sh-calls-it (new), operations/Build and Release Workflow (relation added)
- Verified runs: --smoke-only (74s fixture + 179s test, FAIL — real Dashboard error.banner bug) and --skip-live (44s fixture + 507s test, 14 executed/2 failed — real Dashboard + TemplateInstallUITests bugs). Both exited non-zero correctly and cleaned up their fixture homes. App bugs flagged as a separate spawned task (task_2f2c3462), not fixed here — out of scope.
- Could not exercise --skip-ui-tests inside the real release.sh (its preflight — xcconfig, git-clean, signing cert, notarytool, Sparkle keypair — dies before reaching the gate hook without a maintainer's full signing setup); verified the arg-parse/warn/summary logic in an isolated standalone copy instead.
- Setup note: this worktree's branch was based on main (2e1a26f6), not ui-gate as the task expected; merged ui-gate (4dd744dc) in cleanly (no conflicts) to get the fixture script and test plans before starting.

