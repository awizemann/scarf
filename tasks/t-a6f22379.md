---
id: t-a6f22379
title: Projects D3: transport atomicity parity — iOS truncating write, remote restore, project.json probe
status: todo
added: 2026-09-03
priority: urgent
---

## Description

From the P7 transport-divergence probe (addendum in documents/reports/2026-09-03-projects-full-surface-audit.md). (1) CRITICAL: ScarfIOS CitadelServerTransport.asyncWriteFile (:377-388) truncates in place (SFTP .truncate, 32KB chunks) — make it temp+rename atomic and add TransportPrivateMode 0600 parity; this falsifies the "atomic on every transport" premise at ProjectDashboardService.swift:346-350 (update that comment). (2) CRITICAL: RemoteRestoreService (:378-402) rewrites remote projects.json via truncating Python open(path,'w') bypassing salvage/quarantine/refusal/.bak, and try? at :394 (+ pauseAllCronJobs :420-424) swallows transport failures into reported success — route through the guarded writer (or temp+rename with refusal semantics) and propagate errors. (3) HIGH: ProjectStore.load (:77-94) has no absent-vs-unreadable discrimination — extend the registry's stat+retry probe so an SSH blip cannot nil the load and let derive() commit a stripped record; consider .bak for writeRecord. (4) HIGH: remote watcher mtime-seconds signature + first-delta swallow on poller restart (SSHTransport.swift:788-799, HermesFileWatcher restart path) — add size to the signature and yield-on-restart-delta; optionally compare save baseline. (5) MEDIUM: add -T to sshArgs for readFile/stat/listDirectory channel cleanliness; quote the scp remote spec (:390); mkdir parent in SSH writeFile (parity with LocalTransport:101-104); preserve destination mode on remote replace. (6) iOS guards: session map + cron jobs.json writes go through guarded shapes once D1 lands; surface salvage on iOS by using loadRegistryDetailed (banner-equivalent). (7) P6 miss: BuiltinSlashCommands.bundle (scarf-new.md:25, scarf-dashboard.md:20, scarf-widget.md:29) still instructs unconditional direct edits — make them tool-first with the same fallback language as the skill.

## Plan



## Artifacts



