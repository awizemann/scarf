---
id: t-2d39ff83
title: v0.21.1 P2: Gateway — multiplexer status, platform roster, dead fields
status: done
added: 2026-09-08
priority: high
---

## Description

Phase 2 of the Hermes v0.21.1 parity plan (brief: documents/hermes-v0.21.1-parity-agent-brief.md; findings: documents/hermes-v0.21.1-audit-report.md). Depends on P0. Absorbs and closes existing task t-0c0b1aa7 (read it first with get_task).

Scope:
1. Finding A6: `hermes gateway status` at v2026.9.7 (hermes_cli/gateway.py:6112-6115) has a new first branch `✓ Gateway is running via the default-profile multiplexer` + `Manage it from the default profile: hermes gateway status` with no PID. Add a third case to `isServiceLoaded` in GatewayViewModel.swift:178-215 so a multiplexed satellite profile shows as running/served (with a "served by default profile" badge or subtitle), refresh the stale doc comment line references (now gateway.py:6127/6133). Also `gateway list` (hermes_cli/gateway.py:1517-1522) new trailing clause `— served by the default multiplexer`: make HermesGatewayListService.swift:113-123 recognise it explicitly (isRunning true, pid nil, servedByMultiplexer true) and render that state. Drift-alarm fixtures from the tagged source for both.
2. Finding B4: widen `KnownPlatforms.all` (HermesTool.swift:94) with the user-configurable platforms that exist at both tags: dingtalk, sms, irc, wecom, weixin, bluebubbles, qqbot, msgraph_webhook, api_server, photon. Do NOT add wecom_callback, relay, local. For a2a and raft: add only if the platform has a real user-facing setup (check plugins/platforms/<name>/plugin.yaml and adapter env/config keys); record the decision. For each added platform verify the allowlist key and kind (chats/channels/users) in its adapter at v2026.9.7 and wire GatewayAllowlistKind accordingly (dingtalk's existing `.chats` mapping becomes reachable). Discord: `discord.allowed_channels` is real (plugins/platforms/discord/adapter.py:4620-4622, enforced :3689-3698); replace the KNOWN GAP nil at GatewayAllowlistKind.swift:79-96 with a real mapping and make sure the Discord setup view path that writes it exists (GatewayBehaviorSection). Verify `gateway list` renders the new platform names.
3. Finding B5: remove the dead `slashCommandNoticeTTLSeconds` field and fix the doc comments in GatewayPlatformSettings.swift:6-7,50-66 (busy-ack is `display.busy_ack_enabled`, global). Fix `gatewayRestartNotification` default drift: Hermes default is True (gateway/config.py PlatformConfig); Scarf's model defaults false, so an unset key renders "off". Make the unset state render as Hermes behaves.
4. Tests: parser fixtures for both new gateway output branches; a roster test asserting KnownPlatforms against the Platform enum at v2026.9.7 minus the documented internal exclusions; allowlist kind tests for each new platform.

When done, move t-0c0b1aa7 to done with a pointer to this task.

## Plan



## Artifacts

Two commits on `feat/hermes-v0211-parity`:

- **f716fd64** `fix(gateway): a profile served by the default multiplexer reads as served, not dead` — finding A6.
  - `GatewayViewModel.swift`: new `isServedByMultiplexer(statusOutput:)` (matches `via the default-profile multiplexer`, gateway.py:6114) + a third case in `isServiceLoaded` tested BEFORE the `pid != nil` fallback; `MessagingGatewayInfo.isServedByMultiplexer`; doc line refs refreshed to gateway.py:6127/6133 at v2026.9.7.
  - `GatewayView.swift`: renders "Served by default profile" instead of "Loaded", and forces the status pill to "running" (the satellite's stored `gateway_state` is stale).
  - `HermesGatewayListService.swift`: `ProfileEntry.servedByMultiplexer` recognised explicitly from the `— served by the default multiplexer` clause (gateway.py:1520-1522), surfaced in `headerDigest`.
  - Tests: `GatewayViewModelTests` — tagged-shape fixture, pre-target outputs never match, "not running" still beats the marker.

- **70c564c5** `fix(gateway): real platform roster, Discord's allowlist, and two dead settings` — findings B4 + B5 (both pre-existing at v2026.8.31, so ungated).
  - `HermesTool.swift`: +9 rows (dingtalk, sms, irc, wecom, weixin, qqbot, msgraph_webhook, api_server, photon) and the `imessage` → `bluebubbles` **rename** (Scarf's `imessage` is not a Hermes id at any version; the adapter is bluebubbles and the form always wrote `BLUEBUBBLES_*`). Legacy spelling still resolves in `icon(for:)`, `PlatformsView` and `identifyingEnvVar`.
  - `GatewayAllowlistKind.swift`: `discord` → `.channels` (KNOWN GAP closed; adapter.py:4620-4622, enforced :5675-5677); the other nine stay `nil` (user allowlists / CIDR ACL), documented per platform.
  - `DiscordSetupView.swift`: wired to `GatewayBehaviorSection` so the mapping reaches a real editor; `HermesConfig+YAML` reads `discord` too (else write-only key).
  - `GatewayPlatformSettings.swift`: `slashCommandNoticeTTLSeconds` **deleted** (exists in no Hermes version); `gatewayRestartNotification` default false → **true**, read via `boolTrueDefault`; busy-ack docs corrected to the global `display.busy_ack_enabled`.
  - Tests: new `HermesV0211GatewayParityTests` — roster gate against the tagged `Platform` enum + `plugins/platforms/` in BOTH directions with an explicit exclusion set, list-parser drift fixtures, restart-notification default/falsy-spelling tests, Discord round-trip.

Decisions recorded: a2a and raft stay OUT (a2a = agent-to-agent infra, `requires_env: []`; raft = one env var, no account); `local`/`relay`/`wecom_callback` internal.

Deliberate NO-OPs: no setup forms for the nine new rows (they use `PlatformsView`'s existing default panel) — spun out as **t-1ca040c2**; the multi-profile `headerDigest` doesn't name multiplexing (single-profile does; the status badge covers the current profile).

Tests: ScarfCore 2318 tests — all pass except `ACPClientStartIdempotenceTests` (4), the known parallel-load flake, which passes 5/5 in isolation. App target `scarfTests`: TEST SUCCEEDED. Debug build clean.

Memory: appended a "Phase 2 — gateway" section to `scarf/decisions/hermes-v0-21-1-compatibility-decisions`; corrected the v0.21 note's "two verdicts" claim to point at the new third branch.

