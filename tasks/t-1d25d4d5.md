---
id: t-1d25d4d5
title: Fix stale Hermes citations in the MCP login/device-prompt files
status: done
added: 2026-09-10
priority: low
---

## Description

Round-2 whole-surface audit LOW, not assigned to any phase (`documents/hermes-v0.21.1-whole-surface-audit-round2.md:102`). Noticed while doing P24 (`t-00d04dcc`), which touches these same files but left the citations alone to stay in scope.

Charter C2: a citation that does not point at the claim is a claim with no evidence.

- `scarf/Packages/ScarfCore/Sources/ScarfCore/Parsing/HermesMCPDevicePrompt.swift:40,50` cite `tools/mcp_oauth_device.py:125-126`; the audit says the real lines are `:126-127`.
- `scarf/scarf/Features/MCPServers/ViewModels/MCPLoginController.swift:189-192` (now shifted by the P24 commit `e4bf9653` — search for `mcp_oauth_device.py`) cites `:123-124` for the device deadline (real: `:124-125`) and `:131-140` for the polling loop (real: `:132-144`).
- `scarf/scarf/Features/MCPServers/ViewModels/GatewayViewModel.swift:306` cites `gateway.py:8958`, which is past EOF (the file is 6202 lines; the line is `:6133`, already cited correctly ~60 lines above).

Verify each against `git -C ~/.hermes/hermes-agent show v2026.9.7:<path>` before editing — the audit's replacement numbers are themselves a claim.

## Plan



## Artifacts

Folded into P26 (`t-caba4971`) and shipped in `ced2866c` on `fix/whole-surface-audit-r2` — this task was a duplicate of that phase's last bullet.

All four citations re-read out of `git -C ~/.hermes/hermes-agent show v2026.9.7:<path>`:

- `tools/mcp_oauth_device.py:126-127` — the three-line device prompt is ONE `print(f"\n  MCP OAuth: open {verification} on any device.\n  Code: {authorization['user_code']}\n" "  Waiting for approval...\n", file=sys.stderr, flush=True)`. Was cited `:125-126`. Fixed at `HermesMCPDevicePrompt.swift:40` (`completionSentinel`) and `:50` (the `parse` doc).
- `tools/mcp_oauth_device.py:124-125` — the `min(expires_in, timeout)` deadline (`deadline = time.monotonic() + min(_positive_seconds(authorization["expires_in"], …), _positive_seconds(cfg.get("timeout", 300), …))`). Was cited `:123-124`. Fixed at `MCPLoginController.swift:254`.
- `tools/mcp_oauth_device.py:132-144` — `_authorize`'s `while True:` poll loop (opens at `:132`, runs through the `interval *= 2; continue` backoff at `:143-144`). Was cited `:131-140`. Fixed at `MCPLoginController.swift:251`.
- `hermes_cli/gateway.py:6133` — `print("✗ Gateway is not running")`. The old cite `:8958` was past EOF; the file is 6202 lines. Fixed at `GatewayViewModel.swift:353` and `scarf/scarfTests/GatewayViewModelTests.swift:68`.

Comment-only — no behaviour change, nothing in the MCP login or gateway-status parsing paths was touched. Covered by the phase's build + test run: ScarfCore 2590/2590 pass, `xcodebuild -scheme scarf -configuration Debug build` SUCCEEDED, `scarfTests/GatewayViewModelTests` still green.

Full P26 artifact list (including the other ~20 citations in the same sweep) is on `t-caba4971`.

