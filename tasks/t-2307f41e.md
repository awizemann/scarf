---
id: t-2307f41e
title: P7: Settings — new v0.20.4 config keys + stale defaults/docs
status: done
added: 2026-08-20
---

## Description

Add Settings fields (written keys via existing setSetting paths; render only when isV0204OrLater where the key is new): wake_word.capture (auto|local|client, Voice tab), stt.local.unload_after_idle_seconds, stt.cloud_trim_silence/cloud_trim_threshold_db/cloud_trim_keep_ms, agent.background_review.enabled kill switch (on by default, costs tokens — near memory settings), auxiliary.title_generation.max_concurrency + auxiliary.compression.max_concurrency, agent.cron_drain_timeout + agent.gateway_turn_lease_timeout (Advanced, near gateway_timeout). Fix stale parse defaults: delegation.max_iterations default 50→250, add max_concurrent_children (default 10) (HermesConfig+YAML.swift:229). Parse gateway multiplex_profile_allowlist and warn in ProfileRoutesSection.swift:92 when a configured profile isn't allowlisted (malformed value fails safe to default-only upstream). Fix stale doc block GatewayPlatformSettings.swift:21-29 (claims gateway.platforms.<p>.* — actual is top-level <platform>.<key>; fix allowlist-kind mapping claims; note LINE allowed_rooms env-only). Tests for parse defaults + allowlist warning. Branch feat/hermes-v0204-parity.

## Plan



## Artifacts



