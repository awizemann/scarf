---
id: t-8523a5d5
title: A11y P3: localization/xcstrings audit for a11y strings
status: done
added: 2026-08-28
---

## Description

Audit Localizable.xcstrings and InfoPlist.xcstrings after P1/P2 land: confirm new accessibilityLabel strings are extracted/localizable per ops/Localization Workflow memory note (Localizable.xcstrings is source of truth), check for stale/orphaned keys, and verify existing iOS accessibilityLabels are localizable (several are interpolated raw strings). Report + fix.

## Plan



## Artifacts

Audit only, no repo changes. Findings: (1) SWIFT_EMIT_LOC_STRINGS/STRING_CATALOG_GENERATE_SYMBOLS/LOCALIZED_STRING_SWIFTUI_SUPPORT all on, but headless xcodebuild NEVER merges new keys back into Localizable.xcstrings (verified byte-identical MD5 after clean build) — only an interactive Xcode build syncs; new a11y literals will land in the catalog on Alan's next Xcode build. (2) 7 locales (en,de,es,fr,ja,pt-BR,zh-Hans), 1383 keys, no stale keys in sample, InfoPlist.xcstrings fine. (3) DEFECT: String-returning helpers (rowLabel/projectRowLabel/skillRowLabel/hubRowLabel) bind the StringProtocol overload — never extracted, never localized; call-site interpolated literals are fine. Handed to P4 for fix. TemplateConfigSheet runtime field.label labels are template-author content — intentionally unlocalized, footnote only.

