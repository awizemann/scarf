---
id: t-1cc0a505
title: Hermes v0.20 deferred settings surfaces (pickup-ready backlog)
status: todo
added: 2026-08-03
---

## Description

Remainder of the v0.20 Phase 3 bucket after the power-settings pass (compression/reasoning-overrides/excluded_providers) shipped separately. Each item is independent; all keys verified present at Hermes v2026.8.3 (audit: documents/hermes-v0.20.0-audit-report.md §D; memory: integration/hermes-v0-20-0-audit-findings). Scarf writers live in scarf/scarf/Features/Settings/ViewModels/SettingsViewModel.swift (per-key setSetting) + readers in scarf/Packages/ScarfCore/Sources/ScarfCore/Parsing/HermesConfig+YAML.swift; gate new UI on capabilities (isV020OrLater unless noted). Items:
1. STT/TTS expansion (only if voice demand): stt.language (global, default en) + stt.openai.language, stt.groq.{model,language}, stt.local.* VAD knobs; tts.speed (global), tts.xai.{language,speed,text_normalization,optimize_streaming_latency,sample_rate,bit_rate}, tts.deepinfra.{model,voice}. Scarf already has an STT tab + tts.xai fields — extend those groups. Hermes: hermes_cli/config_defaults.py.
2. auxiliary.title_generation block ({enabled,provider,model,timeout,language}) + per-task auxiliary.<task>.reasoning_effort — Scarf already writes auxiliary.<task>.<field>, follow that pattern.
3. approvals.smart_policy (free-text policy for the LLM approval reviewer) — sits next to existing approvals.mode/timeout writes; pair with the existing Allowlist Suggestions section in SecurityTab.
4. gateway.profile_routes editor — list of {name,platform,guild_id,chat_id,thread_id,profile} match rules, conjunctive, specificity-ranked (Hermes gateway/profile_routing.py). Needs a real list-editor UI + direct-YAML write (GatewayConfigWriter pattern). Biggest UI lift in this list.
5. secrets.bitwarden.encrypted_cache.{enabled,max_stale_seconds} + secrets.command.* (any-CLI vault helper) — extend the existing Bitwarden settings group.
6. telemetry.shared_metrics.enabled (privacy toggle, default false) and database.{journal_mode,wal_autocheckpoint,journal_size_limit} (advanced, for remote/container servers).
7. BIGGER separate features (own design pass each): `hermes import-agent [claude-code|codex] --dry-run` onboarding flow (hermes_cli/agent_import.py; --dry-run gives a previewable plan); `hermes sync {status,pull,push,now,enable,disable,device,propose}` skills-sync surface in the Skills tab (subcommands/sync.py).
Also parked from the audit: HERMES_ACP_SKIP_CONFIGURED_MCP=1 env for Scarf's metadata-only ACP helper sessions (startup perf); cron blueprints catalog mirror (cron/blueprint_catalog.py); optional 2048-char cap parity in sanitizeFTSQuery; google_chat presence probe for iOS beyond the rawYAML scan.

## Plan



## Artifacts



