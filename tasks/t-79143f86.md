---
id: t-79143f86
title: Audit P38: round-4 NEW findings fixed pre-merge (P30–P37 residue)
status: done
added: 2026-09-10
priority: high
---

## Description

Round-4 whole-surface audit (documents/hermes-v0.21.1-whole-surface-audit-round4.md). The 25 findings introduced by P30–P37 that need no product decision, fixed before the merge per the round-3 convention: cron hint copy Hermes refuses, context menu and iOS gate not unified with the recovery offer, friendlyCronFailure names re-arm for recurring jobs, parseNestedYAML maps purge, unsetSetting nothing-stored guard, false "config set exits 1" doc comments, dashboardListenerPID duplicate drain routine, test-host stability (subscript after count expect, try! #require), P22 sweep allowance dead debt, stale docs/citations (nonInterruptiveCommands, hasCronRecoverableErrorResume, hasGoals, hasACPQueue, AgentTab, pairing prefix tag, skills_hub.py lines).

## Plan



## Artifacts

Full per-item results: `documents/hermes-v0.21.1-audit-p38-findings-results.md`.

All 25 findings addressed on `fix/whole-surface-audit-r3` in six commits:
- `ea65ff90` — cron recovery (items 1-7): the hint Hermes refuses, the fourth offer site, iOS's shadowed gate, the static re-arm wording, three citations.
- `fa4d1414` — Settings/YAML (items 8-11): the last-wins purge's own `values`/`maps` entry plus the flat-dotted-sibling exemption, the clear-row no-op, the two carried group keys, and both false "config set always exits 1" docs.
- `87f0ca40` — `dashboardListenerPID` adopts `Process.waitDraining` and goes `nonisolated` (items 12, 23); pairing prefix tag and skills_hub.py line numbers (13, 14).
- `43b594bd` — capabilities/chat docs and the union that could not fail (items 15-20).
- `e9055e55` — test-host stability sweeps and the dead `ProvenConfig.exists` (items 21, 22, 24, 25).
- `6db90fe2` — two gaps the fresh-eyes pass found in P38's own diff.

NOT shipped (pending product decisions, per the findings file):
- a Duplicate button on the cron dead-end hint (item 1 is copy only);
- an output verdict for `config set` (item 11 is doc only) — tracked by `t-ba727c07`.

New surface: `HermesCapabilities.hasCronPastOneShotResumeRefusal` (`isV0181OrLater`), floor-walked across all 32 `v2026.*` tags; `CronRecoveryOffer.refusesResume`; `HermesCronJob.isPastDeadlineOneShot(now:)`; `CronRecoveryOffer.noFutureOccurrencesHint(repeatTimes:)`. Three new catalogue keys in six locales; one removed.

Tasks: `t-cd9fd829` closed (its debt is gone and the allowance removed); `t-f43f0af5` filed for the ~100 pre-existing subscript-after-count-expect sites outside the phase suites.

Memory: "## Whole-surface remediation — P38" appended to `decisions/hermes-v0-21-1-compatibility-decisions`; `architecture/hermes-pairing-approve-revoke-refuse-at-exit-0-judge-by-the` corrected (wrong tag + the split-f-string grep false negative).

Tests: ScarfCore 2685/2685 green. Mac `scarfTests` full parallel run red only in the five known load-sensitive suites; all 47 of those pass in isolation and none touches a P38 surface.

