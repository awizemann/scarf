---
id: t-e2cd2861
title: Projects W1: kill fileExists-inference in the four destroy-shaped writers (ModelPresetService, AGENTS.md block, set_config, Citadel fallback)
status: done
added: 2026-09-04
priority: urgent
---

## Description

P8 audit (reports/2026-09-04-projects-post-remediation-audit.md) DI C1/C2/H3/H1: ModelPresetService.persist still destroys on a transport blip (convert to GuardedJSONStore); ProjectContextBlock.writeBlock replaces the user's whole AGENTS.md on a fileExists lie or non-UTF-8 byte (proof-based probe + first .bak for AGENTS.md); MCP project_set_config rebuilds config.json from a failed read and drops unknown keys (guarded write); Citadel rename fallback deletes the destination on ANY rename error — gate on destination-exists and retry first.

## Plan

Done (P8 W1). All four writers converted; `fileExists`-as-inference removed from every write site in scope.

1. DI-C1 ModelPresetService → GuardedJSONStore; load+persist collapsed to one `mutate` (single inspection feeds decode and write); undecodable REFUSES (presets exist nowhere else, like projects.json); new `unreadableStore` error; `ModelPresetStoreReader.probe` is proof-based too.
2. DI-C2 ProjectContextBlock.writeBlock → proof-based probe, refuses unreadable AND non-UTF-8 (`?? ""` was the text spelling of `try? decode ?? []`), first-ever AGENTS.md.bak; empty AGENTS.md stays writable. Mac's ProjectAgentContextService.refresh now delegates to it (the same bug lived in two files).
3. DI-H3/SEC-M6(write) project_set_config → guarded inspect + JSONValue-graph mutation (unknown top-level keys survive), refusal on unreadable/zero-byte, .bak, and the inspection runs BEFORE the Keychain write so a refusal can't orphan a secret.
4. DI-H1 Citadel fallback → `SFTPRenamePublisher.publish`: retry the plain rename once, then probe destination-exists; only a provably-present destination is displaced, otherwise the destination is untouched and the staged path is named in the error.

Knock-on: AGENTS.md.bak is Scarf's artifact, so ProjectTemplateUninstaller gained `scarfOwnedProjectRootFiles(in:)` (root-level sibling of `scarfOwnedFiles(in:)`) — otherwise it counted as user content and blocked project-dir removal.

## Artifacts

Changed: ScarfCore/Services/ModelPresetService.swift, ScarfCore/Services/ProjectContextBlock.swift, ScarfProjectsMCPKit/ProjectMCPTools.swift, ScarfIOS/CitadelServerTransport.swift, scarf/Core/Services/ProjectAgentContextService.swift, scarf/Core/Services/ProjectTemplateUninstaller.swift.
Tests: new ScarfCoreTests/ProjectsW1GuardedWriterTests.swift (10), +4 in ScarfProjectsMCPKitTests/ProjectMCPToolTests.swift, new ScarfIOSTests/SFTPRenamePublisherTests.swift (6).
Verified: ScarfCore swift test 2075 + 42 pass; ScarfIOS 48 pass; scarf scheme 689 tests (one unrelated ChatViewModelStartLifecycle flake under parallel load, green in isolation); "scarf mobile" iOS build succeeded. Private DerivedData only; nothing committed.

