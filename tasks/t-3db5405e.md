---
id: t-3db5405e
title: UI gate P2a journey: Projects (new project + template install from local file)
status: done
added: 2026-09-08
---

## Description

XCUITest journeys on ScarfUITestCase + fixture: create a project via the New Project sheet and see it in the sidebar; install a template from a local .scarftemplate (templates.installFromFile or a launch-arg file handoff) so the existing TemplateInstallUITests no longer needs the network; uninstall round-trip. Add only the identifiers each step needs. Depends on P1a + P1b.

## Plan



## Artifacts

Commit `b20108fb` on `worktree-agent-abfb913cce6f13212` (based on ui-gate @ 4dd744dc), not pushed.

**New — `scarf/scarfUITests/ProjectsJourneyUITests.swift`**
`testCreateProjectAppearsInSidebarAndRegistryThenRemoves`: sidebar.section.Projects →
sidebar.projects.newProject → newProject.name / newProject.parent →
newProject.createButton → asserts BOTH `sidebar.projects.row.<name>` AND the row in
`<isolated-home>/scarf/projects.json` (plus the scaffolded dir and the derived slug
`ui-gate-journey`) → projects.contextMenu.removeFromList →
projects.removeFromList.confirm → asserts row gone, registry row gone, and the
directory still on disk (that is what "keep files" means). Hermes-free by design, so
it runs on a Mac with no Hermes install.

**De-networked — `TemplateInstallUITests.swift`**
Installs the repo's own `templates/awizemann/hackernews-digest/hackernews-digest.scarftemplate`
(copied per test into the runner tmp) via the `file://` branch of `TemplateURLRouter`
instead of raw.githubusercontent. Also fixed its sidebar-row predicates: they were
still on the pre-well `projects.row.` prefix and matched nothing, so the journey had
been failing at the "row appears" step on ui-gate independently of this work.

**App changes (identifiers + one test-mode-gated arg shape)**
- `SidebarProjectsWell.swift`: `projects.contextMenu.removeFromList`,
  `projects.removeFromList.confirm`, `projects.removeFromList.cancel`.
- `scarfApp.swift`: `--scarf-test-install-url` passes a `file://` argument through as
  is, still wrapping anything else as `scarf://install?url=…` so the router's https
  guard keeps applying (`ScarfApp.testInstallURL(from:)`).

**Harness — `UITestIsolation.swift`**
`setText(_:in:of:)` types then reads the value back and retries; non-strict
`XCTExpectFailure` on non-final attempts so a "Failed to synthesize event" doesn't end
the test before the retry. `attachScreenshot` promoted from private-in-sweep to the
base class.

**Runs** (`-testPlan Full -only-testing:…`, `TEST_RUNNER_SCARF_UITEST_FIXTURE` for the
fixture runs)
- Projects journey, no fixture: 3 passes (47.6s / 87.3s / 119.1s / 139.6s).
- Projects journey, with fixture: 1 pass (91.6s) after the remove-step retry landed.
- Template journey, no fixture: 1 pass (59.3s).
- Template journey, with fixture: 2 passes (78.8s / 110.3s).
- `scarfTests` (unit): passes.

**Honest caveats**
- Another agent ran `xcodebuild test` concurrently on this Mac throughout. That
  produced several non-deterministic failures — "Failed to activate application
  (current state: Running Background)", "Application com.scarf.app is not running"
  (build-detached.sh quits every running copy), and a stray keystroke. Every one
  reproduced green once the machine was quieter. UI-test runs need to be serialized
  across agents; recorded in the runner-gotchas note.
- `SectionSweepUITests` failed on a different section each run
  (Projects, then Platforms). NOT from this work: ui-gate commit `92d062bd`
  ("honour NSArgumentDomain strings for collapse state") fixes exactly that and
  landed after this branch's base. This branch is behind ui-gate (P3 + WAL + that
  sidebar fix) and wants a merge before the gate is judged.
- `scripts/ui-fixture/make-ui-fixture.sh` aborts partway on this Mac: `hermes -z`
  returns "No LLM provider configured" while seeding session 2. The fixture runs used
  the partial-but-marked home it left behind (real state.db, memories, skills, one
  session, empty projects.json). The builder's provider config is P1a's problem, not
  this task's.

