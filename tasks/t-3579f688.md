---
id: t-3579f688
title: A11y P4: fresh-eye accessibility audit + tests
status: done
added: 2026-08-28
---

## Description

After P1–P3: adversarial fresh-eye audit of the whole a11y change set against the Walkabout findings (W19/W22 + zoom button), verify build (./scripts/build-detached.sh or xcodebuild) and full test suite green, and check no accessibilityIdentifier regressions for the XCUITest journey. Record durable a11y conventions as a memory note.

## Plan



## Artifacts

Localization fix: String(localized:) wrapped fragments in projectRowLabel ("in %@", "archived"), skillRowLabel ("pinned"/"disabled"), hubRowLabel ("from %@"); rowLabel(for:) needed none (no hardcoded English). Verified via .stringsdata: all 30 a11y strings extract. Audit: fixed one real regression (hubRow .combine + explicit label had made hub.description unreachable — appended to helper); identifiers intact (35 before/after); AddServerSheet 8/7 fields covered; Voice Control labels match visible titles; no double-announcement; no logic changes. Flagged (needs GUI runtime check): ProjectsSidebar row .combine with .tag(project) selection. Build succeeded. Tests: 400/58 suites, 1 failure — SettingsWriteReadParityTests.unresolvedInterpolatedShapesFailLoudly, PRE-EXISTING (pinned to b9b07ab 2026-08-20 via git log -L; scanned file untouched by this change set).

