---
id: t-1c11e831
title: Audit F5: Interact/Manage/Config correctness
status: done
added: 2026-09-02
priority: high
---

## Description

Memory: conflict-aware editor (never resync a dirty draft from the watcher; warn + reload/overwrite like SOUL.md). Interact smalls: ANSI regex fix (SkillsViewModel:622), clearPendingPermissions→cancelPermission, curator transient-clear generation token, InstallFromURLSheet name/category validation, skills symlink resolution. Manage: one-shot cron edit (never send display string as schedule), CronScheduleFormatter dead runat branch, Gateway exit codes via saveFailureMessage + stale green running badge + cancel asyncAfter reloads, ToolsViewModel toggle failure message, HermesCapabilitiesPanel through v0.21, hasFailureMarker false positive. Config1: slack.require_mention→extra (writer+reader+test), email skip_attachments→extra, QuickCommands toast typed name, credential add trim+clear, GatewayBehaviorViewModel isSaving. Projects fleet cluster: preset UUID skip-with-explanation, partial failure not green, stderr surfaced, no re-derive rename, preview/executor cron-id match, scheduleArg structured, executor cancellation + sheet placement. Mini-app settle handshake + promptInFlight. DashboardWidget/Section identity. KANBAN (verified audit): promote uses --ids for bulk (single positional + reason nargs* — kanban.py:744-749; today bulk corrupts the audit reason), assignees adds --json (text parse reads v0.12 format → all-zero counts + phantom NAME row), List mode carries tenant/session scope through KanbanService (KanbanView.swift:51, KanbanViewModel.load hardcoded argv), plan() handles .scheduled as source (unblock accepts it per kanban.py:685-694), dispatch decode failures surfaced not zero-stubbed, skill chips keyed by value.

## Plan



## Artifacts



