---
id: t-602f6b7b
title: Audit P35: remaining floor, selection and residue items from round 3
status: done
added: 2026-09-10
---

## Description

Round-3 whole-surface audit (`documents/hermes-v0.21.1-whole-surface-audit-round3.md`). Needs product decisions (max/ultra re-floor under C1; post-load selection reconciliation; iOS sentinel unset-vs-refuse) from the report.

- MED · PRE · `max`/`ultra` reasoning-effort floors too high: `VALID_REASONING_EFFORTS` gains `max` at v2026.7.7 (0.18.1) and `ultra` at v2026.7.20 (0.19.0); Scarf gates both on `isV020OrLater` (`PowerSettingsWriter.swift:10-12,23-25`; consumers `AgentTab.swift:40,258,277`). `AuxiliaryReasoningEffort` (`HermesConfig.swift:54`) is correct. Re-flooring adds a picker row on 0.18.1–0.19.1 hosts (C1 in the permissive direction) — decision.
- MED · NEW(P23/P28) · `selected`/`selectedPlatform` never reconciled after the roster narrows post-load: a sub-floor form stays open and writable (`PlatformsView.swift:130,174-201`; `ToolsView.swift:19-23,90-101`, `ToolsViewModel.swift:43-46,55`, where `toggleTool` shells `hermes tools enable … --platform <name>`).
- LOW · NEW(P24) · `loadMCPServers` probes two token spellings per server with `fileExists` — up to 2N SSH round trips; one directory listing answers all (`HermesFileService.swift:321-323`).
- LOW · NEW(P24) · dead residue: `HermesMCPServer.sseReadTimeout` still parsed/threaded (`HermesMCPServer.swift:67-80`, `HermesFileService.swift:1150`) and four orphaned `Localizable.xcstrings` entries (`SSE read timeout` + footers).
- LOW · PRE · `check-hermes-tables.py:194-210` `ALIASES` non-Dict/non-comprehension arm silently falls through to `alias_groups`; no test. Also `scripts/tests` sit behind `skipUnless(_target_tag_available())` so a machine without the checkout prints OK (`:198-199,107-110`).
- LOW · `HermesCronIncidentsParser.swift:55-57` cites v0.21.0 `subcommands/cron.py:287-305`; at v2026.9.7 it is `:164-172`.
- LOW · `EmailSetupViewModel.swift:70-72` and `WhatsAppCloudSetupViewModel.swift:73` cite v2026.8.31 lines; re-anchor at v2026.9.7.
- Decision (forms vs Settings): platform-setup forms prime and WRITE the resolved default on every Save (`GatewayBehaviorViewModel.swift:39-45`), Settings treats absence as a sentinel and writes nothing. Pick one rule or document why forms differ.

## Plan



## Artifacts

Branch `fix/whole-surface-audit-r3`. Eight commits: `b08b2eea`, `1a41f79f`, `037a22e2`, `7e541c29`, `a4bfad1c`, `533de6e0`, `edb69e79`, `8e77f5b0`.

**Shipped**
- **Decision 5** (`b08b2eea`): `max` re-floored to 0.18.1, `ultra` to 0.19.0. Walked `VALID_REASONING_EFFORTS` across every `v2026.*` tag — five-level tuple at v2026.7.1 (0.18.0, `hermes_constants.py:794`), `+max` at v2026.7.7 (0.18.1), still `max` alone at v2026.7.7.2 (0.18.2), `+ultra` at v2026.7.20 (0.19.0, `:835-837`). New `hasReasoningEffortMax`/`hasReasoningEffortUltra` replace the single `isV020OrLater` gate.
- **Decision 8** (`1a41f79f`, `edb69e79`): `KnownPlatforms.reconcile(selection:against:)` snaps `selected`/`selectedPlatform` to `cli` when the post-load roster drops it; driven from each view's `.onChange(of: visiblePlatforms.map(\.name))` plus an appear-time call for cached-VM re-entry. Tools reloads through `switchPlatform`.
- **Decision 10** (`037a22e2`, `533de6e0`): "Host default" → `hermes config unset approvals.mode` behind `hasConfigUnset`, on Mac and iOS. argv verified at v2026.7.20 and v2026.9.7. Judged by OUTPUT — the managed-install refusal prints and returns (exit 0) — and the self-audit moved that verdict onto `unsetSetting` itself, fixing six other call sites with the same hole.
- **LOW** (`7e541c29`): one `mcp-tokens/` listing instead of up to 2N `fileExists` (5 servers: 10 probes → 1 listing); `HermesMCPServer.sseReadTimeout` + four orphaned `Localizable.xcstrings` entries deleted.
- **LOW** (`a4bfad1c`): `check-hermes-tables.py` `ALIASES` fall-through fails closed; `scripts/tests` fails (not skip-OK) without the target checkout unless `SCARF_ALLOW_SKIP=1`; three citations re-anchored at v2026.9.7.
- **Test gate** (`8e77f5b0`): `AllConfigWritersParityTests` manifest re-balanced for the extracted unset argv.

**Deliberately NOT done**
- Decision 9 (forms vs Settings): already shipped by P33 (`PlatformSetupHelpers.swift:273-277` + `decisions/setup-forms-write-the-resolved-default-settings-treats`). Verified, not assumed.
- `PowerSettingsWriter`'s two direct-YAML writers keep `isV020OrLater` — the dict and list ARE v0.20 surfaces; only the vocabulary re-floored.
- iOS `valueToWrite` untouched: the clear is a separate `clearAction` ahead of it, so P29's no-empty-scalar pin still holds.

**Tests**: ScarfCore 2654 (known ACP idempotence load flake, green in isolation, t-f3820038); Mac `scarfTests` **1052/1052 serial**; iOS `SettingsEditorClearP35Tests` 5/5; `scripts/tests` 18/18 (and 3 real failures with no checkout, 3 skips with the opt-in); `check-hermes-tables.py` `OK … lanes=5/5`.

**Pre-existing RED, proven on a worktree at `99412912`**: `scarfUITests/SectionSweepUITests.testEverySectionRenders` (Activity error banner) — filed `t-a9ef75f0`.

**Memory**: P35 section appended to `decisions/hermes-v0-21-1-compatibility-decisions`; new note `architecture/a-host-default-picker-row-clears-its-key-with-hermes-config`; stale v0.20 effort-vocabulary claim corrected in `decisions/hermes-v0-20-compatibility-decisions`.

**Tasks filed**: `t-32794e9f` (sibling exit-0 config verbs), `t-a9ef75f0` (Activity UI-sweep banner).

