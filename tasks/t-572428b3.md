---
id: t-572428b3
title: Projects A2: WidgetErrorCard grouping + remaining decorative-icon/announce lows
status: done
added: 2026-09-04
priority: low
---

## Description

P8 audit AX M1 + 4 lows — all fixed and verified.

1. WidgetErrorCard: added `.accessibilityElement(children: .combine)` on the card body and `.accessibilityHidden(true)` on the warning triangle — every refusal state (image policy, size caps, path errors) now reads as one VO stop (title+reason+hint).
2. ImageWidgetView: hid the decorative header "photo" icon; added `.accessibilityLabel` (widget title, falling back to "Image") on both the local (`Image(nsImage:)`) and remote (`AsyncImage` success) loaded images; wrapped the outer VStack (header+content) in `.accessibilityElement(children: .combine)` so a loaded image no longer reads as a blank "image" stop.
3. KanbanSummaryWidgetView: hid the header `rectangle.split.3x1` icon, consistent with the file's existing hidden status dots.
4. MiniAppLaunchView consent sheet: hid the sensitive-warning triangle / plain checkmark in `permissionRow` and the unknown-permission question-mark icon; grouped the unknown-permission row with `.accessibilityElement(children: .combine)` (grantable rows are already single AX stops via `Toggle`); added `.accessibilityHint("Grants only the checked permissions")` on "Approve & Run".
5. iOS ProjectsListView: added `lastAnnouncedDamage` state and post `AccessibilityNotification.Announcement(AttributedString(damage))` only when `damage` transitions to a new non-nil value, matching the Mac `RegistryDamageBanner` pattern without re-announcing on every pull-to-refresh.

Build verified: macOS `scarf` scheme and iOS `scarf mobile` scheme both build clean (`BUILD SUCCEEDED` / fresh .app products) against private DerivedData `dd-a2`. No behavior changes — accessibility-only.

Memory: extended `conventions/macos-accessibility-label-conventions` (edit_memory append) with 6 new convention bullets from this batch; the `source_paths` frontmatter list could not be extended via `edit_memory` (find_replace only targets the body, not frontmatter) — left as the original 4 anchor files.

## Plan



## Artifacts



