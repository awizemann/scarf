---
id: t-855abc31
title: Analytics follow-up: consent groups, bundled-skill bootstrap event, template install source attribution
status: done
added: 2026-08-20
---

## Description

Fix audit follow-ups #1–#3 from the swift-stats integration: (1) consent-group alignment — diagnostics events (perf_measure, bootstrap_task_failed) vs .usage gating: set explicit consent at startup or amend taxonomy; (2) rename the SkillBootstrapService launch-time skill_installed{bundled} bulk emission to its own event (e.g. skills_bootstrapped {count_bucket}) so user installs aren't drowned; (3) thread the template/skill install source through the pending-install state in TemplateInstallerViewModel instead of shared VM field so overlapping installs can't misattribute. Build + tests green; update taxonomy doc if semantics change.

## Plan



## Artifacts



