---
id: t-15c5d10f
title: Local models T0 — source-verify Hermes resolution of local providers
status: done
added: 2026-07-13
---

## Description

Verify against Hermes source (not `config set` acceptance) exactly what config.yaml keys / env vars each local provider needs at request time: bare `ollama` (aliased to custom — where does the base URL come from? OLLAMA_HOST? default localhost:11434?), `lmstudio` (LM_BASE_URL default 127.0.0.1:1234/v1), `local` (vllm/llamacpp), and `custom` with explicit base_url (+ api_mode?). Output: memory note with the exact key set the picker UI must write per provider, plus credential expectations (dummy api key needed?). Blocks T1/T3.

## Plan



## Artifacts



