---
id: t-840a1f4d
title: Audit P22: Main-actor and spawn discipline (C10)
status: todo
added: 2026-09-09
priority: high
---

## Description

Round-2 whole-surface audit phase P22. Source: `documents/hermes-v0.21.1-whole-surface-audit-round2.md`; brief: `documents/hermes-v0.21.1-parity-agent-brief.md`. Opus agent, tests that fail without the fix, fresh-eyes review, memory note.

Class: charter C10 — the sites P11/P17 did not sweep, plus spawn-lifecycle leaks.

- HIGH · `TelegramSetupViewModel.load()`/`save()` run on the MainActor and do the whole `.env` + config.yaml read and N sequential `hermes config set` spawns inline; on an `.ssh` context each is an SCP/exec round-trip. `GatewayBehaviorViewModel.swift:169` wraps the identical `saveForm` in `Task.detached` — the platform-setup VMs were never swept · `scarf/scarf/Features/Platforms/ViewModels/PlatformSetup/TelegramSetupViewModel.swift:55,107`, `PlatformSetupHelpers.swift:88-94`
- MED · `launchDashboard()` calls `Process.run()` and `stopDashboard()` calls `dashboardListenerPID()` → `lsof` + `waitUntilExit()` with NO timeout, both on the main actor (`SWIFT_DEFAULT_ACTOR_ISOLATION = MainActor` for the Mac target) · `scarf/scarf/Features/Health/ViewModels/HealthViewModel.swift:1049-1059,1117,1148-1176`; `scarf/scarf.xcodeproj/project.pbxproj:606`
- MED · The 30 s kanban diagnostics throttle only advances on SUCCESS (`lastDiagnosticsFetchAt` set inside the `if let diags = try? …` body), so a failing host respawns `kanban diagnostics --json` on every 5 s board tick · `scarf/scarf/Features/Kanban/ViewModels/KanbanBoardViewModel.swift:201-206,487-490`
- MED · `HermesCLIRunner` injection covers only 2 of the gateway's 3 load probes and none of its mutations — `HermesGatewayListService.fetch(context:)` spawns through the transport directly, and `startGateway`/`stopGateway`/`restartGateway`/`approvePairing`/`revokeUser` call `ctx.runHermes(...)` with the silent 60 s default the typealias doc says every site should name; the P11/P17 tests cannot observe the third spawn · `scarf/scarf/Features/Gateway/ViewModels/GatewayViewModel.swift:187,405,442,465`; `HermesGatewayListService.swift:171-186`; `scarf/scarf/Core/Models/HermesCLIRunner.swift:13-14`
- LOW · `isLoading` never cleared when a load is superseded (the generation guard returns first), so the spinner runs until the post-mutation reload lands; also poisons the coalescing test at `:157` · `GatewayViewModel.swift:161,190-198`
- LOW · `attachCapabilitiesIfNeeded` replaces the whole VM without cancelling the outgoing instance's `loadTask` — the first appear leaks a detached load running three CLI probes · `scarf/scarf/Features/Gateway/Views/GatewayView.swift:66-74`
- LOW · `MCPLoginController.start` calls `proc.run()` on the MainActor (possibly a remote `ssh` spawn) · `scarf/scarf/Features/MCPServers/ViewModels/MCPLoginController.swift:150`
- LOW · `selectSkill`/`loadSelectedFile`/`computeMissingConfig` are nonisolated (ScarfCore builds Swift 5) yet mutate `@Observable` UI state; `computeMissingConfig` also does a synchronous `context.readText(configYAML)` inline · `scarf/Packages/ScarfCore/Sources/ScarfCore/ViewModels/SkillsViewModel.swift:300-334,1050-1088`

## Plan



## Artifacts



