---
id: t-069033ec
title: Audit P34: reconcile the chat slash-command roster with the ACP adapter
status: done
added: 2026-09-10
---

## Description

Round-3 whole-surface audit (`documents/hermes-v0.21.1-whole-surface-audit-round3.md`, capabilities section). Needs product decision (scope: whole-list reconciliation vs delete only `/cost`) from the report.

- HIGH · PRE · Seven slash-menu rows are names the ACP adapter has never dispatched. ACP surface at v2026.9.7 is exactly `help, model, tools, context, reset, compress, steer, queue, version` (`acp_adapter/commands.py:44-66`, handlers `:116-291`, `_available_commands:71-74`); unknown commands fall through to the LLM (`:88-95`). `alwaysAvailableCommands` offers `clear`, `cost`, `reload-skills`, `exit` plus gated `yolo`, `sessions`, `codex-runtime` (`RichChatViewModel.swift:730-819`). `/cost` has never existed at any of 32 tags (real command is `usage`, `hermes_cli/commands.py:277`); `clear`/`exit` are `cli_only` (`:58-59,302-303`); `reload-skills` (`:259-260`) is not wired in ACP. `reset`, `context`, `version` are missing from the fallback. `hasYOLOSlashCommand`'s doc says "Available in ACP" — `yolo` appears nowhere under `acp_adapter/` at any tag (C2) · `HermesCapabilities.swift:330-347`.

P29 already restored the `/compact`↔`/compress` gate at 0.19.1 (`hasACPCompressSpelling`); this task is the rest of the roster.

## Plan



## Artifacts

Commit `99412912` — fix(p34): make the chat slash menu offer only what the ACP adapter dispatches.

SHIPPED
- `RichChatViewModel.alwaysAvailableCommands` roster is now `/new` (client-side) + `help model tools context reset compact|compress version`; `/steer` and `/queue` still come from `nonInterruptiveCommands`. Dropped `clear`, `cost`, `reload-skills`, `exit`, `yolo`, `sessions`, `codex-runtime`.
- `sessionRequiredCommandNames` reconciled; three stale doc-comment lists and `RichChatInputBar`'s example list updated.
- `reset`/`context`/`version` take NO capability flag: they are in `_SLASH_COMMANDS` from v2026.3.17 (0.3.0), below Scarf's v0.6.0 floor, so C1 does not apply.
- `hasYOLOSlashCommand` / `hasSessionsSlashCommand` / `hasCodexRuntimeSlashCommand` doc comments corrected to "CLI/gateway only, NOT ACP" with citations + the file's "No consumer" note. `hasACPCompressSpelling` untouched.
- 5 new tests in `M9SlashCommandTests` (all watched fail first): roster equality @ v0.21.1; never-dispatched sweep across 4 hosts incl. `.empty`; `reset`/`context`/`version` on every host; every fallback name dispatched-or-client-side; advertisement-supersedes-fallback ordering. `v014ConfigCommandsRespectCapabilityGate` asserted the bug → replaced by `v014ConfigCommandsAreNotInTheACPMenu`. Two `SlashMenuLogicTests` re-pointed off `/clear` and `/yolo`.

CITATIONS (32-tag walk of `acp_adapter/`): `_SLASH_COMMANDS` `acp_adapter/server.py:453-463` @ v2026.7.20 → `SlashCommandsMixin._COMMANDS` `acp_adapter/commands.py:44-66` @ v2026.9.7, `_available_commands():69-74`; unknown → LLM at `commands.py:88-95`. `help model tools context reset version` at every tag with an adapter (from v2026.3.17); `steer`/`queue` @ v2026.5.7; `compact`→`compress` @ v2026.7.30. Dropped names: `hermes_cli/commands.py` `usage:277`, `clear:58`, `quit/exit:302-303`, `reload-skills:259-260`, `sessions:148`, `codex-runtime:156-158`, `yolo:181` @ v2026.9.7.

CLIENT-SIDE CHECK: only `/new` is client-side (`clientSideSlashCommand(for:)` has one case; callers `ChatViewModel.swift:1213`, iOS `ChatView.swift:1622`). No `/clear` transcript clear, no `/sessions` sheet. Test asserts both directions.

ADVERTISEMENT: Scarf already consumes `available_commands_update` into `acpCommands` and dedupes, so the fallback only applies pre-advertisement; covered by the ordering test.

NOT SHIPPED (deliberate)
- The three v0.14 flags were KEPT, not deleted, despite having no consumer — `HermesCapabilities.swift` states that convention verbatim three times (`hasSubgoal`, `hasGrokOAuthProvider`, `hasNovitaProvider`); deleting one of three identically-situated flags would be the inconsistent move. Their `HermesCapabilitiesTests` coverage stands.
- `ScarfDesign` `SlashMenu` mockup (static design gallery, still lists `/clear`, `/cost`) → task `t-15013d78`.

TESTS: ScarfCore full suite 2646 tests — pass except the four known load-sensitive `ACPClientStartIdempotenceTests` (`t-f3820038`), which pass in isolation. `xcodebuild` Debug build: BUILD SUCCEEDED.

MEMORY: new note `scarf/architecture/the-acp-adapter-s-slash-roster-is-nine-names-and-has-been`; P34 section appended to `scarf/decisions/hermes-v0-21-1-compatibility-decisions`.

