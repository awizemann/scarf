---
id: t-2f6577cf
title: Audit F3: CLI-contract rewrites (Plugins/Webhooks/Profiles/MCP)
status: done
added: 2026-09-02
priority: urgent
---

## Description

Happy-path liars vs the real CLI (verify every shape against v2026.8.31 source): Plugins → `plugins list --json` for state (retire .disabled marker), explicit --enable/--no-enable on install, tool-override consent wired with in-app confirmation, surface after-install stdout; Profiles → .tar.gz everywhere (save panel, content types, remote temp path, import validator), `profile delete -y` behind Scarf's own confirm; Webhooks → rewrite parseWebhookList against the indented ◆ format; MCP add → stop blind y-piping (drive non-interactively per auth mode, parse stdout for outcome, don't trust exit 0), stdio servers must land enabled, editor gets `context:` so remote hosts edit remote config, finishEdit uses load(force:true), catalog default_enabled carried to tools.include, pendingDefaultExcludedTools cleared.

## Plan



## Artifacts



