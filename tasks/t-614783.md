---
id: t-614783
title: Hermes 0.16.0 - Final Release Pass
status: done
added: 2026-06-14
priority: urgent
---

## Description

Everything below is captured in .memory/integration/hermes-v0-16-wire-verification-results-ws-audit.md (now on main). Done this session: parity (messages.active, flags, session_info_update, rename gate, Spotify, Bedrock), the 3 quick-wins (sessions-optimize, kanban goal-mode badge, rewind_count), WS verified-TODO cleanup, and the v0.16 memory. Remaining:

1. Mismatch fixes (real, pre-existing bugs the WS pass surfaced):

Fix	Note
🔴 Gateway allowlists path (gateway.platforms.<p>.* vs top-level slack:/telegram:/…)	re-verify override nuance first
🔴 hermes gateway list --json doesn't exist (text only)	re-verify Scarf's fallback first
ACP /goal + /subgoal are gateway-only	drop them from the ACP chat surface (they no-op there)
kanban verify <id> verb doesn't exist	remove dead verify()/rejectHallucinated()
curator list-archived --json flag doesn't exist	drop the wasted attempt
openrouter.response_cache is a scalar bool, not .enabled	re-verify truthy-dict nuance, then fix
addMCPServerSSE uses non-existent hermes mcp add flags	dead function — remove / pivot to YAML
minor: tts.xai.model unused; compression-count not on prompt usage; kanban max_retries default	low priority
2. Deferred additive (low value): kanban run-terminate — REST-only (POST /runs/{id}/terminate), no CLI verb; Scarf's kanban is CLI/SQLite, so not worth a REST client.

3. Worth a quick confirm (from the original parity plan, not yet checked): does Scarf's TTS-backend picker include v0.16's gemini TTS + mistral (Voxtral)? And mirror the hourly model-catalog TTL if Scarf caches it. Both minor.

Closed 2026-07-14: superseded — Scarf now targets the v0.18 line (memory: decisions/hermes-v0-18-compatibility-decisions). The v0.16 mismatch fixes listed above (gateway allowlist path, gateway list text-parse, /goal de-advertise, dead kanban verify, OpenRouter scalar, MCP SSE add) all shipped in v2.11.0 (PRs #114/#115/#116) and were re-verified by the v0.17/v0.18 audit cycles.

## Plan



## Artifacts



