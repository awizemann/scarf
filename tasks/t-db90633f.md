---
id: t-db90633f
title: gh#112: diagnose+surface why the Hermes CLI fallback chain fails (Docker host, no host-side wrapper)
status: done
added: 2026-07-17
---

## Description

Reporter on v2.16.1 b51 (which contains 5dc1c55) still sees old "config.yaml not found" — meaning readViaConfigPath AND probeModelConfig both returned nil: no `hermes` reachable from a non-interactive SSH shell (Docker alias/interactive-only wrapper). Add a `command -v hermes` diagnostic to the failure path in IOSSettingsViewModel.load() and teach the wrapper-script fix (docker compose exec -T ...) + Advanced binary-hint in the message. Unit-test the message selection.

## Plan



## Artifacts



