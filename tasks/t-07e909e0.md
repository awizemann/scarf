---
id: t-07e909e0
title: Projects L2: registry lock ownership + coverage for all read-modify-writes
status: done
added: 2026-09-04
priority: high
---

## Description

P8 audit DI H4/H5/M4/M5, SEC M5: release() must verify ownership (token in the lock file) so a broken-then-reacquired lock isn't deleted out from under the successor; staleness/timeout must account for remote 60s scp windows; extend lock + expecting: to the doctor/installer/uninstaller RMWs (Repair All currently erases a concurrent project_register from a stale snapshot); decide + document the grants/session-map cross-writer story (Mac+iPhone on one remote lose attributions); document the lock file as agent-controllable (DoS/removal), not an integrity guarantee.

## Plan

1. DI-H4: uuid ownership token written into the lock at acquire; `release(token:)` verifies before removing (O_EXCL create semantics kept).
2. DI-M5: `staleAfter`/`acquireTimeout` became per-context instance properties — local 30s/2s, remote 300s/60s. Chose a bigger staleness bound over a holder heartbeat, documented why.
3. DI-H5/L1: lock + `expecting:` extended to ProjectDoctorService.repair (per repair, load inside the lock), ProjectTemplateInstaller.registerProject, ProjectTemplateUninstaller.removeRegistryRow, RemoteRestoreService.reanchorProjectsRegistry.
4. DI-M4/SEC-M5: MiniAppGrantStore + SessionAttributionService take the lock on their own lock paths; cross-device last-write-wins and the lock file's agent-writability documented in RegistryWriteLock's header.

## Artifacts

Changed: RegistryWriteLock.swift, ProjectDoctorService.swift, ProjectTemplateInstaller.swift, ProjectTemplateUninstaller.swift, MiniAppGrantStore.swift, SessionAttributionService.swift, RemoteRestoreService.swift, AdjacentRegistryGuardTests.swift, scarfTests/ProjectTemplateTests.swift.

Tests: +6 ScarfCore (ownership release/non-release, remote hold not broken as stale + crashed-holder collection, per-context bounds, sidecar lock paths, doctor repair under 8-way concurrent writers) and +2 app-target (installer/uninstaller refuse while another process holds the lock). ScarfCore filtered run 126/126; full ScarfCore 2103 tests with 24 pre-existing ACP/process-spawn env failures; scarf scheme 705 tests, 1 failure in ProjectsPFTickDecouplingTests (P2's watcher work, not this task); iOS "scarf mobile" build succeeded.

Memory: decisions/registry-writes-take-a-reentrant-cross-process-file-lock amended with the L2 section.

