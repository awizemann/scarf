<?xml version="1.0" encoding="utf-8"?>
<rss version="2.0" xmlns:sparkle="http://www.andymatuschak.org/xml-namespaces/sparkle">
  <channel>
    <title>Scarf Updates</title>
    <link>https://awizemann.github.io/scarf/appcast.xml</link>
    <description>Scarf macOS app updates</description>
    <language>en</language>
    <item>
      <title>Version 2.16.0</title>
      <sparkle:version>49</sparkle:version>
      <sparkle:shortVersionString>2.16.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Fri, 10 Jul 2026 22:38:15 +0000</pubDate>
      <description><![CDATA[
<!DOCTYPE html><html><head><meta charset="utf-8"><style>body {
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  font-size: 13px;
  line-height: 1.5;
  color: #1d1d1f;
  margin: 0;
  padding: 0 4px;
}
h2 {
  font-size: 17px;
  margin: 16px 0 6px 0;
  border-bottom: 1px solid #e5e5e7;
  padding-bottom: 3px;
}
h3 {
  font-size: 14px;
  margin: 14px 0 4px 0;
  color: #424245;
}
h4 {
  font-size: 13px;
  font-weight: 600;
  margin: 10px 0 2px 0;
}
p { margin: 6px 0; }
ul { margin: 6px 0; padding-left: 20px; }
li { margin: 3px 0; }
code {
  background: #f5f5f7;
  border-radius: 3px;
  padding: 1px 4px;
  font-family: "SF Mono", Menlo, Consolas, monospace;
  font-size: 12px;
}
pre {
  background: #f5f5f7;
  border-radius: 5px;
  padding: 8px 10px;
  overflow-x: auto;
  font-size: 12px;
}
pre code { background: transparent; padding: 0; }
a { color: #0066cc; text-decoration: none; }
a:hover { text-decoration: underline; }
hr {
  border: none;
  border-top: 1px solid #e5e5e7;
  margin: 16px 0;
}
strong { color: #1d1d1f; }
@media (prefers-color-scheme: dark) {
  body { color: #f5f5f7; background: #1c1c1e; }
  h2 { border-bottom-color: #38383a; }
  h3 { color: #c7c7cc; }
  code, pre { background: #2c2c2e; }
  hr { border-top-color: #38383a; }
  a { color: #4499ff; }
  strong { color: #f5f5f7; }
}
</style></head><body>
<p># Scarf v2.16.0</p>
<p><strong>Scarf now targets the Hermes v0.18 line — audited against v0.18.0 (v2026.7.1) and re-audited against the v0.18.1/v0.18.2 patches (v2026.7.7.2).</strong> The headline is invisible when it works: Hermes v0.18 introduced <strong>in-place session compaction</strong>, which soft-archives the messages it summarizes away — and Scarf&#x27;s message search now stays in perfect lockstep with Hermes on those sessions instead of silently hiding the archived rows. The two release audits also surfaced long-standing Scarf bugs, now fixed: the <strong>Web Tools settings tab had never actually worked</strong> (it wrote config keys Hermes doesn&#x27;t read), and <strong>editing a cron job from ScarfGo could silently strip scheduler state from jobs.json</strong> — including repeat counts and tool restrictions. Every v0.18 surface degrades gracefully — older Hermes hosts render exactly as before.</p>
<h2>Search keeps up with Hermes v0.18 compaction</h2>
<p>Hermes v0.18 can compact a long session <strong>in place</strong>: the old messages are summarized, marked <code>compacted</code>, and replaced by a fresh active set — but they remain discoverable through Hermes&#x27;s own search. Scarf&#x27;s search filtered strictly to active messages, so on a compacted session Scarf and <code>hermes</code> would return <strong>different results for the same query on the same database</strong>.</p>
<p>Scarf now detects the new <code>messages.compacted</code> column (the first <code>state.db</code> schema change since v0.16) and widens search to include compacted rows, exactly matching Hermes&#x27;s semantics. The distinction that matters: rows you deliberately removed with <code>/undo</code> stay hidden everywhere, compacted rows stay findable in search — and neither ever resurfaces in the chat transcript, which shows only the live conversation, same as Hermes. On pre-v0.18 databases the column doesn&#x27;t exist and nothing changes.</p>
<h2>The Web Tools tab works now (it never did)</h2>
<p>An embarrassing one, caught by the v0.18 source audit: Scarf&#x27;s Web Tools settings wrote <code>web_tools.backend</code>-style keys into <code>config.yaml</code>, but Hermes reads the <code>web:</code> block (<code>web.backend</code>, <code>web.search_backend</code>, <code>web.extract_backend</code>) — and has since at least v0.9. Because <code>hermes config set</code> accepts any key without complaint, every save &quot;succeeded,&quot; wrote a block nothing reads, and the tab read that same dead block back — a perfectly self-consistent illusion. If you ever picked a search or extract backend in Scarf and wondered why nothing changed: that&#x27;s why.</p>
<p>Both sides now use the real keys. An empty selection means what it means in Hermes: fall back to the shared backend, then auto-detect from your API keys. Any stale <code>web_tools:</code> block left in your <code>config.yaml</code> is inert and safe to delete or ignore — Scarf deliberately does <strong>not</strong> auto-migrate its values, since they were never in effect and silently activating an old choice could change a working setup.</p>
<h2>Cron jobs round-trip losslessly — the whole bug class is gone</h2>
<p>Toggling a cron job&#x27;s enabled switch in ScarfGo — or saving any edit in the cron editor — rebuilt the job from Scarf&#x27;s model and dropped everything the model didn&#x27;t know about. The v0.18.0 audit caught three such fields (<code>workdir</code>, <code>context_from</code>, <code>no_agent</code>); the v0.18.2 re-audit went deeper and found the model was missing <strong>over a dozen more</strong> that Hermes persists: <code>repeat</code> counts (a &quot;run 5 times&quot; job silently became &quot;run forever&quot;), <code>enabled_toolsets</code> (a per-job tool restriction), per-job <code>provider</code>/<code>base_url</code> routing, creation metadata, and the v0.18.2 scheduler&#x27;s new <code>run_claim</code> double-execution guard.</p>
<p>Rather than add fields forever, Scarf now preserves <strong>every key it doesn&#x27;t model</strong> through the decode → edit → encode cycle, so a future Hermes can add job fields and ScarfGo edits will never strip them again. The audit also found two keys that had <em>never</em> matched Hermes&#x27;s spelling — the pre-run script is stored as <code>script</code> (not <code>pre_run_script</code>) and cron expressions as <code>expr</code> with interval <code>minutes</code> — so those now read and write the real keys. Verified against a live jobs.json: byte-equivalent round-trip through the toggle path.</p>
<h2>No more false &quot;model/provider mismatch&quot; on custom endpoints</h2>
<p>v2.15.1 stopped the mismatch banner from misfiring on aggregator providers like OpenRouter (GH #121). The same courtesy now extends to <strong>custom endpoints</strong>: if your provider is <code>custom</code> or <code>custom:&lt;name&gt;</code> (a self-hosted vLLM, a LAN gateway), a slash in the model ID is your server&#x27;s namespace, not a stale provider prefix — Hermes itself treats <code>custom:*</code> as an aggregator and never second-guesses those configs, and now neither does Scarf.</p>
<h2>Providers: Mixture of Agents in, Vertex AI replaces Gemini CLI</h2>
<ul>
  <li><strong>MoA (Mixture of Agents)</strong> — Hermes v0.18&#x27;s virtual provider that fans a prompt out to multiple advisor models and aggregates their answers — now appears in Scarf&#x27;s model picker. It needs no credentials; its &quot;models&quot; are preset names.</li>
  <li><strong>Google Vertex AI</strong> (<code>vertex</code>) replaces the removed <code>google-gemini-cli</code> OAuth provider upstream — Gemini via GCP with service-account or ADC auth. It&#x27;s catalog-backed, so it surfaces in the picker automatically; the retired Gemini CLI entry and its aliases are gone.</li>
</ul>
<h2>Under the hood</h2>
<ul>
  <li>The full v0.18 audit ran across eight integration surfaces against the tagged Hermes source: the ACP wire protocol, all 42+ CLI invocations Scarf makes, the config keys Scarf writes, and the gateway platform roster are verified byte-stable — zero changes needed. The provider tables pass the mechanical <code>check-hermes-tables.py</code> gate against the exact <code>v2026.7.1</code> tag.</li>
  <li>The v0.18.1/v0.18.2 patches (a ~712-commit rollup) were re-audited surface-by-surface against <code>v2026.7.7.2</code>: the ACP wire, CLI argparse, provider tables, gateway platforms, MCP/skills/curator, and every state.db column Scarf queries are unchanged — the cron <code>run_claim</code> field above was the only integration-facing addition. Hermes&#x27;s new <code>sessions.display_name</code> and <code>hermes curator usage</code> are noted as future Scarf surfaces.</li>
  <li>New capability flags (<code>hasCronAttachToSession</code>, <code>hasMCPReauth</code>, <code>isV018OrLater</code>) with the standard degradation test cluster; the compacted-column handling is schema-detected, not version-gated, so it follows the database you&#x27;re actually connected to.</li>
  <li>New Hermes v0.18 verbs noted for future Scarf surfaces: <code>hermes serve</code> (headless backend), <code>hermes journey</code> (learning timeline), <code>hermes mcp reauth</code> (refresh expired MCP OAuth tokens).</li>
  <li>18 new tests: search widening + transcript narrowing on compacted DBs, cron field round-trips (including a full v0.18.2-shaped job and legacy-key migration), v0.18 capability degradation, MoA credential-gate routing, and custom-endpoint preflight skips. 799/799 ScarfCore tests green.</li>
</ul>
<h2>Upgrade notes</h2>
<ul>
  <li><strong>Sparkle</strong> will offer the update automatically, or use <strong>Scarf → Check for Updates</strong>. macOS 14.6+ deployment target unchanged. No action needed on upgrade: the compacted-search behavior activates only against v0.18 databases, and your first Web Tools save writes the correct keys.</li>
  <li>If you had configured Web Tools backends in Scarf before, re-pick them once — the old values never reached Hermes, so Scarf now shows the true (likely unset) state.</li>
  <li><strong>iOS / ScarfGo:</strong> the cron field-loss fix applies to the iOS cron editor and ships with the next TestFlight build, alongside the v2.15.0 project-context fix.</li>
</ul>
</body></html>
      ]]></description>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.16.0/Scarf-v2.16.0-Universal.zip"
                 sparkle:edSignature="k97KZnWc4hlMHACxbOJTxKgCjTETeFXwtUTiPBHEvkbvwL/4/BBBq3OYUFfs9zqBY/3Z/1Nx8/4Jau4qvtkBBw=="
                 length="21200724"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.15.1</title>
      <sparkle:version>48</sparkle:version>
      <sparkle:shortVersionString>2.15.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Sat, 04 Jul 2026 06:53:00 +0000</pubDate>
      <description><![CDATA[
<!DOCTYPE html><html><head><meta charset="utf-8"><style>body {
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  font-size: 13px;
  line-height: 1.5;
  color: #1d1d1f;
  margin: 0;
  padding: 0 4px;
}
h2 {
  font-size: 17px;
  margin: 16px 0 6px 0;
  border-bottom: 1px solid #e5e5e7;
  padding-bottom: 3px;
}
h3 {
  font-size: 14px;
  margin: 14px 0 4px 0;
  color: #424245;
}
h4 {
  font-size: 13px;
  font-weight: 600;
  margin: 10px 0 2px 0;
}
p { margin: 6px 0; }
ul { margin: 6px 0; padding-left: 20px; }
li { margin: 3px 0; }
code {
  background: #f5f5f7;
  border-radius: 3px;
  padding: 1px 4px;
  font-family: "SF Mono", Menlo, Consolas, monospace;
  font-size: 12px;
}
pre {
  background: #f5f5f7;
  border-radius: 5px;
  padding: 8px 10px;
  overflow-x: auto;
  font-size: 12px;
}
pre code { background: transparent; padding: 0; }
a { color: #0066cc; text-decoration: none; }
a:hover { text-decoration: underline; }
hr {
  border: none;
  border-top: 1px solid #e5e5e7;
  margin: 16px 0;
}
strong { color: #1d1d1f; }
@media (prefers-color-scheme: dark) {
  body { color: #f5f5f7; background: #1c1c1e; }
  h2 { border-bottom-color: #38383a; }
  h3 { color: #c7c7cc; }
  code, pre { background: #2c2c2e; }
  hr { border-top-color: #38383a; }
  a { color: #4499ff; }
  strong { color: #f5f5f7; }
}
</style></head><body>
<p># Scarf v2.15.1</p>
<p><strong>The model/provider mismatch banner learned how OpenRouter works.</strong> If your Hermes is configured with an aggregator provider — OpenRouter, OpenCode, KiloCode, Hugging Face, NovitaAI — Scarf&#x27;s chat preflight could flag a perfectly valid config as a &quot;Model/provider mismatch&quot; and offer two one-click fixes that would each have <strong>broken</strong> it (<a href="https://github.com/awizemann/scarf/issues/121">#121</a>). That false alarm is gone, and the banner&#x27;s fix buttons are now validated so they can never write a provider Hermes doesn&#x27;t have. Thanks to <strong>@rmichelena</strong> for the sharp report.</p>
<h2>The mismatch banner and aggregator providers</h2>
<p>The banner exists for a real failure: switching providers via Credential Pools could leave the old provider&#x27;s prefixed model stranded in <code>model.default</code> (say <code>anthropic/claude-…</code> with <code>model.provider: nous</code>), and chats would die at first prompt with an opaque <code>-32603</code> error. The check caught that by treating anything before a <code>/</code> in <code>model.default</code> as a provider prefix.</p>
<p>But for <strong>aggregator</strong> providers, the slash is part of the model&#x27;s own name — OpenRouter&#x27;s <code>xiaomi/mimo-v2.5</code> is <code>org/model</code>, not <code>provider/model</code>. Hermes models this distinction explicitly (<code>is_aggregator</code> in its provider table); Scarf&#x27;s check didn&#x27;t, so a working OpenRouter config raised the banner, and both offered fixes (&quot;use <code>xiaomi</code>&quot; / &quot;strip the prefix&quot;) would have corrupted <code>config.yaml</code>. The preflight now skips the mismatch check entirely when <code>model.provider</code> resolves to an aggregator — including Hermes&#x27;s bare-<code>openai</code> → OpenRouter alias.</p>
<p>The original protection is untouched: a genuinely stale prefix under a direct provider still raises the banner with the same one-click fixes.</p>
<h2>The fix buttons can no longer write a broken provider</h2>
<p>Two more hardening passes on the same banner:</p>
<ul>
  <li><strong>Alias-aware comparison.</strong> Hermes accepts many provider spellings (<code>claude</code> ↔ <code>anthropic</code>, <code>x-ai</code> ↔ <code>xai</code>, <code>zhipu</code> ↔ <code>zai</code>, …). Scarf now mirrors Hermes&#x27;s full alias table and canonicalizes both sides before comparing, so an alias-equivalent prefix no longer reads as a mismatch.</li>
  <li><strong>Unknown prefixes are contained.</strong> If <code>model.default</code> carries a prefix that isn&#x27;t any provider Hermes knows, the banner still warns (that config really will fail) — but the &quot;Use <em>prefix</em>&quot; button is hidden rather than offering to write a nonexistent provider into <code>config.yaml</code>, and the banner explains the likely aggregator intent. The provider roster comes from the models.dev catalog and is only consulted on the rare mismatch path, so remote (SSH) servers pay nothing on normal refreshes.</li>
</ul>
<h2>Under the hood</h2>
<ul>
  <li>Scarf hand-mirrors three provider tables out of Hermes&#x27;s source; a new <code>scripts/check-hermes-tables.py</code> now diffs all three against <code>hermes_cli/providers.py</code> mechanically and fails loudly on drift, replacing the eyeball reconciliation on every Hermes bump. All tables verified against the exact Hermes v0.16.0 (<code>v2026.6.5</code>) tag Scarf targets.</li>
  <li>Dormant provider-overlay entries (<code>lmstudio</code>, <code>tencent-tokenhub</code> — since absorbed by models.dev) are annotated as deliberate stale-cache fallback so the checker&#x27;s warnings read as policy, not drift.</li>
  <li>11 new <code>ModelPreflight</code> tests pin the aggregator skip, alias equivalence, unknown-prefix containment, and the original stale-prefix regression guard.</li>
</ul>
<h2>Upgrade notes</h2>
<ul>
  <li><strong>Sparkle</strong> will offer the update automatically, or use <strong>Scarf → Check for Updates</strong>. macOS 14.6+ deployment target unchanged. No data migrations, and no config changes — if you saw the false banner on an aggregator config, it simply disappears; nothing to reconcile.</li>
  <li><strong>iOS / ScarfGo:</strong> this is a Mac-track release. Also on main since v2.15.0: ScarfGo project chats now load the full project context (AGENTS.md, cron, config, template) — that fix rides the next TestFlight build.</li>
</ul>
</body></html>
      ]]></description>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.15.1/Scarf-v2.15.1-Universal.zip"
                 sparkle:edSignature="IYHOZqZ+jJA9N80d/vFdrcU6Mw+swKvkel4yIqbybLwYylj2676ifd54uCV4ffUbwYomUzVT16Cdw7COrUWNCQ=="
                 length="21179573"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.15.0</title>
      <sparkle:version>47</sparkle:version>
      <sparkle:shortVersionString>2.15.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Sun, 28 Jun 2026 18:40:11 +0000</pubDate>
      <description><![CDATA[
<!DOCTYPE html><html><head><meta charset="utf-8"><style>body {
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  font-size: 13px;
  line-height: 1.5;
  color: #1d1d1f;
  margin: 0;
  padding: 0 4px;
}
h2 {
  font-size: 17px;
  margin: 16px 0 6px 0;
  border-bottom: 1px solid #e5e5e7;
  padding-bottom: 3px;
}
h3 {
  font-size: 14px;
  margin: 14px 0 4px 0;
  color: #424245;
}
h4 {
  font-size: 13px;
  font-weight: 600;
  margin: 10px 0 2px 0;
}
p { margin: 6px 0; }
ul { margin: 6px 0; padding-left: 20px; }
li { margin: 3px 0; }
code {
  background: #f5f5f7;
  border-radius: 3px;
  padding: 1px 4px;
  font-family: "SF Mono", Menlo, Consolas, monospace;
  font-size: 12px;
}
pre {
  background: #f5f5f7;
  border-radius: 5px;
  padding: 8px 10px;
  overflow-x: auto;
  font-size: 12px;
}
pre code { background: transparent; padding: 0; }
a { color: #0066cc; text-decoration: none; }
a:hover { text-decoration: underline; }
hr {
  border: none;
  border-top: 1px solid #e5e5e7;
  margin: 16px 0;
}
strong { color: #1d1d1f; }
@media (prefers-color-scheme: dark) {
  body { color: #f5f5f7; background: #1c1c1e; }
  h2 { border-bottom-color: #38383a; }
  h3 { color: #c7c7cc; }
  code, pre { background: #2c2c2e; }
  hr { border-top-color: #38383a; }
  a { color: #4499ff; }
  strong { color: #f5f5f7; }
}
</style></head><body>
<p># Scarf v2.15.0</p>
<p><strong>Projects grow up.</strong> This release turns a Scarf project from &quot;a folder your chats happen to point at&quot; into a first-class object with its own mission control — and gives it three big new powers: <strong>mini-apps</strong> (sandboxed web panels that can drive your agent), a <strong>fleet/portfolio</strong> view that groups the same project across every machine you run it on, and <strong>one-click Upgrade Project</strong> that brings an existing repo up to the full experience. Project chats also now load your <code>AGENTS.md</code>, so the agent finally knows the project it&#x27;s working in. It&#x27;s the largest Projects update since v2.3.</p>
<h2>Mini-apps — sandboxed panels that drive your agent</h2>
<p>A <strong>mini-app</strong> is a small web UI (plain HTML/CSS/JS) that lives inside a project and renders in a slide-in panel in the project cockpit. It&#x27;s the answer to &quot;I wish this project had a custom dashboard / approval queue / task board / chart that could actually <em>talk to the agent</em>.&quot; Mini-apps can read project data and send the agent prompts — through a tiny, versioned <code>window.scarf</code> bridge — without ever leaving the sandbox.</p>
<p>Two things make this safe enough to ship:</p>
<ul>
  <li><strong>Everything runs in a locked-down <code>WKWebView.</code></strong> Non-persistent storage (no cross-app cookie or <code>localStorage</code> bleed), assets served only through a project-scoped <code>scarf-miniapp://</code> handler, navigation pinned to that scheme, and a strict Content-Security-Policy (<code>default-src &#x27;none&#x27;; connect-src &#x27;none&#x27;</code>) that blocks all network access. A mini-app is self-contained by construction — no external scripts, no CDNs, no <code>fetch()</code> to the internet. File reads resolve through symlink-containment so a planted link can&#x27;t escape the project directory.</li>
  <li><strong>Default-deny permissions, reviewed on first open.</strong> Every sensitive capability a mini-app declares — sending the agent prompts, reading files, querying data — is <strong>off until you approve it</strong>. The first time you open a mini-app, Scarf shows a permission sheet listing exactly what it wants; sensitive permissions are pre-unchecked for <strong>agent-generated</strong> mini-apps and unknown permissions are refused outright. Your decision is saved per <code>(project, mini-app)</code>, and changing it later live-revokes access.</li>
</ul>
<p>When a mini-app calls <code>scarf.prompt(...)</code>, it gets its <strong>own dedicated <code>hermes acp</code> session</strong> (spawned lazily, torn down when the panel closes), isolated from your chats — web content can&#x27;t reach into other conversations. That session is rate-limited (8 prompts/60s) to cap runaways, and because no human is watching it turn-by-turn, any tool-permission request the agent raises mid-turn is auto-denied.</p>
<p><strong>Building one:</strong> ask the agent — the bundled <strong><code>scarf-miniapp-author</code></strong> skill scaffolds the manifest, directory, and a working starter. Or run <strong>Upgrade Project</strong> (below), which has the agent build a starter mini-app for you. Installed mini-apps show up in the cockpit&#x27;s <strong>Mini-apps</strong> panel, with agent-generated ones clearly flagged.</p>
<p>&gt; <strong>v1 scope.</strong> This release wires the read + prompt surfaces: <code>scarf.prompt</code>, <code>scarf.onEvent</code> (streamed agent output), <code>scarf.store</code> (per-app key-value), <code>scarf.file.read</code>, and <code>scarf.query(&quot;kanban.tasks&quot;)</code>. Writing surfaces (<code>kanban:write</code>, <code>file:write</code>) and outbound network (<code>net</code>) are declared in the permission model but not yet enabled — they&#x27;re coming, and the sandbox already accounts for them.</p>
<h2>Fleet &amp; Portfolio — your project across every machine</h2>
<p>If you run the same repo on more than one host — your laptop and a server, say — Scarf now understands those as <strong>one logical project</strong>. Because a project&#x27;s identity is a stable id minted once and carried in its <code>project.json</code>, cloning the repo to another machine and registering it there groups both copies under a single <strong>Fleet</strong> view in the cockpit.</p>
<p>The Fleet panel shows each host&#x27;s copy side by side and flags <strong>drift</strong> — places where the same project disagrees across machines (a different bound model preset, a renamed board, a different cron-job count, mismatched mini-apps). When you&#x27;ve got one host configured the way you want, <strong>Apply to Fleet…</strong> pushes that config out to the others: the model preset, the Kanban board, and the project&#x27;s cron jobs are recreated on the targets you select.</p>
<p>Apply-to-fleet is deliberately conservative. Every host is handled independently — one unreachable machine or one failed field never aborts the rest, and you get a per-host, per-field report of what was applied, skipped, or failed. Copying cron jobs <strong>probes the target host&#x27;s Hermes version first</strong> and drops flags that an older Hermes wouldn&#x27;t understand (e.g. <code>--deliver all</code> on pre-0.14) rather than failing the copy. The same version-gating now protects template-install cron jobs.</p>
<h2>One-click Upgrade Project</h2>
<p>Got a project from before all this — or one you scaffolded by hand? <strong>Upgrade Project</strong> brings it up to the full first-class experience. A cockpit banner offers it; one click runs a fast, idempotent structural pass (mints a stable id and <code>project.json</code>, creates a Kanban board, refreshes the Scarf-managed <code>AGENTS.md</code> block, seeds a placeholder dashboard) and then hands off to chat, where the agent tailors a real dashboard, slash commands, cron jobs, and a starter mini-app to <em>your</em> project. Re-running it is a no-op, so it&#x27;s safe to click.</p>
<h2>The cockpit — one pane for the whole project</h2>
<p>Selecting a project now opens a single <strong>cockpit</strong> — mission control for everything about that project, instead of the old narrow Dashboard/Site/Slash tab bar. A unified header (name, path, bound model preset, the hosts it lives on) sits above a flat row of panels: <strong>Dashboard, Sessions, Board, Site, Context, Cron, Memory, Secrets, Templates, Slash, Mini-apps,</strong> and <strong>Fleet</strong>. Panels that don&#x27;t apply to a given project hide themselves, so even a bare project gets a clean, complete control center. (Secrets shows field <em>names</em> only — values stay in the Keychain.)</p>
<h2>Project chats now load your AGENTS.md</h2>
<p>Opening a chat <strong>inside a project</strong> finally gives the agent that project&#x27;s context. Scarf now spawns <code>hermes acp</code> with the project as its working directory, so Hermes loads the project&#x27;s <code>AGENTS.md</code> / <code>CLAUDE.md</code> / <code>.cursorrules</code> into the agent automatically — for new chats and for resumed, reconnected, and auto-started ones alike. No more re-explaining the repo every time.</p>
<p>&gt; <strong>A note on trust.</strong> Opening a chat in a project now loads that project&#x27;s <code>AGENTS.md</code> / <code>CLAUDE.md</code> / <code>.cursorrules</code> into the agent (so it has project context). Treat a project&#x27;s context files like its code — only open chats in projects you trust. (Mini-apps deliberately do <strong>not</strong> load these files into their agent sessions, since they run less-trusted web content.)</p>
<h2>Smaller fixes</h2>
<ul>
  <li><strong>Remote SSH paths are hardened against command injection</strong> — <code>$</code> and backticks in a remote project path are now escaped before they reach the shell.</li>
  <li><strong>Window size and position persist again.</strong> SwiftUI was saving the frame but never restoring it; Scarf now restores it manually on launch.</li>
  <li><strong>Mini-app streamed events keep their order</strong> — agent output is delivered FIFO on the main thread instead of racing per-event tasks.</li>
</ul>
<h2>Under the hood</h2>
<ul>
  <li>The whole release sits on a new <strong>first-class <code>ScarfProject</code> object</strong> — a portable <code>project.json</code> plus a per-host registry — that replaces project identity previously smeared across sessions, AGENTS.md blocks, template locks, and cron tags.</li>
  <li>New <code>scripts/build-detached.sh</code> launches an isolated, visually-distinct dev copy you can dogfood while you keep building.</li>
  <li>Test-suite isolation fixes (per-instance temp homes, de-flaked remote-SQLite tilde-home test) and a sweep making off-main helpers <code>nonisolated</code> for the app&#x27;s MainActor-default isolation.</li>
</ul>
<h2>Upgrade notes</h2>
<ul>
  <li><strong>Sparkle</strong> will offer the update automatically, or use <strong>Scarf → Check for Updates</strong>. macOS 14.6+ deployment target unchanged. <strong>No data migrations</strong> — existing projects keep working; click <strong>Upgrade Project</strong> when you want the new structure.</li>
  <li><strong>Mini-apps</strong> are new this release; nothing you have changes until you add or generate one. The writing/network mini-app surfaces noted above arrive in a later release.</li>
  <li><strong>iOS / ScarfGo:</strong> this is a Mac-track release. The shared-core pieces (first-class project model) ride along, but the Projects UI is Mac-only for now.</li>
</ul>
</body></html>
      ]]></description>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.15.0/Scarf-v2.15.0-Universal.zip"
                 sparkle:edSignature="bzMifgFX7RfFqzWU6I3lfloEUmZze7g055mA8Y45w93iJv/SWIO3F/D1uiUdwawZmPcFVFCr3HlRSDanvsqBDw=="
                 length="21363809"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.13.0</title>
      <sparkle:version>46</sparkle:version>
      <sparkle:shortVersionString>2.13.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Fri, 26 Jun 2026 06:03:50 +0000</pubDate>
      <description><![CDATA[
<!DOCTYPE html><html><head><meta charset="utf-8"><style>body {
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  font-size: 13px;
  line-height: 1.5;
  color: #1d1d1f;
  margin: 0;
  padding: 0 4px;
}
h2 {
  font-size: 17px;
  margin: 16px 0 6px 0;
  border-bottom: 1px solid #e5e5e7;
  padding-bottom: 3px;
}
h3 {
  font-size: 14px;
  margin: 14px 0 4px 0;
  color: #424245;
}
h4 {
  font-size: 13px;
  font-weight: 600;
  margin: 10px 0 2px 0;
}
p { margin: 6px 0; }
ul { margin: 6px 0; padding-left: 20px; }
li { margin: 3px 0; }
code {
  background: #f5f5f7;
  border-radius: 3px;
  padding: 1px 4px;
  font-family: "SF Mono", Menlo, Consolas, monospace;
  font-size: 12px;
}
pre {
  background: #f5f5f7;
  border-radius: 5px;
  padding: 8px 10px;
  overflow-x: auto;
  font-size: 12px;
}
pre code { background: transparent; padding: 0; }
a { color: #0066cc; text-decoration: none; }
a:hover { text-decoration: underline; }
hr {
  border: none;
  border-top: 1px solid #e5e5e7;
  margin: 16px 0;
}
strong { color: #1d1d1f; }
@media (prefers-color-scheme: dark) {
  body { color: #f5f5f7; background: #1c1c1e; }
  h2 { border-bottom-color: #38383a; }
  h3 { color: #c7c7cc; }
  code, pre { background: #2c2c2e; }
  hr { border-top-color: #38383a; }
  a { color: #4499ff; }
  strong { color: #f5f5f7; }
}
</style></head><body>
<p># Scarf v2.13.0</p>
<p>ScarfGo — the iOS companion — gets its headline feature this release: <strong>switch between your Hermes profiles right from your phone</strong>, without disturbing the profile your Mac, terminal, or running gateway is using on that host. It ships alongside a <strong>reliability fix for remote chat and Settings on iOS</strong> that anyone running ScarfGo against an SSH host will feel immediately — connections no longer churn under load. These are iOS-track changes; the shared-core pieces ride into the Mac app too.</p>
<h2>Switch Hermes profiles from ScarfGo</h2>
<p>If you run more than one Hermes profile on a host — say an <code>admin</code> profile and a locked-down <code>gateway</code> profile — ScarfGo could <em>list</em> them but not operate against them; switching was Mac-only. Now the Profiles tab is a <strong>switcher</strong>: pick a profile and ScarfGo points its chat, memory, cron, sessions, gateway, and every <code>hermes</code> call at that profile&#x27;s <code>HERMES_HOME</code> for the selected server.</p>
<p>Crucially, this uses <strong>per-connection scoping (Design B)</strong>: ScarfGo scopes <em>its own</em> view via <code>HERMES_HOME</code> / <code>remoteHome</code> and <strong>never runs <code>hermes profile use</code></strong>, so it does <strong>not</strong> touch the host&#x27;s <code>active_profile</code>. Your Mac app, terminal, cron daemon, and the running gateway on that host keep using whatever profile they were on — switching on the phone is invisible to them. (Verified on a live host: a profile switch leaves <code>~/.hermes/active_profile</code> unchanged.) Profile create / rename / delete / import / export remain Mac-only by design.</p>
<h2>Reliable remote chat &amp; Settings on iOS</h2>
<p>Some ScarfGo users hit <strong>&quot;Couldn&#x27;t save model.provider to config.yaml via hermes config set — Transport refused the command,&quot;</strong> with Settings showing the model and config source empty even though the config was valid and the same command worked over plain <code>ssh</code>. It reproduced across unrelated hosts — which pointed at ScarfGo, not the host.</p>
<p>Root cause: ScarfGo opened a <strong>brand-new SSH connection for every file read and CLI call</strong>. A Settings load or chat pre-flight fans out a burst of those, and under the churn the SSH handshake itself starts failing — surfaced as &quot;Transport refused&quot; on writes and silently swallowed to &quot;empty&quot; on reads. A single manual <code>ssh</code> never reproduced it because it&#x27;s one connection.</p>
<p>ScarfGo now <strong>pools one SSH connection per server and coalesces concurrent opens</strong>, so reads and writes reuse one warm connection — the iOS equivalent of the Mac app&#x27;s SSH ControlMaster. Verified against a real sshd: a 24-way concurrent burst went from 20-of-24 connection failures to <strong>zero</strong>. The chat pre-flight also now surfaces the real connection error instead of a generic line. (This is separate from the Docker <code>~/.hermes</code>-in-container read path, which is still tracked.)</p>
<h2>Under the hood</h2>
<ul>
  <li><strong>Skills &quot;What&#x27;s New&quot; now tracks each profile independently.</strong> After per-connection switching landed, the Skills pill could show bogus &quot;new / changed&quot; counts by diffing one profile&#x27;s skills against another profile&#x27;s baseline (on iOS it bled across servers too). The last-seen snapshot is now keyed per (server, profile); the skills list itself was always correct — only the diff indicator was wrong.</li>
  <li>The ScarfIOS package is now unit-testable on macOS (<code>swift test</code>), and a gated live integration test drives the real Citadel SSH transport through the pool against an ephemeral sshd to guard the churn fix against regressions.</li>
  <li>iOS direct-file reads now honor the selected profile (previously they ignored profiles and always read the host default), closing a latent half-switch.</li>
</ul>
<h2>Upgrade notes</h2>
<ul>
  <li><strong>iOS / ScarfGo:</strong> these are iOS-track features — a ScarfGo TestFlight build carrying them is queued on the iOS track (independent of the Mac update path).</li>
  <li><strong>Mac:</strong> the shared-core changes (profile scoping in ScarfCore) ride into the Mac app; Sparkle will offer the update automatically (or <strong>Scarf → Check for Updates</strong>). macOS 14.6+ deployment target unchanged. No data migrations.</li>
</ul>
</body></html>
      ]]></description>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.13.0/Scarf-v2.13.0-Universal.zip"
                 sparkle:edSignature="bMSrZZ7f74399lMYc11c18j+T9AQB441bcEuFmFFwjjWnk6XsgbpM2wwO71TxucztKgYelO1bzPqoQi3bPOHCQ=="
                 length="20679293"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.12.0</title>
      <sparkle:version>44</sparkle:version>
      <sparkle:shortVersionString>2.12.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Mon, 22 Jun 2026 04:27:05 +0000</pubDate>
      <description><![CDATA[
<!DOCTYPE html><html><head><meta charset="utf-8"><style>body {
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  font-size: 13px;
  line-height: 1.5;
  color: #1d1d1f;
  margin: 0;
  padding: 0 4px;
}
h2 {
  font-size: 17px;
  margin: 16px 0 6px 0;
  border-bottom: 1px solid #e5e5e7;
  padding-bottom: 3px;
}
h3 {
  font-size: 14px;
  margin: 14px 0 4px 0;
  color: #424245;
}
h4 {
  font-size: 13px;
  font-weight: 600;
  margin: 10px 0 2px 0;
}
p { margin: 6px 0; }
ul { margin: 6px 0; padding-left: 20px; }
li { margin: 3px 0; }
code {
  background: #f5f5f7;
  border-radius: 3px;
  padding: 1px 4px;
  font-family: "SF Mono", Menlo, Consolas, monospace;
  font-size: 12px;
}
pre {
  background: #f5f5f7;
  border-radius: 5px;
  padding: 8px 10px;
  overflow-x: auto;
  font-size: 12px;
}
pre code { background: transparent; padding: 0; }
a { color: #0066cc; text-decoration: none; }
a:hover { text-decoration: underline; }
hr {
  border: none;
  border-top: 1px solid #e5e5e7;
  margin: 16px 0;
}
strong { color: #1d1d1f; }
@media (prefers-color-scheme: dark) {
  body { color: #f5f5f7; background: #1c1c1e; }
  h2 { border-bottom-color: #38383a; }
  h3 { color: #c7c7cc; }
  code, pre { background: #2c2c2e; }
  hr { border-top-color: #38383a; }
  a { color: #4499ff; }
  strong { color: #f5f5f7; }
}
</style></head><body>
<p># Scarf v2.12.0</p>
<p>A coordinated catch-up to <strong>Hermes v0.17.0 (2026.6.19)</strong> — by far the largest Hermes release yet, though Scarf only needed a focused slice of it — bundled with a <strong>remote-chat performance fix</strong> that anyone running Scarf against an SSH host will feel immediately. The audit of v0.17 also turned up several pre-existing bugs in Scarf&#x27;s own CLI plumbing, which are fixed here. Every new v0.17 surface is capability-gated, so pre-v0.17 hosts render byte-identical to v2.11.0, and all flag/config/wire shapes were verified against the live Hermes v0.17 source before implementation.</p>
<h2>Remote chat is responsive again</h2>
<p>If you chat against a remote Hermes over SSH, typing into an active session could lag and spike CPU. Root cause: during a live chat, every persisted message bumps <code>state.db-wal</code>&#x27;s modification time, which fires Scarf&#x27;s file watcher — and several view models were doing <strong>synchronous</strong> SSH/scp reads <strong>on the main thread</strong> on every tick. The biggest offender was the chat&#x27;s credential preflight (reading <code>.env</code> + <code>auth.json</code> over SSH per tick), alongside the sessions-list, platforms, and projects refreshes. A ScarfMon capture showed <code>loadRecentSessions</code> taking 2.7–4.2 s while the chat render itself measured ~0 ms — the lag was main-thread I/O contention, not rendering.</p>
<p>Every watcher-driven read now runs off the main actor (debounced where appropriate), with cancel-prior + recency guards so a slower older read can&#x27;t clobber fresher state. Two adversarial review passes verified the concurrency ordering. (Split from the original Dashboard CPU spin fixed in v2.10.3; thanks to the reporter whose ScarfMon dump localized it.)</p>
<h2>Four broken Health/Settings actions, fixed</h2>
<p>Auditing v0.17 surfaced four Scarf actions that had been quietly broken — in some cases since they shipped — because they invoked the <code>hermes</code> CLI with the wrong verb or flags:</p>
<ul>
  <li><strong>Run supply-chain audit</strong> called a bare <code>hermes audit</code>, which isn&#x27;t a command — it was being interpreted as a chat prompt and burning an agent turn instead of running the scan. It now calls <code>hermes security audit</code>.</li>
  <li><strong>Migrate retired xAI model</strong> ran <code>hermes migrate xai</code>, which is dry-run by default — so it printed a plan, wrote nothing, and still reported success while the retired-model warning never cleared. It now passes <code>--apply</code> (and no longer claims success on a no-op).</li>
  <li><strong>Set up browser tools</strong> passed <code>--assume-yes</code>, which the CLI rejects; the correct flag is <code>--yes</code>.</li>
  <li><strong>WhatsApp recipient allowlist</strong> wrote <code>whatsapp.allowed_chats</code>, a key Hermes never reads (WhatsApp gates senders via <code>allow_from</code>), so the allowlist silently did nothing. It&#x27;s been removed from the chat-id editor rather than left lying. Saving a setting that fails now surfaces the real reason (e.g. an administrator-managed key) instead of a generic &quot;Failed to save.&quot;</li>
</ul>
<h2>Curator &quot;Prune&quot; is now &quot;Archive idle skills&quot; — and it actually works</h2>
<p>Scarf&#x27;s Curator screen had a destructive <strong>&quot;Prune Archived&quot;</strong> action that claimed to <em>permanently delete archived skills from disk</em>. It turns out no such Hermes command exists — <code>hermes curator prune</code> does the opposite: it <strong>bulk-archives _active_ skills that have been idle for N days</strong>, and archiving is fully reversible via Restore. On top of the wrong mental model, Scarf invoked it with a flag that doesn&#x27;t exist and without the confirmation-skip flag, so a real run would hang until it timed out.</p>
<p>It&#x27;s been rebuilt to match reality: a <strong>&quot;Archive idle skills&quot;</strong> action in the Curator menu with a 30/60/90/180-day threshold, a preview of which idle skills would be archived (and how long they&#x27;ve been idle), and a confirm sheet that&#x27;s correctly framed as reversible — not a red &quot;permanent delete&quot; gate. The CLI invocation and output parsing now match the real verb.</p>
<h2>New gateway platforms: WhatsApp Cloud + SimpleX</h2>
<ul>
  <li><strong>WhatsApp Business Cloud API</strong> (Meta&#x27;s hosted webhook path — no bridge process) joins the platform list with a full setup form: phone number ID, access token, webhook verify token + app secret, and a direct-message allowlist. It&#x27;s distinct from the existing QR-paired WhatsApp web bridge.</li>
  <li><strong>SimpleX Chat</strong> finally has a setup form (it had been in the platform list since v0.14 with no way to configure it): daemon WebSocket URL, contact/group allowlists, auto-accept, and the home channel.</li>
</ul>
<h2>More v0.17 surfaces</h2>
<ul>
  <li><strong>Telegram rich messages</strong> (Bot API 10.1, on by default) and an <strong>online/offline status indicator</strong> (opt-in) are exposed as toggles.</li>
  <li><strong>Curator consolidation is now opt-in.</strong> v0.17 turned the curator&#x27;s LLM skill-merge pass off by default; Settings → Advanced now has a toggle (with a note) so anyone who relied on the automatic pass can turn it back on. Deterministic pruning is unaffected.</li>
  <li><strong>Max concurrent sessions</strong> — the new optional cap on simultaneously-active chat sessions is exposed as a stepper (0 = unlimited).</li>
</ul>
<h2>Under the hood</h2>
<ul>
  <li><strong>Verified against the live v0.17 source.</strong> The headline finding of the audit: v0.17 required <strong>zero</strong> mandatory compatibility changes — the <code>state.db</code> schema, ACP wire protocol, CLI verbs, config keys, and model catalog are all stable — so this release is a focused feature catch-up plus the pre-existing-bug fixes above, not a forced migration.</li>
  <li><strong>New capability flags</strong> (<code>hasCuratorConsolidate</code>, <code>hasMaxConcurrentSessions</code>, <code>hasTelegramRichMessages</code>, <code>hasPhotonPlatform</code>, <code>hasWhatsAppCloudPlatform</code>) plus the <code>isV017OrLater</code> predicate gate every new surface; an adversarial fresh-eyes audit of the whole branch caught a real bug before release (WhatsApp Cloud is a built-in platform, so it needed an explicit <code>enabled</code> write to actually start).</li>
  <li><strong>642 ScarfCore tests</strong> pass, including a full v0.17 capability cluster (flags-on / pre-v0.17-degradation / patch-still-on) and the v0.16 cluster that the prior cycle had shipped without.</li>
  <li><strong>Deferred</strong> (tracked for later): iMessage via Photon (the underlying protocol is still changing upstream), the per-MCP-server keepalive knob, and a pre-existing Google Chat allowlist fix.</li>
</ul>
<h2>Upgrade notes</h2>
<ul>
  <li>Sparkle will offer this update automatically on next launch (or <strong>Scarf → Check for Updates</strong>).</li>
  <li>macOS 14.6+ (Sonoma) deployment target unchanged.</li>
  <li><strong>Fully backward compatible.</strong> Every v0.17 surface is capability-gated; on Hermes v0.16.x or earlier, Scarf renders exactly as v2.11.0 did. There are no data migrations.</li>
  <li><strong>iOS testers:</strong> the shared-core changes (the curator/config/platform model updates and the off-main read fixes) ride in ScarfCore; a ScarfGo TestFlight build carrying them is queued separately on the iOS track.</li>
</ul>
</body></html>
      ]]></description>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.12.0/Scarf-v2.12.0-Universal.zip"
                 sparkle:edSignature="A7OUDT0p2XysB4Iaaxd0EAMN/xPCjxHn3OkyHj8wwuJO4mLr4Y1hoTWihKrZHuRs0ZdeaPDXVPuRPZ/xxDrUAg=="
                 length="20673715"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.11.0</title>
      <sparkle:version>43</sparkle:version>
      <sparkle:shortVersionString>2.11.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Sun, 21 Jun 2026 08:34:59 +0000</pubDate>
      <description><![CDATA[
<!DOCTYPE html><html><head><meta charset="utf-8"><style>body {
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  font-size: 13px;
  line-height: 1.5;
  color: #1d1d1f;
  margin: 0;
  padding: 0 4px;
}
h2 {
  font-size: 17px;
  margin: 16px 0 6px 0;
  border-bottom: 1px solid #e5e5e7;
  padding-bottom: 3px;
}
h3 {
  font-size: 14px;
  margin: 14px 0 4px 0;
  color: #424245;
}
h4 {
  font-size: 13px;
  font-weight: 600;
  margin: 10px 0 2px 0;
}
p { margin: 6px 0; }
ul { margin: 6px 0; padding-left: 20px; }
li { margin: 3px 0; }
code {
  background: #f5f5f7;
  border-radius: 3px;
  padding: 1px 4px;
  font-family: "SF Mono", Menlo, Consolas, monospace;
  font-size: 12px;
}
pre {
  background: #f5f5f7;
  border-radius: 5px;
  padding: 8px 10px;
  overflow-x: auto;
  font-size: 12px;
}
pre code { background: transparent; padding: 0; }
a { color: #0066cc; text-decoration: none; }
a:hover { text-decoration: underline; }
hr {
  border: none;
  border-top: 1px solid #e5e5e7;
  margin: 16px 0;
}
strong { color: #1d1d1f; }
@media (prefers-color-scheme: dark) {
  body { color: #f5f5f7; background: #1c1c1e; }
  h2 { border-bottom-color: #38383a; }
  h3 { color: #c7c7cc; }
  code, pre { background: #2c2c2e; }
  hr { border-top-color: #38383a; }
  a { color: #4499ff; }
  strong { color: #f5f5f7; }
}
</style></head><body>
<p># Scarf v2.11.0</p>
<p>A coordinated catch-up to <strong>Hermes v0.16.0 (2026.6.5)</strong>. The headline is a correctness fix for the first <code>state.db</code> schema change since v0.11 — Hermes can now <strong>soft-delete messages</strong> when you rewind a conversation, and Scarf needs to hide them to show you what the agent actually sees. Alongside it: live session titles, a &quot;rewound&quot; indicator, the Spotify-skill-became-a-plugin move, two real gateway-config bugs fixed, and a handful of v0.16 quick wins. Every new surface is capability-gated or schema-detected, so pre-v0.16 hosts render byte-identical to v2.10.3. All flag, config, and wire shapes were verified against the live Hermes v0.16 source before implementation.</p>
<h2>Rewound messages are hidden, matching what the agent sees</h2>
<p>v0.16 is the first <code>state.db</code> schema change since v0.11: the <code>messages</code> table gained an <code>active</code> column. When you run <code>/undo</code> in Hermes, the rewound messages are flipped to inactive (a soft delete) rather than being removed — so the agent no longer sees them, but they&#x27;re still on disk. Scarf was reading every row regardless, which meant a rewound chat showed you messages the agent had already discarded.</p>
<p>Scarf now filters to active messages on <strong>every</strong> message-read path — chat transcript, resume, skeleton loaders, the lot. The filter is schema-gated: Scarf detects the column first (a <code>PRAGMA</code> check locally, a preflight on remote hosts), so connecting to a pre-v0.16 Hermes that doesn&#x27;t have the column still works exactly as before instead of erroring with &quot;no such column.&quot; The net effect: what you see in a rewound conversation now matches what the agent is actually working from.</p>
<h2>Session titles update live</h2>
<p>When Hermes generates or regenerates a session&#x27;s title, v0.16 now pushes an ACP notification (<code>session_info_update</code>). Scarf listens for it and updates the title in the Mac chat sidebar in place — no need to navigate away and back to see a freshly-titled session get its name. (Title-setting itself remains a Hermes/gateway concern; there&#x27;s no ACP path to set a title from Scarf, by design.)</p>
<h2>&quot;Rewound ×N&quot; badge on sessions</h2>
<p>v0.16 tracks how many times a session has been rewound in a new <code>sessions.rewind_count</code> column. Scarf schema-detects it and surfaces a <strong>&quot;rewound ×N&quot;</strong> badge in the chat sidebar, the Sessions table, and the dashboard rows, so a heavily-edited session is visible at a glance. Like the message filter, this is schema-gated and read by column name, so older hosts simply don&#x27;t show the badge.</p>
<h2>Spotify sign-in moved to Plugins</h2>
<p>In v0.16, Spotify is no longer a skill — it became a built-in tool with a native plugin. Scarf follows suit: on a v0.16 host, the Spotify sign-in affordance now lives in the <strong>Plugins</strong> view instead of Skills (the <code>hermes auth spotify</code> PKCE flow is unchanged). The old Skills-view path is kept for pre-v0.16 hosts, so both render correctly depending on what you&#x27;re connected to.</p>
<h2>Two gateway-config bugs fixed</h2>
<p>Both were silent failures found while verifying Scarf&#x27;s writes against the live v0.16 source:</p>
<ul>
  <li><strong>Platform allowlists now actually apply.</strong> Scarf was reading and writing channel/chat/room allowlists under <code>gateway.platforms.&lt;platform&gt;.*</code>, but Hermes enforces them from the <strong>top-level</strong> platform section (<code>slack.allowed_channels</code>, <code>telegram.allowed_chats</code>, <code>matrix.allowed_rooms</code>, <code>dingtalk.allowed_chats</code>). The mismatch meant an allowlist you set in Scarf silently never took effect. Scarf now writes to the correct top-level location, merging into the existing platform block so your other settings are preserved. (This also fixes DingTalk, which uses <code>allowed_chats</code>, not <code>allowed_rooms</code>.)</li>
  <li><strong>The gateway digest works again.</strong> <code>hermes gateway list --json</code> doesn&#x27;t exist in v0.16 — the command prints a text table and errors on the flag — so Scarf&#x27;s cross-profile gateway digest was silently hiding. Scarf now runs the plain command and parses the text output (status, profile, PID), so the digest shows up.</li>
</ul>
<h2>v0.16 quick wins</h2>
<ul>
  <li><strong>Optimize sessions database</strong> — a new Health-view action wired to <code>hermes sessions optimize</code> (FTS merge + VACUUM), gated on the host advertising it.</li>
  <li><strong>Kanban goal pill</strong> — cards now decode <code>goal_mode</code> / <code>goal_max_turns</code> and render a <strong>&quot;Goal · N&quot;</strong> pill when a task is running in goal mode.</li>
  <li><strong>Session rename</strong> is gated on the v0.16 <code>hermes sessions rename</code> capability (the rename path Scarf already drove).</li>
</ul>
<h2>Under the hood</h2>
<ul>
  <li><strong>Config-shape corrections, verified against live v0.16.</strong> OpenRouter response-caching is now written as a scalar bool — so toggling it <em>off</em> actually takes effect (the old nested shape wrote a value Hermes always read as &quot;on&quot;). The MCP SSE server-add path was corrected to match the real <code>hermes mcp add</code> flags (URL on add, then <code>transport: sse</code> stamped into the YAML). A dead Kanban <code>verify</code> verb was removed (Reject now routes through the real <code>comment</code> + <code>archive</code> verbs). The <code>curator list-archived</code> text output is parsed directly instead of retrying a non-existent <code>--json</code> flag. ACP <code>/goal</code> and <code>/subgoal</code> were de-advertised from the slash-command surface, since they&#x27;re gateway-only in v0.16 and no-op over ACP.</li>
  <li><strong>New providers and models flow automatically.</strong> AWS Bedrock is registered as an overlay-only provider; the other v0.16 providers self-surface from the model catalog with no Scarf change, and new models (deepseek-v4-flash, MiniMax-M3, qwen3.7-plus, gemini-3.5-flash) appear automatically. The upstream catalog now refreshes hourly.</li>
  <li><strong>New capability flags</strong> — <code>hasSessionsRename</code>, <code>hasSessionsOptimize</code>, <code>hasKanbanGoalMode</code>, <code>hasInsightsCommand</code>, <code>hasDashboardCommand</code>, plus the <code>isV016OrLater</code> predicate — gate every surface above.</li>
  <li><strong>628 ScarfCore tests</strong> pass, including new soft-delete SQL-shape, allowlist sibling-preservation, config round-trip, and gateway text-parser tests.</li>
</ul>
<h2>Upgrade notes</h2>
<ul>
  <li>Sparkle will offer this update automatically on next launch (or <strong>Scarf → Check for Updates</strong>).</li>
  <li>macOS 14.6+ (Sonoma) deployment target unchanged.</li>
  <li><strong>Fully backward compatible.</strong> Every v0.16 surface is capability-gated or schema-detected; if you&#x27;re still on Hermes v0.15.x or earlier, Scarf renders exactly as v2.10.3 did. There are no data migrations.</li>
  <li><strong>iOS testers:</strong> the shared-core changes (the <code>messages.active</code> filter, rewind count, config-shape fixes) ride in ScarfCore; a ScarfGo TestFlight build carrying them is queued separately on the iOS track.</li>
</ul>
</body></html>
      ]]></description>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.11.0/Scarf-v2.11.0-Universal.zip"
                 sparkle:edSignature="X2SNAms7sl5Q12541NT2K9hOGxUVvLZIq/qepP3t2EfyM2ApOKYbpZ2yCPrI64S6rgcd3XCDXgjzZYO2JLMdCw=="
                 length="20593824"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.10.3</title>
      <sparkle:version>42</sparkle:version>
      <sparkle:shortVersionString>2.10.3</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Sun, 14 Jun 2026 00:32:42 +0000</pubDate>
      <description><![CDATA[
<!DOCTYPE html><html><head><meta charset="utf-8"><style>body {
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  font-size: 13px;
  line-height: 1.5;
  color: #1d1d1f;
  margin: 0;
  padding: 0 4px;
}
h2 {
  font-size: 17px;
  margin: 16px 0 6px 0;
  border-bottom: 1px solid #e5e5e7;
  padding-bottom: 3px;
}
h3 {
  font-size: 14px;
  margin: 14px 0 4px 0;
  color: #424245;
}
h4 {
  font-size: 13px;
  font-weight: 600;
  margin: 10px 0 2px 0;
}
p { margin: 6px 0; }
ul { margin: 6px 0; padding-left: 20px; }
li { margin: 3px 0; }
code {
  background: #f5f5f7;
  border-radius: 3px;
  padding: 1px 4px;
  font-family: "SF Mono", Menlo, Consolas, monospace;
  font-size: 12px;
}
pre {
  background: #f5f5f7;
  border-radius: 5px;
  padding: 8px 10px;
  overflow-x: auto;
  font-size: 12px;
}
pre code { background: transparent; padding: 0; }
a { color: #0066cc; text-decoration: none; }
a:hover { text-decoration: underline; }
hr {
  border: none;
  border-top: 1px solid #e5e5e7;
  margin: 16px 0;
}
strong { color: #1d1d1f; }
@media (prefers-color-scheme: dark) {
  body { color: #f5f5f7; background: #1c1c1e; }
  h2 { border-bottom-color: #38383a; }
  h3 { color: #c7c7cc; }
  code, pre { background: #2c2c2e; }
  hr { border-top-color: #38383a; }
  a { color: #4499ff; }
  strong { color: #f5f5f7; }
}
</style></head><body>
<p># Scarf v2.10.3</p>
<p>A reliability and polish release. The headline is a fix for the <strong>100% single-core CPU spin</strong> that hit users with large <code>state.db</code> files, plus a broad performance + correctness sweep from a full code audit. Most of the work is under the hood — every build surface is now warning-clean and the test suite is parallel-safe — but several user-visible paper cuts are fixed too.</p>
<h2>100% CPU on a single core, fixed (gh#102)</h2>
<p>On Macs with a large Hermes history, Scarf could peg one core at 100% indefinitely while the Dashboard was open. The root cause was the Dashboard reloading its SQLite handle on <strong>every</strong> file-change tick: each time Hermes touched <code>state.db</code> / <code>state.db-wal</code>, Scarf would <code>close()</code> + re-<code>open()</code> the database, forcing SQLite to re-walk the WAL page map. On a 285 MB DB with a 114 MB uncheckpointed WAL — the reporter&#x27;s real setup — that close+reopen is pure, compounding cost.</p>
<p>The fix keeps the read-only handle open across loads (a read-only SQLite connection sees Hermes&#x27;s WAL writes transparently, so there&#x27;s nothing to &quot;refresh&quot; in steady state). Close+reopen is now reserved for the schema-migration escape hatch when you upgrade Hermes mid-session. Each file-change tick now runs the four Dashboard queries against the already-open handle in microseconds instead of reopening a 285 MB file.</p>
<p>&gt; If your WAL has grown large, the upstream driver is Hermes (the only process with write access). A one-time <code>sqlite3 ~/.hermes/state.db &quot;PRAGMA wal_checkpoint(TRUNCATE); VACUUM;&quot;</code> while Hermes is stopped will shrink it.</p>
<h2>Menu-bar status stopped flashing every 10 seconds (gh#105)</h2>
<p>Remote-server users saw the menu-bar status chrome flash on a 10-second cadence even when nothing changed. <code>ServerLiveStatus</code> was republishing its <code>@Observable</code> state on every health poll regardless of whether the value actually changed — and SwiftUI invalidates on every setter call, not on real change. A one-line equality guard before each assignment means an unchanging healthy poll no longer re-renders anything; the menu bar stays still through the entire 10s/30s/60s/120s/300s backoff cycle.</p>
<p>(The companion request in gh#105 — a manual &quot;Hermes binary&quot; path field for Docker-fronted hosts — shipped in v2.10.1.)</p>
<h2>Reasoning is visible again on resumed thinking-model chats</h2>
<p>When you reopen a chat with a thinking model, the <strong>REASONING</strong> disclosure now appears and loads the model&#x27;s chain-of-thought on demand — including the newest models that store <em>only</em> the rich <code>reasoning_content</code> (verified against Hermes v0.16&#x27;s source). Previously, the two-phase chat loader deliberately skips the heavy reasoning blob for speed, and for these models the lighter legacy <code>reasoning</code> column is empty — so the disclosure was hidden entirely on resume. Scarf now carries a cheap &quot;reasoning available&quot; signal in the fast path and lazy-loads the full transcript the first time you open the disclosure, so the speed benefit is preserved and the reasoning is one tap away.</p>
<h2>Snappier sidebar navigation</h2>
<p>Switching sidebar sections no longer re-runs each feature&#x27;s remote <code>load()</code> over SSH on every re-entry. Feature panes (Settings, Platforms, Plugins, Quick Commands, Models, MCP Servers, Webhooks, Cron) now keep their loaded data and view state across section switches, refreshing only when the underlying files actually change or you hit Reload. On a remote host that&#x27;s several fewer SSH round-trips every time you bounce between sections.</p>
<h2>Docker config-save errors are now self-diagnostic (gh#112)</h2>
<p>On the iOS app, a failed <code>hermes config set</code> during chat pre-flight used to surface only a generic &quot;Couldn&#x27;t save model+provider to config.yaml.&quot; For Hermes-in-Docker setups (where <code>hermes</code> is a wrapper around <code>docker compose exec</code>), the wrapper&#x27;s actual diagnostic was being swallowed. The failure banner now shows the real exit code and stderr — missing container, missing TTY, PATH miss, etc. — so you can self-diagnose without capturing a separate log.</p>
<h2>More fixes from the audit</h2>
<ul>
  <li><strong>Loading overlays</strong> on the Insights, Sessions, and Logs panes (no more blank-then-pop on slow remotes).</li>
  <li><strong>Standard menu commands</strong>: ⌘, opens Settings, a Help menu links the Hermes docs, and ⌘F focuses the Sessions search.</li>
  <li><strong>Quieter when backgrounded</strong>: the menu-bar live-status poll floors its cadence at 60s while Scarf isn&#x27;t frontmost, killing the idle SSH-poll storm without freezing the always-visible status.</li>
  <li><strong>Cron corruption is surfaced</strong>: a <code>jobs.json</code> that can&#x27;t be parsed now shows a warning instead of a misleading &quot;No cron jobs yet.&quot;</li>
  <li><strong>Chat streaming polish</strong>: block markdown is skipped mid-stream (no half-rendered code fences), and the transcript anchors correctly on session activate.</li>
  <li>A <code>fatalError</code> in the SQL layer became a recoverable <code>throw</code>; per-render formatter allocations were hoisted to cached statics; in-flight remote loads now cancel when you navigate away.</li>
</ul>
<h2>Under the hood</h2>
<ul>
  <li><strong>Warning-clean across the board.</strong> The macOS app, the iOS app, and the ScarfCore package now build with <strong>zero</strong> compiler warnings, including the full Swift 6 strict-concurrency (main-actor isolation, <code>Sendable</code>) cleanup.</li>
  <li><strong>A trustworthy test suite.</strong> 613 tests, parallel-safe, and no longer coupled to your real <code>~/.hermes</code> — ScarfCore tests run against isolated temp homes, so a test run can never touch your live Hermes data.</li>
  <li><strong>Release-signing safeguard.</strong> The release script now preflights the Sparkle EdDSA key against the public key baked into the app, preventing the mis-signed-appcast class of incident.</li>
</ul>
<h2>Upgrade notes</h2>
<ul>
  <li>Sparkle will offer this update automatically on next launch (or <strong>Scarf → Check for Updates</strong>).</li>
  <li>macOS 14.6+ (Sonoma) deployment target unchanged.</li>
  <li><strong>iOS testers:</strong> the gh#112 diagnostic and the reasoning-disclosure fix live in the shared core; a ScarfGo TestFlight build carrying them is queued separately on the iOS track.</li>
</ul>
</body></html>
      ]]></description>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.10.3/Scarf-v2.10.3-Universal.zip"
                 sparkle:edSignature="v7CvWRORo67Mac58DamR4JW3rAopqtHseK/EzjKGhp565rSr9i+bPRxExCJvzfNkyHXPEygYYjAGAS8N6ez3Cg=="
                 length="20617474"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.10.2</title>
      <sparkle:version>41</sparkle:version>
      <sparkle:shortVersionString>2.10.2</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Fri, 05 Jun 2026 21:30:29 +0000</pubDate>
      <description><![CDATA[
<!DOCTYPE html><html><head><meta charset="utf-8"><style>body {
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  font-size: 13px;
  line-height: 1.5;
  color: #1d1d1f;
  margin: 0;
  padding: 0 4px;
}
h2 {
  font-size: 17px;
  margin: 16px 0 6px 0;
  border-bottom: 1px solid #e5e5e7;
  padding-bottom: 3px;
}
h3 {
  font-size: 14px;
  margin: 14px 0 4px 0;
  color: #424245;
}
h4 {
  font-size: 13px;
  font-weight: 600;
  margin: 10px 0 2px 0;
}
p { margin: 6px 0; }
ul { margin: 6px 0; padding-left: 20px; }
li { margin: 3px 0; }
code {
  background: #f5f5f7;
  border-radius: 3px;
  padding: 1px 4px;
  font-family: "SF Mono", Menlo, Consolas, monospace;
  font-size: 12px;
}
pre {
  background: #f5f5f7;
  border-radius: 5px;
  padding: 8px 10px;
  overflow-x: auto;
  font-size: 12px;
}
pre code { background: transparent; padding: 0; }
a { color: #0066cc; text-decoration: none; }
a:hover { text-decoration: underline; }
hr {
  border: none;
  border-top: 1px solid #e5e5e7;
  margin: 16px 0;
}
strong { color: #1d1d1f; }
@media (prefers-color-scheme: dark) {
  body { color: #f5f5f7; background: #1c1c1e; }
  h2 { border-bottom-color: #38383a; }
  h3 { color: #c7c7cc; }
  code, pre { background: #2c2c2e; }
  hr { border-top-color: #38383a; }
  a { color: #4499ff; }
  strong { color: #f5f5f7; }
}
</style></head><body>
<p># Scarf v2.10.2</p>
<p>A small bug-fix release. The headline fix lands a long-running paper cut on remote-host workflows, and the version pin catches Scarf&#x27;s tested-against Hermes range up to the latest patch.</p>
<h2>ACP permission prompts: &quot;Allow Once&quot; / &quot;Allow For Session&quot; actually work now</h2>
<p>When Hermes asked for permission to run a command (typically a <code>sudo</code> or any policy-gated tool), tapping <strong>Allow Once</strong> or <strong>Allow For Session</strong> in the Mac permission sheet — and the same buttons in the ScarfGo iOS sheet — was reaching Hermes as if the user had <strong>cancelled</strong> the prompt. Hermes correctly refused to run the command and printed <em>&quot;blocked from executing&quot;</em>; the user saw a sheet that responded to taps but had no effect downstream.</p>
<p>Root cause was a wire-format mismatch in Scarf&#x27;s response to ACP&#x27;s <code>session/request_permission</code>. The Zed-spec <code>RequestPermissionOutcome</code> is a union keyed on a literal field named <code>outcome</code> with values <code>&quot;selected&quot;</code> and <code>&quot;cancelled&quot;</code>. Scarf was sending <code>{kind: &quot;allowed&quot;|&quot;rejected&quot;}</code> instead — fine-looking JSON, but no spec-compliant discriminator, so Hermes (correctly) fell through to its cancellation default. Every Allow tap rode the same code path as a dismissal.</p>
<p>The fix sends the spec-correct shape: <code>{outcome: {outcome: &quot;selected&quot;, optionId: &quot;&lt;id&gt;&quot;}}</code> for any picked option (Allow Once, Allow For Session, Reject Once, Reject Always — Hermes reads the user&#x27;s intent from the optionId, not the discriminator), and <code>{outcome: {outcome: &quot;cancelled&quot;}}</code> for a true dismissal. The bug affected both Mac and iOS; the report came in from an iOS TestFlight user running sudo against a remote SSH host where prompts are unavoidable.</p>
<p>Coverage tightened to lock the shape: <code>M4ACPIOSTests</code> now decodes the actual response JSON and asserts the discriminator field name + values, so the pre-fix shape can&#x27;t return via substring-passing test.</p>
<h2>Hermes v0.15.2 compatibility</h2>
<p>Scarf&#x27;s verified-compatibility range is extended to <strong>v0.15.2</strong> (released 2026-05-29), the latest patch on the v0.15 Velocity Release line. v0.15.1&#x27;s hotfix wave (dashboard 401 reload fix, Docker <code>--insecure</code> opt-in, MCP bare-command resolution under Docker, Kanban worker SIGTERM, full skills.sh catalog restored, <code>/yolo</code> mid-session, <code>/model</code>/<code>hermes model</code> parity) and v0.15.2&#x27;s packaging fix are transparently back-compat — no Scarf-side gating changes were needed, but the test suite now exercises a v0.15.x parse line and asserts that all 14 v0.15 capability flags stay active across patch releases.</p>
<p>Users running Hermes in Docker should re-read v0.15.1&#x27;s release notes for the <code>HERMES_DASHBOARD_INSECURE=1</code> opt-in — that&#x27;s an explicit Hermes server-side change unrelated to Scarf.</p>
<h2>Upgrade notes</h2>
<ul>
  <li>Sparkle will offer this update automatically on next launch.</li>
  <li>macOS 14.6+ (Sonoma) deployment target unchanged.</li>
  <li>iOS testers: this build is Mac-only. A ScarfGo TestFlight build carrying the same ACP permission fix is queued separately.</li>
</ul>
</body></html>
      ]]></description>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.10.2/Scarf-v2.10.2-Universal.zip"
                 sparkle:edSignature="zqNCjCJ+BcdcBI2CM1meegpJQbtHqVYl4PSxqE6pg5q9PlXMGfcqtRMsLLoQoAWzhDGoyK1dkeSJH/p6jiLYDQ=="
                 length="20563913"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.10.1</title>
      <sparkle:version>40</sparkle:version>
      <sparkle:shortVersionString>2.10.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Thu, 04 Jun 2026 15:11:29 +0000</pubDate>
      <description><![CDATA[
<!DOCTYPE html><html><head><meta charset="utf-8"><style>body {
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  font-size: 13px;
  line-height: 1.5;
  color: #1d1d1f;
  margin: 0;
  padding: 0 4px;
}
h2 {
  font-size: 17px;
  margin: 16px 0 6px 0;
  border-bottom: 1px solid #e5e5e7;
  padding-bottom: 3px;
}
h3 {
  font-size: 14px;
  margin: 14px 0 4px 0;
  color: #424245;
}
h4 {
  font-size: 13px;
  font-weight: 600;
  margin: 10px 0 2px 0;
}
p { margin: 6px 0; }
ul { margin: 6px 0; padding-left: 20px; }
li { margin: 3px 0; }
code {
  background: #f5f5f7;
  border-radius: 3px;
  padding: 1px 4px;
  font-family: "SF Mono", Menlo, Consolas, monospace;
  font-size: 12px;
}
pre {
  background: #f5f5f7;
  border-radius: 5px;
  padding: 8px 10px;
  overflow-x: auto;
  font-size: 12px;
}
pre code { background: transparent; padding: 0; }
a { color: #0066cc; text-decoration: none; }
a:hover { text-decoration: underline; }
hr {
  border: none;
  border-top: 1px solid #e5e5e7;
  margin: 16px 0;
}
strong { color: #1d1d1f; }
@media (prefers-color-scheme: dark) {
  body { color: #f5f5f7; background: #1c1c1e; }
  h2 { border-bottom-color: #38383a; }
  h3 { color: #c7c7cc; }
  code, pre { background: #2c2c2e; }
  hr { border-top-color: #38383a; }
  a { color: #4499ff; }
  strong { color: #f5f5f7; }
}
</style></head><body>
<p># Scarf v2.10.1</p>
<p>A &quot;projects fundamentals&quot; maintenance release built on user feedback after v2.10.0 shipped. Six interlocking fixes that make project work in Scarf feel deterministic again: the slash menu surfaces what you can actually do, the new-project wizard reliably triggers the right skill, the AGENTS.md block teaches the agent what Scarf actually offers, the Skills sidebar finally shows the bundled <code>scarf-template-author</code>, and a new <code>/scarf-*</code> family of slash commands is available in every chat. Plus a Health-view capability diagnostic so the next time something looks sparse, you can tell at a glance whether the version gate is alive.</p>
<h2>Global <code>/scarf-*</code> slash commands</h2>
<p>Six bundled slash commands that drive Scarf-specific project workflows — available in <strong>every chat</strong> (pre-session, global, and project-scoped), not just per-project:</p>
<ul>
  <li><code>/scarf-new &lt;one-liner&gt;</code> — kicks off the <code>scarf-template-author</code> interview to scaffold a new project from scratch.</li>
  <li><code>/scarf-help</code> — concise tour of Scarf&#x27;s feature surface (dashboard widgets, Kanban, model presets, slash commands, cron, etc.).</li>
  <li><code>/scarf-dashboard &lt;change&gt;</code> — design or edit the active project&#x27;s <code>dashboard.json</code>.</li>
  <li><code>/scarf-widget &lt;kind&gt;</code> — add a single widget to the active dashboard.</li>
  <li><code>/scarf-cron &lt;description&gt;</code> — schedule a recurring <code>hermes cron</code> job for the active project.</li>
  <li><code>/scarf-export</code> — prepare + run the <code>.scarftemplate</code> export of the active project.</li>
</ul>
<p>The commands live as <code>.md</code> files at <code>~/.hermes/scarf/slash-commands/</code>. A new <code>SlashCommandBootstrapService</code> copies the bundled set into that directory on app launch, with the same idempotent + version-gated upgrade pattern as <code>SkillBootstrapService</code>: a frontmatter <code>version: x.y.z</code> field is the source of truth, hand-edited copies are preserved, future shipped updates land automatically when their version is newer. Per-project commands of the same name (at <code>&lt;project&gt;/.scarf/slash-commands/</code>) still win — author your own <code>/scarf-help</code> and it overrides the bundled one.</p>
<h2>Skills sidebar: <code>scarf-template-author</code> now appears</h2>
<p>The bundled <code>scarf-template-author</code> skill was being installed at <code>~/.hermes/skills/scarf-template-author/SKILL.md</code> (flat), but Scarf&#x27;s <code>SkillsScanner</code> expects the <code>&lt;category&gt;/&lt;skill&gt;/SKILL.md</code> two-level layout, so the skill never showed in the Skills sidebar — even though Hermes itself loaded it correctly. <code>SkillBootstrapService</code> now installs into <code>~/.hermes/skills/scarf/&lt;skill&gt;/</code> and auto-migrates the existing flat install on first launch. The skill shows up under a new &quot;scarf&quot; category in the Skills view; no user action needed.</p>
<h2>Pre-session slash menu</h2>
<p>Before v2.10.1, opening a fresh app and typing <code>/</code> in the chat input collapsed the menu down to just <code>/new</code> — every session-required command (<code>/clear</code>, <code>/compact</code>, <code>/cost</code>, <code>/model</code>, <code>/tools</code>, <code>/reload-skills</code>, <code>/help</code>, <code>/exit</code>, <code>/yolo</code>, <code>/sessions</code>, <code>/codex-runtime</code>, <code>/steer</code>, <code>/goal</code>, <code>/queue</code>, <code>/subgoal</code>) was filtered out and indistinguishable from &quot;the menu is broken&quot;. Now those commands all stay in the menu, greyed out, with a tooltip that reads <strong>&quot;Available once a chat is open. Press Return on <code>/new</code> (or click an existing session) to start one.&quot;</strong> The pattern matches the existing <code>/steer</code>-on-pre-v0.13-idle grey-out; only the trigger is different.</p>
<h2>New-project wizard hand-off</h2>
<p>The &quot;New Project from Scratch&quot; wizard&#x27;s kickoff prompt was a single polite sentence (<em>&quot;Use the <code>scarf-template-author</code> skill to walk me through configuring it…&quot;</em>) that agents routinely treated as a suggestion rather than an instruction — colder models would reply conversationally without firing the skill. The new prompt anchors on <code>SKILL:</code> and <code>PROJECT_PATH:</code> markers that pattern-trained agents recognize as invocation, lists the skill&#x27;s expected interview stages explicitly, and tells the agent to &quot;Start with question 1.&quot; If the user filled in the optional one-liner, it&#x27;s threaded as the answer to question 1 so the agent jumps straight to question 2.</p>
<p>Also: <code>NewProjectViewModel.commit()</code> now runs <code>SkillBootstrapService.ensureBundledSkillsInstalled()</code> as a synchronous preflight before handing off to chat, so the bundled skill is <strong>guaranteed</strong> on disk before Hermes&#x27;s <code>session/new</code> runs its skill-index scan. The launch-time bootstrap is a detached task that might not have finished yet on cold launch or slow remotes; this makes the wizard self-contained.</p>
<h2>AGENTS.md <code>scarf-project</code> block: Scarf platform reference</h2>
<p>The Scarf-managed block in <code>&lt;project&gt;/AGENTS.md</code> previously surfaced only project bookkeeping (path, dashboard location, template id, config field names, cron jobs, Kanban tenant, uninstall manifest). Agents walked into projects with no idea what Scarf actually offers beyond bare Hermes — the most common failure mode being agents proposing a shell script when a dashboard widget would do the job in one line of JSON.</p>
<p>The block now appends a static &quot;Scarf platform reference&quot; section describing the dashboard widget vocabulary, project slash command authoring (the <code>&lt;!-- scarf-slash:&lt;name&gt; --&gt;</code> expansion marker), the Kanban tenant convention, model presets, the typed configuration schema with Keychain-backed secrets, cron <code>--workdir</code> scoping, where Hermes loads skills from, and the export-to-template flow. The section is idempotent (byte-identical across refreshes), secret-safe (no values appear, only schema field names), and capped around 30 lines so it doesn&#x27;t crowd the agent&#x27;s context.</p>
<p><code>ProjectTemplateInstaller</code> now also refreshes the block on install (previously only chat-start did) so a freshly-installed template project has the block on disk before the user opens its first chat.</p>
<h2>Health view: capabilities diagnostic panel</h2>
<p>The Health view gets a new &quot;Hermes Capabilities&quot; panel at the top of its scroll area showing:</p>
<ul>
  <li>The raw <code>hermes --version</code> line as parsed by Scarf</li>
  <li>A <code>… · N capabilities active</code> summary with per-release flag rows (v0.12 / v0.13 / v0.14 / v0.15) that show green when the connected Hermes is at or above that release</li>
  <li>A <strong>Re-detect</strong> button that re-runs <code>hermes --version</code> on demand</li>
  <li>An explanatory note for when a flag is missing (&quot;UI for that release is hidden because the connected Hermes is older&quot;)</li>
</ul>
<p>The capabilities store also now auto-refreshes when Scarf returns to the foreground (<code>NSApplication.didBecomeActive</code>) — so if you run <code>hermes update</code> in a Terminal while Scarf is backgrounded, the slash menu, Kanban tab, and other version-gated UI pick up the new version without needing a Scarf relaunch.</p>
<h2>Repo memory: Memophant</h2>
<p>Internal note for contributors: as of this release Scarf&#x27;s repo memory (the developer-facing notes, wiki, design system, and CLAUDE.md guidance for AI coding sessions) is managed via <a href="https://github.com/awizemann">Memophant</a>, a memory manager I built for exactly this. Several recent commits on <code>main</code> show as &quot;via Memophant&quot; — those are repo-memory migrations and consolidations, not Scarf app changes. Memophant will be open-sourced shortly; until then the only user-visible artifact is a new managed block at the bottom of <code>CLAUDE.md</code> describing the layout, plus a <code>wiki/</code> working directory and a <code>TASKS.md</code> kanban file. Nothing in Scarf the app depends on Memophant; it&#x27;s a workflow tool for the repo.</p>
<h2>Hermes compatibility</h2>
<p>Unchanged from v2.10.0 — targets Hermes v0.15.0 (v2026.5.28). Pre-v0.15 hosts continue to work; the v2.10.1 changes are all Scarf-side (slash command bundle, skill install layout, AGENTS.md block content, Health panel) and require no Hermes upgrade.</p>
<h2>Upgrade notes</h2>
<ul>
  <li>The Sparkle appcast at <code>https://awizemann.github.io/scarf/appcast.xml</code> will offer this update automatically on next launch.</li>
  <li>macOS 14.6+ (Sonoma) deployment target unchanged.</li>
  <li><strong>One-time migration runs at first launch</strong>: the existing flat <code>~/.hermes/skills/scarf-template-author/</code> install is removed and re-installed under <code>~/.hermes/skills/scarf/scarf-template-author/</code>. Idempotent and non-fatal — if the migration fails (locked file, permissions), the install proceeds and the old flat copy stays where it was (the agent still loads it, and the Skills view will pick up the categorized copy on the next launch).</li>
  <li>No breaking changes to <code>~/.hermes/</code> state. The new <code>~/.hermes/scarf/slash-commands/</code> directory is created lazily on first bootstrap.</li>
</ul>
</body></html>
      ]]></description>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.10.1/Scarf-v2.10.1-Universal.zip"
                 sparkle:edSignature="84/8ugLGwxu3HS3L/mF0N2yknJj555HoAsvN/hUpulZe3zw49GRQqFoQ7l1CbHy63jvlQjoydymFkupZGQt5Cg=="
                 length="20562830"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.10.0</title>
      <sparkle:version>39</sparkle:version>
      <sparkle:shortVersionString>2.10.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Fri, 29 May 2026 04:09:50 +0000</pubDate>
      <description><![CDATA[
<!DOCTYPE html><html><head><meta charset="utf-8"><style>body {
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  font-size: 13px;
  line-height: 1.5;
  color: #1d1d1f;
  margin: 0;
  padding: 0 4px;
}
h2 {
  font-size: 17px;
  margin: 16px 0 6px 0;
  border-bottom: 1px solid #e5e5e7;
  padding-bottom: 3px;
}
h3 {
  font-size: 14px;
  margin: 14px 0 4px 0;
  color: #424245;
}
h4 {
  font-size: 13px;
  font-weight: 600;
  margin: 10px 0 2px 0;
}
p { margin: 6px 0; }
ul { margin: 6px 0; padding-left: 20px; }
li { margin: 3px 0; }
code {
  background: #f5f5f7;
  border-radius: 3px;
  padding: 1px 4px;
  font-family: "SF Mono", Menlo, Consolas, monospace;
  font-size: 12px;
}
pre {
  background: #f5f5f7;
  border-radius: 5px;
  padding: 8px 10px;
  overflow-x: auto;
  font-size: 12px;
}
pre code { background: transparent; padding: 0; }
a { color: #0066cc; text-decoration: none; }
a:hover { text-decoration: underline; }
hr {
  border: none;
  border-top: 1px solid #e5e5e7;
  margin: 16px 0;
}
strong { color: #1d1d1f; }
@media (prefers-color-scheme: dark) {
  body { color: #f5f5f7; background: #1c1c1e; }
  h2 { border-bottom-color: #38383a; }
  h3 { color: #c7c7cc; }
  code, pre { background: #2c2c2e; }
  hr { border-top-color: #38383a; }
  a { color: #4499ff; }
  strong { color: #f5f5f7; }
}
</style></head><body>
<p># Scarf v2.10.0</p>
<p>Hermes v0.15.0 (v2026.5.28) — &quot;The Velocity Release&quot; — catch-up. Adopts the GUI-relevant surface of v0.15 across providers, web search, messaging platforms, voice, the 104-PR Kanban maturation wave, and five larger surfaces (Bitwarden secrets, supply-chain audit, MCP mTLS, skill bundles, per-session ACP edit-approval modes). Every new affordance is capability-gated on <code>HermesCapabilities</code> v0.15 flags — pre-v0.15 hosts render the v2.9.x surface unchanged. All flag/config/wire shapes were verified against the <code>v2026.5.28</code> Hermes source before implementation.</p>
<h2>Providers &amp; models</h2>
<ul>
  <li><strong>OpenAI API as a first-class provider</strong> — wire ID <code>openai-api</code>, distinct from the existing OpenAI Codex runtime. Surfaces in the Models picker. (Bare <code>openai</code> stays a Hermes alias to OpenRouter, so it is intentionally not registered.)</li>
  <li><strong>Krea image generation</strong> — <code>krea-2-medium</code> ($0.03) and <code>krea-2-large</code> ($0.06) added to the image-generation model list (Settings → Auxiliary).</li>
  <li><strong>xAI May-15 model retirement</strong> — retired Grok IDs (<code>grok-4-0709</code>, <code>grok-4-fast-<em></code>, <code>grok-4-1-fast-</em></code>, <code>grok-code-fast-1</code>, <code>grok-3</code>) resolve to <code>grok-4.3</code> in the picker, and <code>grok-imagine-image-pro</code> → <code>grok-imagine-image-quality</code>, so a stored retired model still works. The Health view warns when your configured model is retired and offers one-click <code>hermes migrate xai</code>.</li>
  <li><strong>Vercel removed</strong> — Vercel AI Gateway (provider) and Vercel Sandbox (terminal backend) were deleted from Hermes in v0.15 and are dropped from Scarf&#x27;s picker and terminal-backend list.</li>
</ul>
<h2>Web Tools</h2>
<ul>
  <li><strong>xAI Web Search</strong> — new <code>web_tools.search.backend</code> option (<code>xai</code>), reusing your Grok OAuth / <code>XAI_API_KEY</code> credentials with no new env var.</li>
</ul>
<h2>Platforms</h2>
<ul>
  <li><strong>ntfy</strong> — 23rd gateway platform. Push notifications via a topic URL, no account required. New setup form (topic, server, optional publish-topic / token / markdown) under the Platforms tab.</li>
  <li><strong>Per-platform config</strong> — Telegram <code>disable_topic_auto_rename</code> + <code>ignore_root_dm</code>, Discord <code>allow_any_attachment</code>, Signal group-only <code>require_mention</code>, surfaced in each platform&#x27;s setup form.</li>
</ul>
<h2>Voice</h2>
<ul>
  <li><strong>xAI TTS <code>auto_speech_tags</code></strong> — opt-in toggle (default off) that inserts light <code>[pause]</code> tags for more natural xAI voice replies.</li>
</ul>
<h2>Kanban — the v0.15 maturation wave</h2>
<ul>
  <li><strong>Server-side sort</strong> — a sort picker on the board header (<code>--sort</code> by priority, created, status, assignee, title, updated).</li>
  <li><strong>New lifecycle actions</strong> — Promote (<code>todo</code>/<code>blocked</code> → <code>ready</code>), Schedule / Park, and Delete-permanently (hard-delete archived tasks via <code>archive --rm</code>) as card context actions.</li>
  <li><strong>New columns</strong> — Scheduled (parked tasks) and Review (post-work verification), collapsed when empty.</li>
  <li><strong>Worktree + model surfacing</strong> — per-task worktree <code>--branch</code> on create; the inspector shows a task&#x27;s branch and (read-only) model override.</li>
  <li><strong>Precise chat-scoped board</strong> — the chat-header Kanban chip and the board it opens now filter by the originating ACP <code>session_id</code> (stamped automatically by Hermes), replacing the old tenant + time-window approximation. A &quot;This chat ⇄ All tasks&quot; scope toggle lets you widen the view.</li>
  <li>Multi-board <code>--board</code> plumbing is wired through the service layer (a board switcher UI and the <code>swarm</code> create-sheet are follow-ups).</li>
</ul>
<h2>Secrets</h2>
<ul>
  <li><strong>Bitwarden Secrets Manager</strong> — new Settings → Secrets tab for the <code>secrets.bitwarden</code> block: enable it, point Hermes at a bootstrap token (env var, default <code>BWS_ACCESS_TOKEN</code>), set project ID / server URL (US / EU / self-hosted) / cache TTL / auto-install / override-existing. The bootstrap token itself lives in <code>~/.hermes/.env</code>, not in this form.</li>
</ul>
<h2>MCP</h2>
<ul>
  <li><strong>mTLS client certificates</strong> — client cert / key paths + an SSL-verify control (with optional custom CA bundle) for HTTP and SSE MCP servers, in the server editor.</li>
  <li><strong>Nous MCP catalog</strong> — a read-only &quot;Browse catalog&quot; sheet showing <code>hermes mcp catalog</code> output.</li>
</ul>
<h2>Skills</h2>
<ul>
  <li><strong>Skill bundles</strong> — a read-only Bundles tab listing the named skill groups in <code>~/.hermes/skill-bundles/*.yaml</code> (each loadable in Hermes via one <code>/&lt;name&gt;</code> slash command), with each bundle&#x27;s member skills and instruction.</li>
</ul>
<h2>Chat</h2>
<ul>
  <li><strong>Per-session edit-approval modes</strong> — a chat-header chip to switch a live session between Default (ask before edits), Accept Edits (auto-allow workspace + /tmp edits), and Don&#x27;t Ask (auto-allow except sensitive paths) via ACP <code>session/set_mode</code>. Separate from the global <code>approvals.mode</code> / YOLO surface; sensitive paths always still prompt.</li>
</ul>
<h2>Health</h2>
<ul>
  <li><strong>Supply-chain audit</strong> — a &quot;Run supply-chain audit&quot; button that runs <code>hermes audit</code> (OSV.dev) and shows the result inline.</li>
</ul>
<h2>Fixes &amp; polish (from the pre-release review)</h2>
<ul>
  <li><strong>Kanban session scope (correctness)</strong> — the chat-scoped board no longer drops session tasks the agent created without tagging the project tenant; the precise <code>--session</code> filter is now authoritative.</li>
  <li><strong>Kanban scope pill</strong> — shown for global chats too, so a session-scoped board with no project tenant isn&#x27;t locked to the session filter.</li>
  <li><strong>MCP SSL-verify</strong> — the verify toggle and the CA-bundle path are independent controls; toggling verification off no longer wipes a typed CA path.</li>
  <li>Removed dead code left behind when the Kanban time-window heuristic was replaced by session scoping.</li>
</ul>
<h2>Hermes compatibility</h2>
<p>Targets Hermes v0.15.0 (v2026.5.28). Pre-v0.15 hosts continue to work — every v0.15 surface is capability-gated on <code>HermesCapabilities.has*</code> flags so older Hermes installs gracefully hide the new affordances. The bulk of v0.15 (the <code>run_agent.py</code> refactor, cold-start performance, promptware defense, <code>session_search</code> rebuild, the Ink TUI orchestrator, the web dashboard, Docker s6 supervision, the API-server session-control REST) is server-side or other-frontend and benefits Scarf transparently with no Scarf change.</p>
<h2>Upgrade notes</h2>
<ul>
  <li>The Sparkle appcast at <code>https://awizemann.github.io/scarf/appcast.xml</code> will offer this update automatically on next launch.</li>
  <li>macOS 14.6+ (Sonoma) deployment target unchanged.</li>
  <li>No breaking changes to <code>~/.hermes/</code> state. New config keys (<code>secrets.bitwarden.<em></code>, <code>platforms.ntfy.</em></code>, <code>tts.xai.auto_speech_tags</code>, MCP <code>client_cert</code>/<code>client_key</code>/<code>ssl_verify</code>, the per-platform flags) are written only when you set them.</li>
</ul>
</body></html>
      ]]></description>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.10.0/Scarf-v2.10.0-Universal.zip"
                 sparkle:edSignature="T6oMmbOGUwTHRyU2a+08cLelf3n3sHy01boIM7O/AjOqpbvK7N9MQj3x6GywcEi42ZZnnf+D1sPDJ63uXlHZDA=="
                 length="20475606"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.9.2</title>
      <sparkle:version>38</sparkle:version>
      <sparkle:shortVersionString>2.9.2</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Thu, 28 May 2026 19:13:00 +0000</pubDate>
      <description><![CDATA[
<!DOCTYPE html><html><head><meta charset="utf-8"><style>body {
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  font-size: 13px;
  line-height: 1.5;
  color: #1d1d1f;
  margin: 0;
  padding: 0 4px;
}
h2 {
  font-size: 17px;
  margin: 16px 0 6px 0;
  border-bottom: 1px solid #e5e5e7;
  padding-bottom: 3px;
}
h3 {
  font-size: 14px;
  margin: 14px 0 4px 0;
  color: #424245;
}
h4 {
  font-size: 13px;
  font-weight: 600;
  margin: 10px 0 2px 0;
}
p { margin: 6px 0; }
ul { margin: 6px 0; padding-left: 20px; }
li { margin: 3px 0; }
code {
  background: #f5f5f7;
  border-radius: 3px;
  padding: 1px 4px;
  font-family: "SF Mono", Menlo, Consolas, monospace;
  font-size: 12px;
}
pre {
  background: #f5f5f7;
  border-radius: 5px;
  padding: 8px 10px;
  overflow-x: auto;
  font-size: 12px;
}
pre code { background: transparent; padding: 0; }
a { color: #0066cc; text-decoration: none; }
a:hover { text-decoration: underline; }
hr {
  border: none;
  border-top: 1px solid #e5e5e7;
  margin: 16px 0;
}
strong { color: #1d1d1f; }
@media (prefers-color-scheme: dark) {
  body { color: #f5f5f7; background: #1c1c1e; }
  h2 { border-bottom-color: #38383a; }
  h3 { color: #c7c7cc; }
  code, pre { background: #2c2c2e; }
  hr { border-top-color: #38383a; }
  a { color: #4499ff; }
  strong { color: #f5f5f7; }
}
</style></head><body>
<p># Scarf v2.9.2</p>
<p>Patch release fixing two reported bugs in remote chat and session resume.</p>
<h2>Fixes</h2>
<h3>Remote Chat no longer blocks on a bare <code>hermes</code> binary (<a href="https://github.com/awizemann/scarf/issues/100">#100</a>)</h3>
<p>Remote servers where <code>hermes</code> resolves on the login <code>PATH</code> (e.g. a pipx install at <code>~/.local/bin/hermes</code>) but isn&#x27;t pinned to an explicit binary path showed <strong>&quot;Hermes Not Found — Expected at hermes&quot;</strong> in Chat, even though Remote Diagnostics passed 14/14 and the iOS app worked against the same server.</p>
<p>The pre-flight gate ran <code>fileExists(&quot;hermes&quot;)</code>, which on a remote context executes <code>test -e hermes</code> — a filesystem check against the remote working directory, not a <code>PATH</code> lookup. A bare command name always failed that check, blocking the chat UI, even though the actual ACP launch (<code>bash -lc</code>, a login shell) resolves <code>hermes</code> correctly.</p>
<p><code>ServerContext.hermesBinaryProbablyResolvable()</code> now distinguishes the two cases: a <strong>path-shaped</strong> binary (absolute/relative, contains <code>/</code>) still gets the accurate <code>fileExists</code> check, while a <strong>bare command name</strong> is presumed resolvable and the authoritative check is deferred to the login-shell ACP launch — whose failure path already surfaces a &quot;command not found&quot; hint if <code>hermes</code> is genuinely absent. Remote chat now opens without needing a <code>~/hermes</code> symlink workaround.</p>
<h3>Session resume no longer silently drops context (<a href="https://github.com/awizemann/scarf/issues/99">#99</a>)</h3>
<p>Reopening a chat could spawn a brand-new Hermes session — losing conversation history, task state, and loaded skills — with no error shown.</p>
<p><code>ACPClient.loadSession</code> assumed any non-throwing response meant the session loaded. But Hermes&#x27;s <code>load_session</code> returns a JSON-RPC <strong><code>result: null</code></strong> (not an error) when a session can&#x27;t be restored into the ACP runtime. Scarf treated that null as success and proceeded against a phantom session id, so prompts landed in fresh context.</p>
<p><code>loadSession</code> now throws on a null/non-dict result, routing into the existing fallback that creates a fresh session <strong>and replays the conversation transcript from <code>state.db</code></strong> so the thread reads continuously. Restorable sessions resume correctly as before, keeping full context (history, task state, skills) — the fix is specifically the previously-silent not-restorable path.</p>
<h2>Tests</h2>
<p>3 new ScarfCore tests: <code>loadSession</code> throws on a null result + succeeds on a dict result (mock round-trip), and <code>hermesBinaryProbablyResolvable</code> returns true for a bare remote name without a transport round-trip.</p>
<h2>Hermes compatibility</h2>
<p>Targets Hermes v0.14.0 (v2026.5.16); works against v0.13+ with capability gating. No <code>~/.hermes/</code> schema changes.</p>
</body></html>
      ]]></description>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.9.2/Scarf-v2.9.2-Universal.zip"
                 sparkle:edSignature="RfUUTsmkvWw/SpOg9iKGuRueIpOyiKIn/maKzMedz6/PCk8ePzUOUC8Hzlqyf3fdKHQpk26lJrutjJjSxhSTDg=="
                 length="20269596"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.9.1</title>
      <sparkle:version>37</sparkle:version>
      <sparkle:shortVersionString>2.9.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Sun, 17 May 2026 20:39:50 +0000</pubDate>
      <description><![CDATA[
<!DOCTYPE html><html><head><meta charset="utf-8"><style>body {
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  font-size: 13px;
  line-height: 1.5;
  color: #1d1d1f;
  margin: 0;
  padding: 0 4px;
}
h2 {
  font-size: 17px;
  margin: 16px 0 6px 0;
  border-bottom: 1px solid #e5e5e7;
  padding-bottom: 3px;
}
h3 {
  font-size: 14px;
  margin: 14px 0 4px 0;
  color: #424245;
}
h4 {
  font-size: 13px;
  font-weight: 600;
  margin: 10px 0 2px 0;
}
p { margin: 6px 0; }
ul { margin: 6px 0; padding-left: 20px; }
li { margin: 3px 0; }
code {
  background: #f5f5f7;
  border-radius: 3px;
  padding: 1px 4px;
  font-family: "SF Mono", Menlo, Consolas, monospace;
  font-size: 12px;
}
pre {
  background: #f5f5f7;
  border-radius: 5px;
  padding: 8px 10px;
  overflow-x: auto;
  font-size: 12px;
}
pre code { background: transparent; padding: 0; }
a { color: #0066cc; text-decoration: none; }
a:hover { text-decoration: underline; }
hr {
  border: none;
  border-top: 1px solid #e5e5e7;
  margin: 16px 0;
}
strong { color: #1d1d1f; }
@media (prefers-color-scheme: dark) {
  body { color: #f5f5f7; background: #1c1c1e; }
  h2 { border-bottom-color: #38383a; }
  h3 { color: #c7c7cc; }
  code, pre { background: #2c2c2e; }
  hr { border-top-color: #38383a; }
  a { color: #4499ff; }
  strong { color: #f5f5f7; }
}
</style></head><body>
<p># Scarf v2.9.1</p>
<p>Patch release covering <a href="https://github.com/awizemann/scarf/issues/97">#97</a> — the v2.9.0 mid-chat model switcher and per-project model preset binding both fired <code>session/set_model</code> with the bare model ID, never the colon-prefixed <code>&lt;provider&gt;:&lt;model&gt;</code> wire format Hermes&#x27;s ACP adapter expects. For model IDs Hermes can auto-detect from the name alone (e.g. <code>claude-opus-4-7</code>), this worked. For less-obvious IDs (e.g. OpenRouter&#x27;s <code>&lt;author&gt;/&lt;model&gt;</code> slugs like <code>inclusionai/ring-2.6-1t</code>), Hermes&#x27;s <code>_resolve_model_selection</code> would fall through to <code>detect_provider_for_model</code> and infer the wrong provider — the session ended up routing through whatever provider was previously active, and the user got an opaque rejection from the wrong upstream.</p>
<h2>Fix</h2>
<ul>
  <li><strong><code>ACPClient.setSessionModel</code></strong> gains an optional <code>providerID:</code> parameter. When supplied, the wire <code>modelId</code> is encoded as <code>&quot;&lt;provider&gt;:&lt;model&gt;&quot;</code> (matching <code>acp_adapter/server.py</code>&#x27;s <code>_encode_model_choice</code>). When <code>nil</code> or empty, falls back to the bare-model wire shape so existing call sites that genuinely don&#x27;t know the provider keep working.</li>
  <li><strong><code>ChatViewModel.switchModelPreset</code></strong> and <strong><code>ChatViewModel.applyProjectModelPreset</code></strong> now pass <code>preset.providerID</code> through. The &quot;Use global default&quot; mid-chat path additionally reads <code>config.provider</code> from <code>config.yaml</code> so reverting to the global default lands on the same provider the CLI would.</li>
  <li>New public <code>ACPClient.encodeModelChoice(modelID:providerID:)</code> static helper exposes the encoding logic for tests + any future call sites that need the wire form without firing the RPC.</li>
  <li>6 new tests in <code>M1ACPTests</code> cover the encoding rules: bare model when provider absent / empty / whitespace, colon-prefix when both present, provider lower-casing (matches Hermes), whitespace trimming, empty-model no-op semantics.</li>
</ul>
<h2>Not affected</h2>
<ul>
  <li>The <code>/model &lt;X&gt;</code> slash command typed in Scarf chat — Scarf passes the literal text through; the parsing bug lives entirely in Hermes&#x27;s ACP <code>_cmd_model</code> handler (per the RCA in <a href="https://github.com/awizemann/scarf/issues/97">#97</a>). Workaround: use the new chat-header model badge in v2.9 instead, which now resolves correctly via this fix.</li>
  <li>Pre-v0.13 hosts — no <code>session/set_model</code> RPC; preset apply silently no-ops as before.</li>
  <li>Model presets saved before v2.9.0 with empty <code>providerID</code> — fall back to the bare-model wire shape, identical to v2.9.0 behavior. Re-save the preset to opt into colon-encoding.</li>
</ul>
<h2>Hermes compatibility</h2>
<p>Targets Hermes v0.14.0 (v2026.5.16). The fix is forward-compatible — Hermes&#x27;s <code>_resolve_model_selection</code> accepts both the colon-prefixed and bare-model wire shapes on every release of v0.13+, so v2.9.1 works against v0.13 hosts too.</p>
</body></html>
      ]]></description>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.9.1/Scarf-v2.9.1-Universal.zip"
                 sparkle:edSignature="XMZTBWEnqLh33HzXcFyFk0KobS7SLYTUJyLZ6qPhNsmUU9xHCqWYB5sYA0VGwL+wrZ9wdajwYlHViOlCAGAJBw=="
                 length="20266847"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.9.0</title>
      <sparkle:version>36</sparkle:version>
      <sparkle:shortVersionString>2.9.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Sat, 16 May 2026 15:49:06 +0000</pubDate>
      <description><![CDATA[
<!DOCTYPE html><html><head><meta charset="utf-8"><style>body {
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  font-size: 13px;
  line-height: 1.5;
  color: #1d1d1f;
  margin: 0;
  padding: 0 4px;
}
h2 {
  font-size: 17px;
  margin: 16px 0 6px 0;
  border-bottom: 1px solid #e5e5e7;
  padding-bottom: 3px;
}
h3 {
  font-size: 14px;
  margin: 14px 0 4px 0;
  color: #424245;
}
h4 {
  font-size: 13px;
  font-weight: 600;
  margin: 10px 0 2px 0;
}
p { margin: 6px 0; }
ul { margin: 6px 0; padding-left: 20px; }
li { margin: 3px 0; }
code {
  background: #f5f5f7;
  border-radius: 3px;
  padding: 1px 4px;
  font-family: "SF Mono", Menlo, Consolas, monospace;
  font-size: 12px;
}
pre {
  background: #f5f5f7;
  border-radius: 5px;
  padding: 8px 10px;
  overflow-x: auto;
  font-size: 12px;
}
pre code { background: transparent; padding: 0; }
a { color: #0066cc; text-decoration: none; }
a:hover { text-decoration: underline; }
hr {
  border: none;
  border-top: 1px solid #e5e5e7;
  margin: 16px 0;
}
strong { color: #1d1d1f; }
@media (prefers-color-scheme: dark) {
  body { color: #f5f5f7; background: #1c1c1e; }
  h2 { border-bottom-color: #38383a; }
  h3 { color: #c7c7cc; }
  code, pre { background: #2c2c2e; }
  hr { border-top-color: #38383a; }
  a { color: #4499ff; }
  strong { color: #f5f5f7; }
}
</style></head><body>
<p># Scarf v2.9.0</p>
<p>Hermes v0.14.0 (v2026.5.16) — &quot;Foundation Release&quot; — catch-up. Adds support for the new ACP slash commands, two new providers, two new gateway platforms, two new web-search backends, the local OpenAI-compatible proxy, ACP browser-tools setup, the per-turn file-mutation verifier surface, a YOLO mode warning, and a long tail of settings and config additions. Bundles 9 pending fixes and feature commits that landed since v2.8.0.</p>
<h2>Hermes v0.14 catch-up</h2>
<h3>Chat</h3>
<ul>
  <li><strong><code>/subgoal</code> slash command</strong> — append extra success criteria to an active <code>/goal</code> loop. Surfaced as a trailing line on the goal pill. Forms: <code>&lt;text&gt;</code>, <code>remove N</code>, <code>clear</code>.</li>
  <li><strong><code>/yolo</code> slash command</strong> — toggle YOLO mode (skip dangerous-command approvals) without leaving chat. Pairs with a new chat-header warning badge when YOLO is on.</li>
  <li><strong><code>/sessions</code> slash command</strong> — browse and resume prior sessions from inside an active chat.</li>
  <li><strong><code>/codex-runtime</code> slash command</strong> — toggle the Codex app-server runtime for OpenAI/Codex models.</li>
  <li><strong>Per-turn file-mutation verifier</strong> — v0.14 Hermes appends a verifier footer to assistant turns that wrote files (configurable via <code>file_mutation_verifier</code>). The footer flows through Scarf chat as plain agent output; bespoke card-style rendering is deferred until the upstream format stabilizes.</li>
  <li><strong>YOLO mode warning banner</strong> — chat-header badge when <code>agent.approval_mode = yolo</code>. Mirrors the warning Hermes itself surfaces in its banner.</li>
</ul>
<h3>Providers</h3>
<ul>
  <li><strong>xAI Grok OAuth (SuperGrok)</strong> — sign in with your xAI account, talk to Grok models from Scarf. Subscription-gated; surfaces in the Models picker with a &quot;Subscription&quot; pill.</li>
  <li><strong>NovitaAI</strong> — new API-key inference provider.</li>
  <li><strong>Alibaba Cloud → Qwen Cloud</strong> — display rename to match Hermes&#x27;s picker. Provider wire ID stays <code>alibaba</code> so existing config keys keep working.</li>
</ul>
<h3>Platforms</h3>
<ul>
  <li><strong>LINE Messaging API</strong> — 21st gateway platform.</li>
  <li><strong>SimpleX Chat</strong> — 22nd gateway platform. Requires a local <code>simplex-chat</code> daemon running in WebSocket mode (setup instructions in the Discovery panel).</li>
</ul>
<h3>Web Tools</h3>
<ul>
  <li><strong>Brave Search (free tier)</strong> + <strong>DuckDuckGo (DDGS)</strong> — two new web-search backends. Brave honors a <code>BRAVE_SEARCH_API_KEY</code> env var for higher quotas; DDGS works anonymously.</li>
</ul>
<h3>MCP, plugins, and config</h3>
<ul>
  <li><strong>MCP <code>supports_parallel_tool_calls</code></strong> — new optional flag on MCP server entries; the agent batches concurrent tool calls instead of serializing.</li>
  <li><strong>OpenRouter <code>min_coding_score</code></strong> — knob for the Pareto Code router (<code>openrouter/pareto-code</code>). Routes to the cheapest model meeting your quality bar (0.0–1.0, default 0.65).</li>
  <li><strong>Custom provider <code>api_mode</code></strong> — explicit wire-protocol selection (<code>chat_completions</code> / <code>anthropic_messages</code> / <code>codex_responses</code>).</li>
  <li><strong>Plugin <code>tool_override</code> badge</strong> — plugins that replace a built-in tool now show a badge in PluginsView.</li>
  <li><strong><code>docker_extra_args</code></strong> — extra flags passed verbatim to <code>docker run</code> for the docker-backed terminal backend.</li>
  <li><strong><code>display.timestamps</code></strong> — per-message timestamps toggle.</li>
  <li><strong>Cron <code>deliver=all</code></strong> — fan-out cron job delivery to every connected channel.</li>
  <li><strong>Discord channel-history backfill</strong> — toggle the v0.14 default-on backfill behavior per profile.</li>
</ul>
<h3>New feature surfaces</h3>
<ul>
  <li><strong>Hermes Proxy</strong> — new sidebar destination under Configure. Runs <code>hermes proxy start</code> with the user&#x27;s OAuth-authenticated upstream (Nous Portal in v0.14, more adapters coming). Surfaces the endpoint URL (<code>http://127.0.0.1:8645/v1</code>), routed-provider status, and a tail of the startup log. Use it to point Codex / Aider / Cline / VS Code Continue at your Hermes subscription.</li>
  <li><strong>ACP browser tools setup</strong> — Health view gains a &quot;Run setup&quot; button that calls <code>hermes acp --setup-browser</code> to install Chromium and provision Playwright in one shot.</li>
</ul>
<h2>Performance + reliability fixes (post-v2.8.0)</h2>
<ul>
  <li><strong>Per-project model presets + mid-chat switcher</strong> — save named model+provider presets, bind one to a project, or switch mid-chat via a popover from the chat header.</li>
  <li><strong>Kanban toolset enable</strong> — direct YAML write with detector-based verification (the CLI flow had a regression that didn&#x27;t persist correctly).</li>
  <li><strong>MetricKit crash + hang diagnostics</strong> — Scarf-iOS persists MetricKit reports across launches so post-mortems work after a TestFlight crash.</li>
  <li><strong>JSON read caps</strong> — session-attribution and project-dashboard JSON reads now bound their input to defend against pathological files.</li>
  <li><strong>Scroll crash + background-lifecycle hardening</strong> — eliminates a scroll-view crash and tightens behavior when the app suspends mid-chat.</li>
  <li><strong>Selectable text across paragraphs</strong> in chat transcript (#93).</li>
  <li><strong>Process pipe draining</strong> for large Kanban outputs (#95).</li>
  <li><strong>Transcript render perf</strong> — chat transcript scrolls faster on long conversations, plus a cron-filter and two-stage load-earlier.</li>
  <li><strong>iOS slash command parity</strong> — iOS chat now sees the same slash menu as Mac, plus four TestFlight-reported fixes.</li>
</ul>
<h2>Hermes compatibility</h2>
<p>Targets Hermes v0.14.0 (v2026.5.16). Pre-v0.14 hosts continue to work — every v0.14 surface is capability-gated on <code>HermesCapabilities.has*</code> flags so older Hermes installs gracefully hide new affordances rather than throwing.</p>
<p>Note: the new <code>/handoff</code> slash command in Hermes v0.14 is messaging-platform handoff (CLI-only). Mid-chat model switching in Scarf continues to use the existing <code>session/set_model</code> RPC path via the chat-header model badge.</p>
<h2>Upgrade notes</h2>
<ul>
  <li>The Sparkle appcast at <code>https://awizemann.github.io/scarf/appcast.xml</code> will offer this update automatically on next launch.</li>
  <li>macOS 14.6+ (Sonoma) deployment target unchanged.</li>
  <li>No breaking changes to <code>~/.hermes/</code> state. The Scarf sidecar at <code>~/.hermes/scarf/</code> gains a new optional file for per-project Hermes proxy bindings (created lazily on first use).</li>
</ul>
</body></html>
      ]]></description>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.9.0/Scarf-v2.9.0-Universal.zip"
                 sparkle:edSignature="bqPABt315aHEi0BPMYalZzN7JyM/xncRydCZPWgPEu0dSphcBQW328neKugOkO9hnimCQ6mcY0nkq6OLM2gdDg=="
                 length="20265172"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.8.0</title>
      <sparkle:version>35</sparkle:version>
      <sparkle:shortVersionString>2.8.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Sat, 09 May 2026 19:02:51 +0000</pubDate>
      <description><![CDATA[
<!DOCTYPE html><html><head><meta charset="utf-8"><style>body {
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  font-size: 13px;
  line-height: 1.5;
  color: #1d1d1f;
  margin: 0;
  padding: 0 4px;
}
h2 {
  font-size: 17px;
  margin: 16px 0 6px 0;
  border-bottom: 1px solid #e5e5e7;
  padding-bottom: 3px;
}
h3 {
  font-size: 14px;
  margin: 14px 0 4px 0;
  color: #424245;
}
h4 {
  font-size: 13px;
  font-weight: 600;
  margin: 10px 0 2px 0;
}
p { margin: 6px 0; }
ul { margin: 6px 0; padding-left: 20px; }
li { margin: 3px 0; }
code {
  background: #f5f5f7;
  border-radius: 3px;
  padding: 1px 4px;
  font-family: "SF Mono", Menlo, Consolas, monospace;
  font-size: 12px;
}
pre {
  background: #f5f5f7;
  border-radius: 5px;
  padding: 8px 10px;
  overflow-x: auto;
  font-size: 12px;
}
pre code { background: transparent; padding: 0; }
a { color: #0066cc; text-decoration: none; }
a:hover { text-decoration: underline; }
hr {
  border: none;
  border-top: 1px solid #e5e5e7;
  margin: 16px 0;
}
strong { color: #1d1d1f; }
@media (prefers-color-scheme: dark) {
  body { color: #f5f5f7; background: #1c1c1e; }
  h2 { border-bottom-color: #38383a; }
  h3 { color: #c7c7cc; }
  code, pre { background: #2c2c2e; }
  hr { border-top-color: #38383a; }
  a { color: #4499ff; }
  strong { color: #f5f5f7; }
}
</style></head><body>
<h2>What&#x27;s in 2.8.0</h2>
<p>A coordinated catch-up release adopting Hermes v0.13.0 (v2026.5.7) — &quot;The Tenacity Release&quot; — across Scarf&#x27;s full surface area. v2.8.0 ships <strong>Persistent Goals</strong>, <strong>ACP <code>/queue</code></strong>, <strong>Kanban diagnostics + recovery UX</strong>, <strong>Curator archive/prune</strong>, <strong>Google Chat (20th platform) + cross-platform allowlists</strong>, a refreshed <strong>provider catalog</strong> with five new models, and a slate of settings + UX polish — all behind capability flags so pre-v0.13 hosts continue to render the v2.7.5 surface unchanged.</p>
<p>No data migrations, no schema changes. <code>~/.hermes/state.db</code> columns are unchanged from v0.11/v0.12. Existing <code>~/.hermes/scarf/</code> sidecars are untouched. Sparkle picks the update up automatically.</p>
<h3>New features</h3>
<h4>Persistent Goals + ACP <code>/queue</code> (chat)</h4>
<ul>
  <li><strong><code>/goal &lt;text&gt;</code> slash command</strong> — locks the agent on a target that persists across turns. Surfaced via the chat slash menu (gated on <code>HermesCapabilities.hasGoals</code>) and rendered as an <code>info</code>-tinted &quot;Goal locked: …&quot; pill in the chat header. The pill exposes a &quot;Clear goal&quot; context-menu item that dispatches <code>/goal --clear</code>. Optimistic local mirror — Hermes is the authoritative owner; Scarf paints the pill the moment the user sends <code>/goal …</code> so the affordance feels instant.</li>
  <li><strong><code>/queue &lt;text&gt;</code> slash command</strong> — queues a prompt to run after the current turn completes. Joins <code>/steer</code> and <code>/goal</code> in <code>RichChatViewModel.nonInterruptiveCommands</code> (the chat keeps &quot;Agent working…&quot; off when sent). A header chip shows the queued count; tap opens a popover listing prompts + relative timestamps. Per-entry deletion isn&#x27;t exposed (Hermes has no remove-by-id verb), and the popover header makes that explicit so users understand the local mirror&#x27;s role.</li>
  <li><strong><code>/steer</code> on idle</strong> — pre-v0.13 was a no-op when no turn was in flight; v0.13 runs it as a regular prompt. The composer&#x27;s slash button now greys <code>/steer</code> only on pre-v0.13 hosts (gated on <code>hasACPSteerOnIdle</code>).</li>
  <li><strong>Static slash-menu fallbacks</strong> — pre-session, the menu surfaces <code>/new</code> (with optional <code>[&lt;name&gt;]</code> argument hint on v0.13). Active-session-only fallbacks (<code>/clear</code>, <code>/compact</code>, <code>/cost</code>, <code>/model</code>, <code>/tools</code>, <code>/reload-skills</code>, <code>/help</code>, <code>/exit</code>) round out resumed sessions where Hermes ACP doesn&#x27;t re-emit <code>available_commands_update</code> after <code>session/load</code>. Deduped against the ACP-advertised set so the canonical entry always wins once a session opens.</li>
</ul>
<h4>Kanban v0.13 diagnostics + recovery UX</h4>
<ul>
  <li><strong>Hallucination-gate verify / reject</strong> — worker-created cards land with <code>hallucination_gate_status: pending</code>. The inspector renders a yellow banner (&quot;Created by a worker — verify before running&quot;) with a Verify and Reject button. Cards in pending state dim 0.6 with a yellow ⚠ glyph in the title row.</li>
  <li><strong>Diagnostics rendering</strong> — new typed-mirror enum <code>KanbanDiagnosticKind</code> with severity (info / warning / critical). Per-task and per-run diagnostics surface in the inspector Runs tab as chip-lists; auto-block reasons render verbatim in the existing red banner. Darwin zombie detections show as a distinct <code>darwin_zombie_detected</code> kind.</li>
  <li><strong>Per-task <code>max_retries</code></strong> — added to the create sheet (default 3) and shown as a header chip in the inspector. Write-once at create time, matching Hermes&#x27;s pattern.</li>
  <li><strong>Multiline title/body</strong> — the create sheet&#x27;s Title field accepts multiline input, capped to four visible rows.</li>
  <li><strong>Tolerant decoding</strong> — every new field uses <code>decodeIfPresent</code>. Pre-v0.13 JSON parses cleanly with the new fields defaulting to nil, and the v2.7.5 board surface is unchanged on older hosts.</li>
</ul>
<h4>Curator archive + prune</h4>
<ul>
  <li><strong>Archived skills section</strong> in <code>CuratorView</code> showing <code>hermes curator list-archived</code> output. Each row exposes Restore (returns to the active leaderboard) and Prune (destructive — opens a custom confirm sheet matching the template-uninstall pattern, with <code>ScarfDestructiveButton</code> &quot;Prune permanently&quot; and Cancel as the default keyboard action).</li>
  <li><strong>Bulk prune</strong> — a header action (gated on archived list non-empty) that enumerates every archived skill in the confirm sheet before a single-tap destructive action. Per-skill prune buttons are present per row when Hermes supports <code>prune &lt;name&gt;</code>; otherwise only the bulk action is exposed.</li>
  <li><strong>Synchronous &quot;Run Now&quot;</strong> — v0.13 <code>hermes curator run</code> blocks until done. The Run Now button shows a progress affordance for the duration; pre-v0.13 falls back to fire-and-forget.</li>
  <li><strong>New <code>CuratorService</code> actor</strong> in ScarfCore (<a href="scarf/Packages/ScarfCore/Sources/ScarfCore/Services/CuratorService.swift">scarf/Packages/ScarfCore/Sources/ScarfCore/Services/CuratorService.swift</a>) — pure-I/O Sendable actor mirroring <code>KanbanService</code>&#x27;s shape, with defensive <code>--json</code> retry-without-flag fallback for verbs that may not support it on all v0.13 patch releases.</li>
  <li>The legacy <code>CuratorRestoreSheet</code> flow (SAFE-list-restore for v0.12) is preserved; it predates the v0.13 archive surface and serves a distinct case.</li>
</ul>
<h4>Messaging Gateway expansion</h4>
<ul>
  <li><strong>Google Chat</strong> — 20th platform. New entry in the Mac Platforms tab, gated on <code>HermesCapabilities.hasGoogleChatPlatform</code>.</li>
  <li><strong>Cross-platform allowlists</strong> — per-platform editor for <code>allowed_channels</code> (Slack / Mattermost / Google Chat), <code>allowed_chats</code> (Telegram / WhatsApp), and <code>allowed_rooms</code> (Matrix / DingTalk). New <code>AllowlistEditor</code> component plus the <code>GatewayAllowlistKind</code> / <code>GatewayPlatformSettings</code> ScarfCore types. Persisted to <code>~/.hermes/config.yaml</code> via a new <code>GatewayConfigWriter</code> since <code>hermes config set</code> doesn&#x27;t write list blocks.</li>
  <li><strong>Per-platform behavior toggles</strong> — <code>busy_ack_enabled</code> (suppress per-message &quot;agent is working…&quot; acks), <code>gateway_restart_notification</code> (post a &quot;Gateway restarted&quot; notice on boot), and a slash-command auto-delete TTL (seconds, 0 to disable). Each appears in the new <code>GatewayBehaviorSection</code> component.</li>
  <li><strong><code>hermes gateway list</code> cross-profile digest</strong> — inline status row in <code>MessagingGatewayView</code> showing which profile is running which platform across all profiles. New <code>HermesGatewayListService</code> actor parses <code>hermes gateway list --json</code>. Hidden when the verb fails (pre-v0.13 hosts) or no profiles are registered.</li>
  <li><strong><code>MessagingGatewayViewModel</code></strong> — internal rename from <code>GatewayViewModel</code> to disambiguate from the v0.10 Tool Gateway feature. The user-facing label was already &quot;Messaging Gateway&quot; since v0.10.</li>
  <li><strong><code>[[as_document]]</code> hint</strong> — informational tooltip in skill detail surfaces explaining the new media-routing directive for skills that reference it.</li>
</ul>
<h4>Provider catalog refresh</h4>
<ul>
  <li><strong>Five new models</strong> — <code>deepseek/deepseek-v4-pro</code>, <code>x-ai/grok-4.3</code>, <code>openrouter/owl-alpha</code> (free tier), <code>tencent/hy3-preview</code>, and <code>arcee/trinity-large-thinking</code> (with temperature + compression overrides). Surfaced through <code>models_dev_cache.json</code>; no manual entries required.</li>
  <li><strong>Grok rename</strong> — <code>x-ai/grok-4.20-beta</code> → <code>x-ai/grok-4.20</code>. Implemented via read-time alias resolution in <code>ModelCatalogService.modelAliases</code> so existing user configs with the <code>-beta</code> suffix keep validating without YAML rewrites. Three composite-keyed aliases cover the openrouter / xai / vercel routes.</li>
  <li><strong>Vercel AI Gateway demoted</strong> — sort comparator change in <code>loadProviders()</code> puts Vercel last, after the alphabetical group.</li>
  <li><strong><code>image_gen.model</code> honored</strong> — pre-v0.13 the key was advertised but ignored; v0.13 actually drives the image-generation path. Surfaced in <code>Settings → Auxiliary</code> with a curated picker (<code>OpenAI gpt-image-1</code>, <code>Imagen 3/4</code>, <code>Stable Image Ultra</code>, <code>FLUX 1.1 Pro</code>, <code>DALL·E 3</code>); free-form entry is also accepted. Gated on <code>hasImageGenModel</code>.</li>
  <li><strong>OpenRouter response caching</strong> — toggle in <code>Settings → Auxiliary</code> writing <code>openrouter.response_cache.enabled</code> to <code>config.yaml</code>. Off by default in Scarf&#x27;s parser. Gated on <code>hasOpenRouterResponseCache</code>.</li>
</ul>
<h4>Settings tab additions</h4>
<ul>
  <li><strong>MCP SSE transport</strong> — MCP add-server flow gains a Transport picker (<code>stdio</code> / <code>http</code> / <code>sse</code>) with <code>sse_read_timeout</code> field for SSE servers. The YAML round-trip preserves OAuth + headers identically to the existing <code>.http</code> shape. Gated on <code>hasMCPSSETransport</code>.</li>
  <li><strong>Cron <code>--no-agent</code> watchdog mode</strong> — toggle in the Cron edit sheet that maps to <code>hermes cron create/update --no-agent</code>. When ON, the prompt + context fields hide (the AI call is skipped). Defensive write-path strips the flag on pre-v0.13 hosts mirroring the <code>--workdir</code> pattern. New <code>HermesCronJob.noAgent: Bool</code> field with <code>decodeIfPresent</code> so pre-v0.13 reads keep parsing. Gated on <code>hasCronNoAgent</code>.</li>
  <li><strong>Web Tools per-capability backends</strong> — new <code>Settings → Web Tools</code> tab with separate pickers for <code>web_search</code> and <code>web_extract</code>. SearXNG appears in the search picker only. The legacy single <code>web_tools.backend</code> is still readable for round-trip safety on mixed-version installs. Gated on <code>hasWebToolsBackendSplit</code>.</li>
  <li><strong>Profiles <code>--no-skills</code></strong> — &quot;Empty profile (no skills)&quot; toggle in the create-profile flow that appends <code>--no-skills</code> to <code>hermes profile create</code>. Disabled when &quot;Clone all&quot; is on (mutually exclusive). Gated on <code>hasProfileNoSkills</code>.</li>
</ul>
<h4>UX polish</h4>
<ul>
  <li><strong>Context compression count</strong> in the chat status bar. v0.13 emits the count alongside the token tally on the <code>session/prompt</code> response; Scarf renders a <code>🗜 ×N</code> chip next to the token count when <code>count &gt; 0</code>. Gated on <code>hasContextCompressionCount</code>.</li>
  <li><strong><code>/new &lt;name&gt;</code> argument hint</strong> — bracket-aware so v0.13 hosts show <code>[&lt;name&gt;]</code> and pre-v0.13 hosts show no hint.</li>
  <li><strong><code>HermesUpdaterCommandBuilder</code></strong> — forward-compat plumbing for <code>hermes update --yes</code>. No in-app surface in v2.8.0 (Scarf doesn&#x27;t currently expose a &quot;Run hermes update&quot; command); the builder is wired so a future Settings affordance can opt in cleanly.</li>
  <li><strong>Redaction default-flip awareness</strong> — the existing <code>Settings → Advanced → Redaction</code> toggle hint copy now branches on <code>HermesCapabilities.isV013OrLater</code>. v0.13+ hosts read &quot;Recommended: ON. Hermes v0.13 defaults to redacting secrets unless you opt out&quot;; pre-v0.13 keeps the v2.7 hint.</li>
  <li><strong><code>display.language</code> picker</strong> — new <code>Settings → General → Locale</code> row. 8 options: default, zh, ja, de, es, fr, uk, tr. Hermes does the actual translation; Scarf just persists <code>display.language</code> to <code>config.yaml</code>. Gated on <code>hasDisplayLanguage</code>.</li>
  <li><strong>xAI Custom Voices badge</strong> — <code>Settings → Voice</code> shows a &quot;Cloning supported&quot; <code>ScarfBadge</code> next to the xAI TTS provider entry. Informational only; voice management itself happens via <code>hermes voice</code> CLI. Gated on <code>hasXAIVoiceCloning</code>.</li>
</ul>
<h4>ScarfGo iOS catch-up (read-only)</h4>
<p>Following the Phase H precedent, iOS mirrors selected v2.8 surfaces as read-only — write parity is deferred to v2.8.x.</p>
<ul>
  <li><strong>Goal pill + queue chip</strong> in the iOS chat header (<code>projectContextBar</code>). Tap is a no-op; the Mac app owns mutations.</li>
  <li><strong>Kanban v0.13 diagnostics</strong> in <code>ScarfGoKanbanDetailSheet</code> — <code>retries: N</code> chip, &quot;Worker-created — verify on Mac&quot; hallucination badge, red <code>auto_blocked_reason</code> banner, tappable diagnostics chip-lists with severity-tinted badges and a new <code>DiagnosticDetailSheet</code> (replacing Mac&#x27;s <code>.help()</code> tooltip on touch).</li>
  <li><strong>Curator Archived list</strong> in <code>Scarf iOS/Curator/CuratorView.swift</code> — read-only, with footer pointing users to the Mac app for Restore / Prune actions.</li>
  <li><strong>Settings → Platforms extension</strong> — Google Chat status row, busy-ack and restart-notification summary rows across <code>gatewayPlatforms</code> (handles disagreement with &quot;mixed (N platforms)&quot;), allowlist DisclosureGroups with monospaced &quot;platform: id&quot; entries when expanded.</li>
  <li><strong>&quot;v0.13 features active&quot; badge</strong> in iOS Settings (gated on <code>caps.isV013OrLater</code>). Tap presents <code>V013FeaturesSheet</code> listing the new affordances.</li>
</ul>
<h3>Capability gating</h3>
<p>v2.8.0 adds 22 new flags on <code>HermesCapabilities</code> (each gating one v0.13 surface), plus an <code>isV013OrLater</code> convenience predicate. Every new affordance is gated; pre-v0.13 hosts see the v2.7.5 surface byte-identical to before. The HermesVersionBanner threshold remains pre-v0.12 — v0.12 → v0.13 nudging happens via the iOS Settings badge (positive surface) rather than a global yellow banner (which was reserved for &quot;missing every new feature&quot; cases).</p>
<h3>Bug fixes uncovered during v0.13.0 dogfooding</h3>
<ul>
  <li><strong>Dashboard flicker on v0.13 hosts</strong> — Hermes v0.13 writes to <code>state.db-wal</code> and rotating logs at ~10 Hz during gateway activity. Each FSEvents fire ticked <code>lastChangeDate</code>, every observing view re-fired its load handler against it, and on Local hosts the dashboard stacked 5+ concurrent <code>dashboardSnapshot</code> calls in 200 ms — sqlite contention on the read-only handle surfaced as <code>BackendError error 3</code>, plus visible flicker. Two-part fix: <code>HermesFileWatcher.scheduleCoalescedTick</code> coalesces FSEvents into one observable mutation per 500 ms quiet window with a 1.5 s max-wait floor (so a coincident <code>gateway_state.json</code> Start/Stop touch can&#x27;t be starved indefinitely under sustained WAL writes); <code>DashboardViewModel.load()</code> holds a single in-flight <code>Task&lt;Void, Never&gt;</code> handle so concurrent triggers await the in-flight load instead of stacking.</li>
  <li><strong>Sparse slash menu on resumed sessions</strong> — Hermes ACP only emits <code>available_commands_update</code> after <code>session/new</code>, not after <code>session/load</code>. Combined with <code>RichChatViewModel.reset()</code> clearing <code>acpCommands</code> on every session switch, resumed sessions landed at a 4-command fallback even though the agent identity hadn&#x27;t changed. Fix: stop wiping <code>acpCommands</code> in <code>reset()</code> (they&#x27;re agent-level, not session-level), and add an active-session-only static fallback set covering the standard agent commands so cold-start LOAD users see a rich menu immediately.</li>
</ul>
<h3>Migrating from 2.7.5</h3>
<p>Sparkle delivers the update automatically. No config migration, no schema changes — same <code>~/.hermes/state.db</code> columns as v0.11/v0.12, same Scarf-owned sidecars at <code>~/.hermes/scarf/</code>. Existing v2.7.5 Kanban tenants stay valid; existing project manifests are unchanged. Settings tabs grow new rows; existing rows render identically.</p>
<p>If you&#x27;re connecting to a Hermes v0.13.0 host for the first time after this update, the new surfaces light up automatically — no flag flip in the app. Pre-v0.13 hosts continue to render the v2.7.5 surface; nothing breaks if you upgrade Scarf before upgrading Hermes.</p>
<h3>Known limitations</h3>
<ul>
  <li><strong>iOS write surfaces</strong> (Verify hallucination gate, Reject, Curator archive/prune actions, allowlist editor, <code>/goal</code> send, <code>/queue</code> send) are explicitly out of scope for v2.8.0 and slated for v2.8.x. iOS surfaces are read-only mirrors per the Phase H precedent.</li>
  <li><strong>Auto-resumed-from-checkpoint indicator</strong> — Hermes v0.13&#x27;s &quot;auto-resume after gateway restart&quot; feature is server-side; whether the ACP adapter advertises a Scarf-visible signal is unclear pending live host verification. Deferred to v2.8.1.</li>
  <li><strong>xAI voice cloning management UX</strong> — only the &quot;Cloning supported&quot; badge ships in v2.8.0. A full voice-management surface is a follow-up.</li>
  <li><strong>Bulk re-tag for legacy NULL-tenant Kanban tasks</strong> — carryover from v2.7.5; Hermes still has no <code>tenant</code> mutation verb post-create.</li>
  <li><strong>Cluster A wire-shape TODOs</strong> — 25 <code>// TODO(WS-N-Q&lt;n&gt;)</code> markers across the codebase flag fields and CLI flags whose exact shape couldn&#x27;t be verified from release notes alone. Each has a tolerant-decode default that fails closed (hides the affordance rather than throwing); a pre-merge sweep on a v0.13 host can confirm or fix each in seconds.</li>
</ul>
<h3>Acknowledgements</h3>
<p>v2.8.0 was driven by a 9-stream coordinated multi-agent build: WS-1 capability flag foundation through WS-9 iOS catch-up, with planning artifacts archived under <a href="scarf/docs/v2.8/">scarf/docs/v2.8/</a> for future reference. Bug fixes for the dashboard flicker and sparse-slash-menu issues were caught during a fresh end-to-end dogfood pass against a live Hermes v0.13.0 install — the kind of surface-level UX bugs that only show up under real-world <code>state.db-wal</code> write rates and real-world resume flows. As always, real bugs come from doing instead of speculating.</p>
</body></html>
      ]]></description>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.8.0/Scarf-v2.8.0-Universal.zip"
                 sparkle:edSignature="m5HQUKgxfWa5u88gEVCGWMIKaogBIsjPspQG97y1KcrW1w6S5XF1s0v1oRaRWMyIlj46BD+937Inu2Ii5TbXAg=="
                 length="19771149"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.7.5</title>
      <sparkle:version>34</sparkle:version>
      <sparkle:shortVersionString>2.7.5</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Fri, 08 May 2026 10:56:09 +0000</pubDate>
      <description><![CDATA[
<!DOCTYPE html><html><head><meta charset="utf-8"><style>body {
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  font-size: 13px;
  line-height: 1.5;
  color: #1d1d1f;
  margin: 0;
  padding: 0 4px;
}
h2 {
  font-size: 17px;
  margin: 16px 0 6px 0;
  border-bottom: 1px solid #e5e5e7;
  padding-bottom: 3px;
}
h3 {
  font-size: 14px;
  margin: 14px 0 4px 0;
  color: #424245;
}
h4 {
  font-size: 13px;
  font-weight: 600;
  margin: 10px 0 2px 0;
}
p { margin: 6px 0; }
ul { margin: 6px 0; padding-left: 20px; }
li { margin: 3px 0; }
code {
  background: #f5f5f7;
  border-radius: 3px;
  padding: 1px 4px;
  font-family: "SF Mono", Menlo, Consolas, monospace;
  font-size: 12px;
}
pre {
  background: #f5f5f7;
  border-radius: 5px;
  padding: 8px 10px;
  overflow-x: auto;
  font-size: 12px;
}
pre code { background: transparent; padding: 0; }
a { color: #0066cc; text-decoration: none; }
a:hover { text-decoration: underline; }
hr {
  border: none;
  border-top: 1px solid #e5e5e7;
  margin: 16px 0;
}
strong { color: #1d1d1f; }
@media (prefers-color-scheme: dark) {
  body { color: #f5f5f7; background: #1c1c1e; }
  h2 { border-bottom-color: #38383a; }
  h3 { color: #c7c7cc; }
  code, pre { background: #2c2c2e; }
  hr { border-top-color: #38383a; }
  a { color: #4499ff; }
  strong { color: #f5f5f7; }
}
</style></head><body>
<h2>What&#x27;s in 2.7.5</h2>
<p>A feature release that lifts Scarf&#x27;s Kanban surface from a read-only list (the v2.6 placeholder shipped while upstream Kanban was still mid-rework) to a full drag-and-drop board with the complete Hermes v0.12 mutation surface wired up — plus per-project boards bound to a Scarf-minted tenant slug, and a read-only board on iOS for at-a-glance status from your phone. No data migrations, no schema changes; pre-v0.12 hosts gracefully hide the surface.</p>
<h3>New features</h3>
<h4>Mac</h4>
<ul>
  <li><strong>Drag-and-drop Kanban board</strong> (<a href="scarf/scarf/Features/Kanban/Views/KanbanBoardView.swift">scarf/Features/Kanban/Views/KanbanBoardView.swift</a>). Five visible columns — Triage / Up Next (<code>todo</code> + <code>ready</code>) / Running / Blocked / Done — collapsing Hermes&#x27;s seven status values into a layout that doesn&#x27;t waste space on <code>ready</code>, which the dispatcher only ever holds for a few seconds. Triage hides itself when empty; archived hides behind a header toggle. Drop a card onto a column and Scarf maps the gesture to the right Hermes verbs through a pure transition planner: drop-on-Running fires <code>kanban dispatch</code> (the dispatcher then spawns a worker), drop-on-Blocked opens a sheet asking for a reason and calls <code>kanban block</code>, drop-on-Done opens a result sheet and calls <code>kanban complete</code>, blocked → running chains <code>unblock</code> + <code>dispatch</code>. Forbidden transitions (anything dropped on Done; anything dragged out of Triage) reject with a red drop-target stroke and a tooltip explaining why — Done is terminal, Triage is promoted by a specifier worker, neither has a CLI verb that maps cleanly. Optimistic local updates apply on drop and revert on CLI failure with a toast, so the UI feels instant.</li>
</ul>
<ul>
  <li><strong>Side-pane inspector</strong> (<a href="scarf/scarf/Features/Kanban/Views/KanbanInspectorPane.swift">KanbanInspectorPane.swift</a>). Click a card and a 420 px pane slides in from the trailing edge. Not a modal sheet — modal would block triaging the next card after closing. Header carries the status, an inline assignee menu (more on that below), workspace kind, and tenant; below that, four tabs render <code>hermes kanban show &lt;id&gt;</code> data: <strong>Comments</strong> (with an inline composer that calls <code>kanban comment</code>), <strong>Events</strong> (the <code>task_events</code> log with per-kind glyphs), <strong>Runs</strong> (one row per attempt with outcome badge + summary + error), and <strong>Log</strong> — the worker&#x27;s captured stdout/stderr from <code>hermes kanban log &lt;id&gt;</code>, polled every 2 s while the task is running with a &quot;● streaming&quot; indicator and auto-scroll to the latest line, snapshot-only with a refresh button when the task is in a terminal state. The action bar at the bottom has all the per-status verbs — Start (which is <code>claim</code> rebranded as a user-visible action), Complete, Block, Unblock, Archive — every one with a help tooltip explaining what it does and what Hermes verb it invokes. The &quot;Archive&quot; tooltip explicitly notes Hermes has no hard-delete: archived tasks remain in <code>~/.hermes/kanban.db</code> and are recoverable via the &quot;Show archived&quot; toggle until <code>hermes kanban gc</code> runs.</li>
</ul>
<ul>
  <li><strong>Inspector auto-refresh.</strong> While the inspector is open, the detail (header, action buttons, comments, events, runs) re-fetches every 5 s on the same cadence as the board itself, so a worker transition (e.g. running → done elsewhere) is reflected without the user having to close + reopen. The Log tab&#x27;s 2 s poll runs separately and self-cancels the moment the task transitions out of <code>running</code>.</li>
</ul>
<ul>
  <li><strong>Inline assignee picker on the inspector header.</strong> The assignee badge is a clickable menu — set means a <code>.brand</code> (rust) chip, unassigned means a <code>.warning</code> (yellow) chip so the eye catches it instantly. Tapping opens a menu of every known profile (union of <code>~/.hermes/profiles/</code>, current task assignees, and the active local profile from <code>HermesProfileResolver</code>) plus an &quot;Unassigned&quot; option. Selection routes through <code>kanban assign</code> and immediately follows with <code>kanban dispatch</code> so the task gets picked up promptly. Solves the &quot;I assigned a profile but nothing happened&quot; gap end-to-end without the user touching a terminal.</li>
</ul>
<ul>
  <li><strong>Health banner in the inspector.</strong> Surfaces two conditions that previously left users staring at a stuck task with no explanation. <strong>Yellow</strong> when the task is unassigned in <code>ready</code> / <code>todo</code>: <em>&quot;Won&#x27;t run automatically — Hermes&#x27;s dispatcher silently skips tasks with no assignee.&quot;</em> The dispatcher&#x27;s own <code>--json</code> output literally lists these under <code>skipped_unassigned</code>; we now surface that to the human. <strong>Red</strong> when the most-recently-completed run ended in a non-success outcome (<code>stale_lock</code> / <code>crashed</code> / <code>gave_up</code> / <code>timed_out</code> / <code>spawn_failed</code> / <code>reclaimed</code> / <code>failed</code>): banner displays the outcome label + the raw <code>error</code> field from the run record, so you don&#x27;t have to dig into the Runs tab to discover it. The red banner is suppressed while a fresh attempt is running — once status flips back to <code>running</code>, the previous outcome is stale signal and the Log tab&#x27;s live stream is the right thing to look at.</li>
</ul>
<ul>
  <li><strong>Card-level signals.</strong> Cards in <code>running</code> get a 2 px <code>ScarfColor.info</code> left edge + a subtle title shimmer so live work is obvious at a glance. Blocked cards get a 2 px <code>ScarfColor.warning</code> left edge + a ⚠ glyph next to the title. Done cards dim to 0.7 opacity in light mode, 0.55 in dark, with a green ✓ in the title row. Cards in <code>ready</code> / <code>todo</code> with no assignee get a yellow ⚠ glyph in the title row with a tooltip explaining the dispatcher won&#x27;t pick them up — same signal as the inspector banner, just at the board level so triage is one keypress away.</li>
</ul>
<ul>
  <li><strong><code>Board | List</code> toggle at the top of the route.</strong> The v2.6 read-only list view is preserved in <code>KanbanListView.swift</code> and surfaced via a segmented picker, so users on narrow windows or anyone who prefers a flat sortable list can opt in. Choice persists across launches via <code>@AppStorage</code>.</li>
</ul>
<ul>
  <li><strong>New Task sheet</strong> (<a href="scarf/scarf/Features/Kanban/Views/KanbanCreateSheet.swift">KanbanCreateSheet.swift</a>). Title, body (markdown supported), assignee (defaults to <code>HermesProfileResolver.activeProfileName()</code> so newly-created tasks actually run), workspace kind (segmented <code>Scratch / Worktree / Project Dir</code>; locked to Project Dir on per-project boards), priority slider, comma-separated skills with autocomplete from <code>~/.hermes/skills/</code>, optional tenant (hidden on per-project boards — the slug is implicit), and a &quot;Send to triage&quot; toggle. Submit fires <code>kanban create --json</code> and immediately follows with <code>kanban dispatch</code> so an assigned task transitions <code>ready</code> → <code>running</code> within seconds rather than waiting for the gateway dispatcher&#x27;s internal cycle.</li>
</ul>
<ul>
  <li><strong>Kanban moved from Manage → Monitor in the sidebar.</strong> It&#x27;s runtime work-in-progress, not configuration. Sits between Activity and the rest of Manage so users see &quot;what&#x27;s happening right now&quot; at a glance.</li>
</ul>
<h4>Per-project Kanban</h4>
<ul>
  <li><strong><code>DashboardTab.kanban</code> on every project</strong>, capability-gated on <code>HermesCapabilities.hasKanban</code>. Renders a project-scoped <code>KanbanBoardView</code> filtered to the project&#x27;s tenant slug. Workspace defaults in the New Task sheet are pre-pinned to <code>dir:&lt;project.path&gt;</code>. Empty state explains the project doesn&#x27;t have any tasks yet and offers a &quot;New Task&quot; CTA — the empty board IS the discovery surface.</li>
</ul>
<ul>
  <li><strong>Tenant minting via <a href="scarf/scarf/Core/Services/KanbanTenantResolver.swift">KanbanTenantResolver</a>.</strong> Each Scarf project gets a stable <code>scarf:&lt;slug&gt;</code> tenant minted on first kanban interaction and persisted to <code>&lt;project&gt;/.scarf/manifest.json</code> (new optional <code>kanbanTenant</code> field on <code>ProjectTemplateManifest</code>). Slug rules: lowercased, hyphenated, ≤ 48 chars, <code>scarf:</code> prefix to avoid collision with hand-typed tenants. Once minted, the tenant is <strong>immutable across rename</strong> — tasks already on the board carry the original slug, so renaming the project doesn&#x27;t orphan them. Bare projects (no manifest) get a sentinel manifest written with <code>id: scarf/&lt;project-id&gt;</code> + <code>version: 0.0.0</code> + just the <code>kanbanTenant</code> set; the <code>ProjectAgentContextService</code> reader recognizes the sentinel and refuses to surface it as a &quot;Template&quot; line in the AGENTS.md block, so the project doesn&#x27;t suddenly start advertising a fake template to the agent.</li>
</ul>
<ul>
  <li><strong>Agent-side tenant injection.</strong> <a href="scarf/scarf/Core/Services/ProjectAgentContextService.swift">ProjectAgentContextService.renderBlock</a> emits a &quot;Kanban tenant&quot; line inside the <code>&lt;!-- scarf-project --&gt;</code> markers in <code>&lt;project&gt;/AGENTS.md</code> whenever a tenant exists, instructing the agent to pass <code>--tenant scarf:&lt;slug&gt;</code> on <code>hermes kanban create</code>. <code>ChatViewModel.startACPSession</code> already calls <code>refresh(for:)</code> before opening every project chat, so the agent reads a fresh tenant on every session start with no extra wiring. Agents are imperfect at flag discipline; a forgotten <code>--tenant</code> lands the task in the global &quot;Untagged&quot; group rather than failing — acceptable v2.7.5 behavior.</li>
</ul>
<ul>
  <li><strong><code>kanban_summary</code> dashboard widget</strong> (<a href="scarf/scarf/Features/Projects/Views/Widgets/KanbanSummaryWidgetView.swift">KanbanSummaryWidgetView.swift</a>). New widget kind for project dashboards: shows the top three <code>running</code> / <code>blocked</code> / <code>todo</code> tasks for the project&#x27;s tenant by priority, plus a glance footer (<code>&quot;12 todo · 3 running · 5 blocked&quot;</code>) sourced from <code>kanban stats</code>. Polls every 10 s while the dashboard is foregrounded. Widget vocabulary registered in <a href="tools/widget-schema.json">tools/widget-schema.json</a> and rendered on the catalog site via <a href="site/widgets.js">site/widgets.js</a>; template authors can drop a <code>{ kind: kanban_summary, max_rows: 3 }</code> block into <code>dashboard.json</code>.</li>
</ul>
<h4>iOS / iPadOS</h4>
<ul>
  <li><strong>Read-only Kanban tab on <code>ProjectDetailView</code></strong> (<a href="scarf/Scarf%20iOS/Kanban/ScarfGoKanbanView.swift">Scarf iOS/Kanban/ScarfGoKanbanView.swift</a>). Same five-column collapse rendered as a horizontally-paged segmented <code>Picker</code> of single-column lists — HIG-friendly on iPhone where a 5-column grid forces unreadable card widths. Pulls live status, assignee, workspace, skills, priority chips. Tap a card → modal <code>NavigationStack</code> detail sheet (<a href="scarf/Scarf%20iOS/Kanban/ScarfGoKanbanDetailSheet.swift">ScarfGoKanbanDetailSheet.swift</a>) with the same Comments / Events / Runs tabs the Mac inspector has. Read-only in v2.7.5 — mutations + drag-drop on iPad land in v2.8 once the Mac flow is fully shaken out. Card titles use semantic <code>.headline</code> (not <code>ScarfFont</code>) so Dynamic Type works; chrome (badges) stays on <code>ScarfBadge</code> for fixed visual weight per the project&#x27;s iOS conventions.</li>
</ul>
<h4>ScarfCore</h4>
<ul>
  <li><strong><code>KanbanService</code> actor</strong> (<a href="scarf/Packages/ScarfCore/Sources/ScarfCore/Services/KanbanService.swift">Packages/ScarfCore/Sources/ScarfCore/Services/KanbanService.swift</a>) — pure-I/O Sendable actor wrapping every Hermes v0.12 verb (<code>list / show / runs / stats / assignees / create / assign / claim / comment / complete / block / unblock / archive / dispatch / link / unlink / log</code>). Dispatches each CLI invocation through <code>Task.detached(priority: .utility)</code> matching the existing concurrency conventions. Errors land in <a href="scarf/Packages/ScarfCore/Sources/ScarfCore/Models/KanbanError.swift">KanbanError</a> and surface as inline banners (not modal alerts) since the board is high-frequency. The &quot;no matching tasks&quot; stdout sentinel is normalized to <code>[]</code> rather than thrown.</li>
</ul>
<ul>
  <li><strong>Pure transition planner.</strong> <code>KanbanService.plan(for: KanbanTransition)</code> is a synchronous function that maps a <code>(from, to)</code> column pair to the right verb sequence — <code>(.upNext, .running) → [.dispatch]</code>, <code>(.blocked, .running) → [.unblock, .dispatch]</code>, etc. Disallowed transitions throw <code>KanbanError.forbiddenTransition</code> with a user-actionable reason. The planner is fully tested in <code>KanbanModelsTests.swift</code>. Critically: <code>dispatch</code> (not <code>claim</code>) is the verb used for Up-Next → Running. Hermes&#x27;s <code>claim</code> is documented as &quot;manual alternative to the dispatcher&quot; and assumes the caller spawns the worker themselves — Scarf doesn&#x27;t, so calling <code>claim</code> from drag-drop reserved tasks but never spawned work, and the dispatcher reclaimed them ~15 minutes later (<code>stale_lock</code>). <code>dispatch</code> is the right primitive for a GUI client.</li>
</ul>
<ul>
  <li><strong>Cross-platform <a href="scarf/Packages/ScarfCore/Sources/ScarfCore/Services/KanbanTenantReader.swift">KanbanTenantReader</a>.</strong> Read-only projection over <code>&lt;project&gt;/.scarf/manifest.json</code>&#x27;s <code>kanbanTenant</code> field. The full <code>ProjectTemplateManifest</code> type lives in the Mac target; this lightweight reader gives iOS a way to filter the per-project board by tenant without linking the full manifest model.</li>
</ul>
<ul>
  <li><strong>Timestamp decoding tolerates both shapes.</strong> Hermes emits <code>created_at</code> / <code>started_at</code> / <code>completed_at</code> / <code>last_heartbeat_at</code> etc. as Unix integer seconds (its SQLite columns are INTEGER), but earlier wire docs implied ISO-8601 strings. The decoder now accepts either an integer or a string and normalizes to ISO-8601 so downstream code only handles one type. Locked in by <code>decodeUnixIntegerTimestamps</code> in <code>KanbanModelsTests</code>.</li>
</ul>
<ul>
  <li><strong><code>KanbanBoardViewModel</code> optimistic merge.</strong> Holds <code>optimisticOverrides: [taskId: status]</code> for in-flight drags; the polled response merges with optimistic state until the server confirms the new status, so a stale poll arriving milliseconds after a drop can&#x27;t snap the card back to its old column. On CLI failure the override is removed and the message lands in the inline banner.</li>
</ul>
<h3>Dispatch + assignee fixes</h3>
<p>A diagnostic round driving real tasks end-to-end exposed a connected bug pattern that the polish pass closed:</p>
<ul>
  <li><strong>Hermes&#x27;s dispatcher silently skips unassigned tasks</strong> — its <code>kanban dispatch --json</code> output literally lists them under a <code>skipped_unassigned</code> key and moves on. Tasks created without an assignee sat in <code>ready</code> indefinitely and the user had no signal anything was wrong. The New Task sheet now defaults to the active Hermes profile, the inspector header shows a yellow &quot;Unassigned&quot; chip + warning banner, every <code>ready</code> / <code>todo</code> card without an assignee gets a ⚠ glyph + tooltip, and the inspector&#x27;s inline assignee picker fixes it in one click.</li>
</ul>
<ul>
  <li><strong>Drag-to-Running used to call <code>claim</code></strong>, which is a manual alternative to the dispatcher. Status flipped to <code>running</code>, but no worker spawned (Scarf doesn&#x27;t host workers), and 15 minutes later the dispatcher reclaimed the task with a <code>stale_lock</code> outcome. Replaced with <code>dispatch</code> end-to-end so the gateway-running dispatcher actually does the spawning.</li>
</ul>
<ul>
  <li><strong><code>hermes kanban assignees</code> empty-state was leaking into the picker.</strong> The CLI prints a literal sentinel <code>(no assignees — create a profile with hermes -p &lt;name&gt; setup)</code> when the table is empty; the parser was tokenizing it on whitespace and offering <code>(no</code> as a profile in the menu. Parser now skips the sentinel, validates each candidate against <code>^[a-zA-Z0-9_-]+$</code>, and falls back cleanly to the active local profile when the table is empty.</li>
</ul>
<ul>
  <li><strong><code>spawn_failed</code> from &quot;executable not found on PATH&quot;</strong> — most subtle of the lot. macOS GUI apps inherit a launch-services PATH (<code>/usr/bin:/bin:/usr/sbin:/sbin</code>) that doesn&#x27;t include <code>~/.local/bin</code> (where pipx installs <code>hermes</code>) or <code>/opt/homebrew/bin</code>. Scarf was finding <code>hermes</code> for its own invocation via the absolute-path resolver in <code>HermesPathSet.hermesBinaryCandidates</code>, but when the dispatcher then spawned a worker process, that worker inherited Scarf&#x27;s GUI PATH and couldn&#x27;t find <code>hermes</code> by name — recording an <code>outcome=spawn_failed</code> run with the exact &quot;executable not found on PATH&quot; message. <code>LocalTransport</code> now grows an <code>environmentEnricher</code> static (mirroring <code>SSHTransport.environmentEnricher</code>) wired by <code>scarfApp.swift</code> to the same <code>HermesFileService.enrichedEnvironment()</code> login-shell probe the SSH transport uses. Every local subprocess Scarf spawns now sees the user&#x27;s full PATH and credential env, so a spawned-from-Scarf hermes can spawn its children by name without reaching for absolute paths. Defense-in-depth: <code>subprocessEnvironment(forExecutable:)</code> also unconditionally prepends the executable&#x27;s parent directory to PATH, so the fix works even if the enricher hasn&#x27;t been wired (early startup, tests).</li>
</ul>
<h3>Migrating from 2.7.1</h3>
<p>Sparkle will offer the update automatically. No config migration, no schema changes — <code>~/.hermes/kanban.db</code> is shared across all Hermes clients and Scarf only reads/writes through the documented CLI surface. Existing Scarf projects pick up the new project Kanban tab on first open; the tenant slug is minted lazily on first kanban interaction inside the project, so projects with no kanban activity stay byte-identical until the user opens the tab.</p>
<p>If you have an existing project with a Scarf-managed <code>manifest.json</code>, the new optional <code>kanbanTenant</code> field is added on next mint and lives alongside any template-author config schema without touching it. Templates do not ship <code>kanbanTenant</code> (it&#x27;s user-machine-scoped state); the export pipeline strips it.</p>
<p>If you&#x27;ve been running tasks via the v2.6 read-only list and your Hermes host already runs the gateway dispatcher, your existing kanban tasks should appear on the board automatically — there&#x27;s no migration step. Tasks created without an assignee in v2.6 will now show the yellow &quot;Unassigned&quot; warning until you fix them through the inline picker.</p>
<h3>Known limitations</h3>
<ul>
  <li><strong>Within-column reorder is not supported.</strong> Hermes has no <code>update</code> verb and no <code>position</code> column on the tasks table — <code>priority</code> is write-once at create time. Sort order inside each column is <code>priority DESC, created_at DESC</code>, matching the dispatcher&#x27;s actual run order. We considered a client-side ordering sidecar; rejected because the on-screen order would diverge from what runs next, which is worse than no manual order. Will revisit if Hermes ships an <code>update --priority</code> verb.</li>
</ul>
<ul>
  <li><strong>No live <code>watch</code> streaming yet.</strong> The board polls every 5 s; the inspector polls detail on the same cadence and the Log tab on a 2 s cadence while running. <code>hermes kanban watch --json</code> event streaming + reconnect-with-backoff lands in v2.8 along with iOS write surfaces.</li>
</ul>
<ul>
  <li><strong>No bulk re-tag for legacy NULL-tenant tasks.</strong> Tasks created before this release (assignee or no assignee) appear in the global &quot;Untagged&quot; group on the global board. Hermes has no <code>tenant</code> mutation verb post-create, so retagging would be archive + recreate — too destructive to ship in this release.</li>
</ul>
<h3>Acknowledgements</h3>
<ul>
  <li>Driven end-to-end against a fresh local Hermes v0.12.0 install with the gateway dispatcher running. Real bug surface mostly came from doing instead of speculating: the <code>claim</code> vs <code>dispatch</code> distinction, the silent <code>skipped_unassigned</code> behavior, the <code>(no</code> parse leak, the integer-vs-ISO timestamp shape, and the stale &quot;Last run&quot; banner during a fresh attempt all surfaced from driving real tasks and watching what actually happened.</li>
</ul>
</body></html>
      ]]></description>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.7.5/Scarf-v2.7.5-Universal.zip"
                 sparkle:edSignature="6QLmonqLdavcU3+u7NE3oYL4Iui4wZZ0r9OWM+kj0uJ3tM32C14N35g7kXmADvo50YONIAmxqfkYWo/AIX70AA=="
                 length="19346988"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.7.1</title>
      <sparkle:version>33</sparkle:version>
      <sparkle:shortVersionString>2.7.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Thu, 07 May 2026 10:51:54 +0000</pubDate>
      <description><![CDATA[
<!DOCTYPE html><html><head><meta charset="utf-8"><style>body {
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  font-size: 13px;
  line-height: 1.5;
  color: #1d1d1f;
  margin: 0;
  padding: 0 4px;
}
h2 {
  font-size: 17px;
  margin: 16px 0 6px 0;
  border-bottom: 1px solid #e5e5e7;
  padding-bottom: 3px;
}
h3 {
  font-size: 14px;
  margin: 14px 0 4px 0;
  color: #424245;
}
h4 {
  font-size: 13px;
  font-weight: 600;
  margin: 10px 0 2px 0;
}
p { margin: 6px 0; }
ul { margin: 6px 0; padding-left: 20px; }
li { margin: 3px 0; }
code {
  background: #f5f5f7;
  border-radius: 3px;
  padding: 1px 4px;
  font-family: "SF Mono", Menlo, Consolas, monospace;
  font-size: 12px;
}
pre {
  background: #f5f5f7;
  border-radius: 5px;
  padding: 8px 10px;
  overflow-x: auto;
  font-size: 12px;
}
pre code { background: transparent; padding: 0; }
a { color: #0066cc; text-decoration: none; }
a:hover { text-decoration: underline; }
hr {
  border: none;
  border-top: 1px solid #e5e5e7;
  margin: 16px 0;
}
strong { color: #1d1d1f; }
@media (prefers-color-scheme: dark) {
  body { color: #f5f5f7; background: #1c1c1e; }
  h2 { border-bottom-color: #38383a; }
  h3 { color: #c7c7cc; }
  code, pre { background: #2c2c2e; }
  hr { border-top-color: #38383a; }
  a { color: #4499ff; }
  strong { color: #f5f5f7; }
}
</style></head><body>
<h2>What&#x27;s in 2.7.1</h2>
<p>A patch release covering three bug reports filed against 2.7.0, plus follow-up cleanups in the same neighborhood. No data migrations, no UI surface changes — drop-in replacement for 2.7.0 on Mac.</p>
<h3>Bug fixes</h3>
<h4>Mac</h4>
<ul>
  <li><strong><a href="https://github.com/awizemann/scarf/issues/77">#77</a> — Sessions screen renders empty even when Dashboard reports sessions exist.</strong> v2.7.0 folded the Sessions tab&#x27;s two SQL queries (sessions list + previews) into a single batched SSH round-trip for perf. The combined wire payload for any user with ~150+ sessions crossed macOS&#x27;s 16–64 KB pipe-buffer threshold; without a concurrent reader draining the pipe, the remote <code>sqlite3 -json</code> blocked, the script never finished, our 30-second timeout fired, and the call returned an empty result. <code>SSHScriptRunner</code> now drains stdout/stderr concurrently with the running process via <code>FileHandle.readabilityHandler</code>, so the kernel pipe never fills. Same fix applied to the local-execution path. New regression test pushes 256 KB of synthetic output through the runner and asserts full delivery — would have wedged pre-fix.</li>
</ul>
<ul>
  <li><strong><a href="https://github.com/awizemann/scarf/issues/78">#78</a> — Skills &quot;What&#x27;s New&quot; pill contradicts the Updates sub-tab.</strong> The pill at the top of the Skills page was rendering on every sub-tab, including Updates. It counts <strong>local</strong> file deltas since the user last clicked &quot;Mark as seen&quot; (e.g. &quot;18 new&quot; = 18 skills landed on disk that you haven&#x27;t acknowledged), while the Updates body runs <code>hermes skills check</code> to find skills with newer <strong>upstream</strong> versions available — a different concept. Two surfaces using the word &quot;update&quot; for two different things made the screen contradict itself. Two changes: the pill now renders only on the Installed sub-tab (Mac and ScarfGo), and its label says &quot;X <strong>changed</strong> since you last looked&quot; instead of &quot;X updated&quot; so the local-file vocabulary doesn&#x27;t collide with upstream-update vocabulary anywhere on the page.</li>
</ul>
<ul>
  <li><strong><a href="https://github.com/awizemann/scarf/issues/79">#79</a> — Skills hub search returns nothing for terms visible in Browse.</strong> With the source picker on &quot;All Sources&quot;, <code>hermes skills search &lt;query&gt;</code> (no <code>--source</code> flag) routes through Hermes&#x27;s centralized index and skips external API sources (skills-sh, github, clawhub, lobehub, well-known) — but Browse still aggregates from those sources, so a skill like <code>honcho</code> would show up in Browse and disappear in search. Same picker, same query, contradictory results. Rather than chase Hermes&#x27;s index gaps, &quot;All Sources&quot; search now means &quot;filter what you can already see&quot;: Scarf caches the most recent Browse payload and runs a client-side substring filter (case-insensitive against name, description, and identifier) against it, instantly. Source-specific searches still shell out to <code>hermes skills search --source &lt;s&gt;</code> for full upstream search semantics. Five new tests cover the filter behavior.</li>
</ul>
<ul>
  <li><strong><code>hermesPIDResult()</code> — narrow the Hermes &quot;is it running?&quot; probe to the gateway.</strong> Previously <code>pgrep -f hermes</code>, which matched any process with &quot;hermes&quot; in its argv: chat sessions Scarf itself spawns, <code>hermes -z</code> one-shots, log tails, even the README in an editor. The Dashboard &quot;Hermes is running&quot; badge could read true even when the gateway daemon was down. Tightened to a regex that matches only the gateway shape — <code>python -m hermes_cli.main gateway run …</code> and <code>/path/to/hermes gateway run …</code>. All callers (DashboardViewModel, HealthViewModel, SettingsViewModel, scarfApp, stopHermes) want the gateway PID specifically. Cherry-picked from <a href="https://github.com/awizemann/scarf/pull/76">#76</a> — thanks to <a href="https://github.com/unixwzrd">@unixwzrd</a> for the diagnosis and regex.</li>
</ul>
<ul>
  <li><strong><code>HealthViewModel.stopDashboard()</code> — stop the dashboard by port, not <code>pkill -f</code>.</strong> External-instance fallback used to be <code>pkill -f &quot;hermes dashboard&quot;</code>, broad enough to match shell history, log tails, README readers — anything with the substring in its argv. Now <code>lsof -tiTCP:&lt;port&gt; -sTCP:LISTEN</code> resolves the PID actually bound to the dashboard port and only that one process gets <code>SIGTERM</code>. Trusting the port is correct here: Scarf owns the configured port and the user-visible intent is &quot;stop the thing on this port.&quot; Direction cherry-picked from <a href="https://github.com/awizemann/scarf/pull/76">#76</a>; the <code>-c hermes</code> filter from the original was dropped because Hermes installs as a Python shebang script and the kernel COMM is <code>python</code>, not <code>hermes</code> — <code>-c hermes</code> would silently miss every standard install.</li>
</ul>
<h3>Documentation + tooling</h3>
<ul>
  <li><strong><code>scripts/local-build.sh</code> + <code>BUILDING.md</code> for contributor builds.</strong> New unsigned single-arch Debug build script for contributors without an Apple Developer account. Detects arm64 / x86_64, verifies xcode-select / xcrun / xcodebuild, probes the Metal toolchain (offers an interactive install on TTY, errors cleanly on CI), resolves Swift packages, builds Debug with signing disabled. Optional one-touch <code>ditto</code> to <code>/Applications/scarf.app</code> on explicit y/N. The canonical Release universal CLI in <code>README.md</code> is unchanged — <code>local-build.sh</code> is an alternative for contributors, not a replacement for the shipping build. Cherry-picked from <a href="https://github.com/awizemann/scarf/pull/76">#76</a>.</li>
</ul>
<ul>
  <li><strong><code>BUILDING.md</code> + <code>CONTRIBUTING.md</code> — restored Sonoma compatibility messaging.</strong> The runtime min is <strong>macOS 14.6 (Sonoma)</strong> — that&#x27;s the <code>MACOSX_DEPLOYMENT_TARGET</code> on the main <code>scarf</code> target and is intentional. Build min is <strong>Xcode 16.0</strong> (needed for Swift 6 strict-concurrency features). The legacy CONTRIBUTING.md line had drifted to &quot;Xcode 26.3+ / macOS 26.2+&quot;, which would have steered Sonoma contributors and users away from a build that actually runs on their box. Corrected, with a load-bearing-callout in BUILDING.md so future doc edits don&#x27;t silently raise the floor again.</li>
</ul>
<h3>Migrating from 2.7.0</h3>
<p>Sparkle will offer the update automatically. No config migration, no schema changes. Existing sessions, skills, and projects are untouched.</p>
<p>If you&#x27;ve been working around #77 by collapsing the sidebar or restarting Scarf to repopulate the Sessions list, you can stop — sessions should load reliably now.</p>
<h3>Acknowledgements</h3>
<ul>
  <li><a href="https://github.com/bricelb">@bricelb</a> for the three v2.7.0 bug reports (<a href="https://github.com/awizemann/scarf/issues/77">#77</a>, <a href="https://github.com/awizemann/scarf/issues/78">#78</a>, <a href="https://github.com/awizemann/scarf/issues/79">#79</a>) — well-instrumented reproductions including screenshots and environment details made the diagnosis straightforward.</li>
  <li><a href="https://github.com/unixwzrd">@unixwzrd</a> for <a href="https://github.com/awizemann/scarf/pull/76">#76</a> — the gateway-pgrep tighten, the <code>pkill -f &quot;hermes dashboard&quot;</code> direction, and the <code>local-build.sh</code> contributor flow are all cherry-picked from that PR.</li>
</ul>
</body></html>
      ]]></description>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.7.1/Scarf-v2.7.1-Universal.zip"
                 sparkle:edSignature="P9whuwJ264TN1XLaUNvzQK+yKmK2fiyccgKa6TixK3mkIPyVl2XrwPhyHgoMFOQ/c14l+4sizWolx67BSwXBAg=="
                 length="18806277"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.7.0</title>
      <sparkle:version>32</sparkle:version>
      <sparkle:shortVersionString>2.7.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Tue, 05 May 2026 18:47:18 +0000</pubDate>
      <description><![CDATA[
<!DOCTYPE html><html><head><meta charset="utf-8"><style>body {
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", sans-serif;
  font-size: 13px;
  line-height: 1.5;
  color: #1d1d1f;
  margin: 0;
  padding: 0 4px;
}
h2 {
  font-size: 17px;
  margin: 16px 0 6px 0;
  border-bottom: 1px solid #e5e5e7;
  padding-bottom: 3px;
}
h3 {
  font-size: 14px;
  margin: 14px 0 4px 0;
  color: #424245;
}
h4 {
  font-size: 13px;
  font-weight: 600;
  margin: 10px 0 2px 0;
}
p { margin: 6px 0; }
ul { margin: 6px 0; padding-left: 20px; }
li { margin: 3px 0; }
code {
  background: #f5f5f7;
  border-radius: 3px;
  padding: 1px 4px;
  font-family: "SF Mono", Menlo, Consolas, monospace;
  font-size: 12px;
}
pre {
  background: #f5f5f7;
  border-radius: 5px;
  padding: 8px 10px;
  overflow-x: auto;
  font-size: 12px;
}
pre code { background: transparent; padding: 0; }
a { color: #0066cc; text-decoration: none; }
a:hover { text-decoration: underline; }
hr {
  border: none;
  border-top: 1px solid #e5e5e7;
  margin: 16px 0;
}
strong { color: #1d1d1f; }
@media (prefers-color-scheme: dark) {
  body { color: #f5f5f7; background: #1c1c1e; }
  h2 { border-bottom-color: #38383a; }
  h3 { color: #c7c7cc; }
  code, pre { background: #2c2c2e; }
  hr { border-top-color: #38383a; }
  a { color: #4499ff; }
  strong { color: #f5f5f7; }
}
</style></head><body>
<h2>What&#x27;s in 2.7.0</h2>
<p>The biggest release since 2.6.0 — a six-week stretch covering <strong>remote-context performance</strong>, a <strong>new project authoring flow</strong>, <strong>dashboard widgets</strong>, <strong>OAuth resilience</strong>, and a top-to-bottom <strong>performance instrumentation harness</strong> that drove the bulk of the rest. 36 commits, no schema bump, no Hermes capability bump.</p>
<p>The throughline: Scarf got materially faster and more honest on slow remote SSH links, where 30-second sqlite timeouts and silently-empty UI used to be common. The skeleton-then-hydrate pattern, SSH cancellation propagation, and ScarfMon-driven diagnosis are the shape of how that work gets done now.</p>
<hr>
<h3>Remote-context performance — chats and Activity in seconds, not 30s timeouts</h3>
<p>Resuming a chat on a slow remote (a 420ms-RTT droplet, an underprovisioned VPS, a tunnel through 4G) used to fetch the full message column set in one shot, which routinely tripped the 30s SSH timeout on chats with multi-page tool result blobs. The 160-message session was broken; the 30-message session was broken too. Activity didn&#x27;t load at all.</p>
<p>v2.7 introduces a <strong>skeleton-then-hydrate pattern</strong> that bounds the wire payload by what the user actually needs to see RIGHT NOW, then fills in the heavy stuff in the background:</p>
<ul>
  <li><strong>Chat skeleton.</strong> <a href="https://github.com/awizemann/scarf/blob/main/scarf/Packages/ScarfCore/Sources/ScarfCore/Services/HermesDataService.swift"><code>fetchSkeletonMessages</code></a> selects user + assistant rows only (skips <code>role=&#x27;tool&#x27;</code>) with <code>tool_calls</code> / <code>reasoning</code> / <code>reasoning_content</code> hard-NULLed at the SQL level. Wire payload bounded by conversational text alone — typically a few KB. The chat appears in seconds. Background <code>startToolHydration</code> pages through <code>hydrateAssistantToolCalls</code> in 5-id batches to splice tool calls in. Tool-result CONTENT is <strong>opt-in</strong> via Settings → Display → &quot;Load tool results in past chats&quot; (default off); the inspector pane lazy-fetches per-result content via <code>fetchToolResult(callId:)</code> when you open a card.</li>
  <li><strong>Activity skeleton.</strong> <a href="https://github.com/awizemann/scarf/blob/main/scarf/Packages/ScarfCore/Sources/ScarfCore/Services/HermesDataService.swift"><code>fetchRecentToolCallSkeleton</code></a> returns metadata-only rows (id + session_id + role + timestamp; everything else NULLed). Activity opens in &lt;1s on remote with placeholder rows; real per-call entries swap in as paged hydration completes. New &quot;Loading tool details…&quot; pill in the page header surfaces hydration progress.</li>
  <li><strong>Single-id whale recovery.</strong> When a 5-id batch trips the 30s timeout (one row carries an oversized <code>tool_calls</code> blob — a long Edit&#x27;s args, a big diff), an L1 single-id retry isolates the offending row so the rest of the batch still hydrates. Whale row stays bare; assistant message stays readable.</li>
  <li><strong>Lazy tool result loading in the inspector.</strong> Default-off avoids the bulk fetch. When you focus a tool call card, ChatInspectorPane fires <code>loadToolResultIfMissing(callId:)</code> which splices a single result into the message stream without re-fetching anything else.</li>
</ul>
<p>Effect: a 160-message thinking-model session that used to time out at exactly 30s now opens in under 2 seconds with placeholder cards filling in over the next few. Activity loads in 500-800ms.</p>
<h4>SSH cancellation that actually cancels</h4>
<p><code>Task.detached { … }</code> doesn&#x27;t inherit cancellation from the awaiting parent, and <code>Task&lt;…&gt; { … }</code> (unstructured) also drops the signal. Without explicit bridging, cancelling a chat-load Task only unwinds Swift state — the underlying ssh subprocess kept running for the full 30s, pinning a remote sqlite query and a ControlMaster session slot. This produced the &quot;third chat hangs&quot; / &quot;dashboard spins after rapid switching&quot; symptom.</p>
<p>v2.7 wires <code>withTaskCancellationHandler</code> through <a href="https://github.com/awizemann/scarf/blob/main/scarf/Packages/ScarfCore/Sources/ScarfCore/Transport/SSHScriptRunner.swift"><code>SSHScriptRunner.run</code></a> and <a href="https://github.com/awizemann/scarf/blob/main/scarf/Packages/ScarfCore/Sources/ScarfCore/Services/Backends/RemoteSQLiteBackend.swift"><code>RemoteSQLiteBackend.query</code></a> so parent cancellation reaches the <code>Process</code> and calls <code>proc.terminate()</code> within 100ms. New <code>ssh.cancelled</code> ScarfMon event surfaces this.</p>
<h4>In-flight coalescing for <code>loadRecentSessions</code></h4>
<p>File-watcher deltas during an active stream used to stack 2-3 parallel sessions-list reload tasks (the 500ms <code>scheduleSessionsRefresh</code> debounce only suppresses a pending tick, not one already executing). Subsequent callers now await the in-flight load instead of spawning a parallel SSH subprocess. New <code>mac.loadRecentSessions.coalesced</code> event tracks dedup hits.</p>
<h4>Loading-state UX hardening</h4>
<p>The Mac chat sidebar greys out and disables row taps the moment a session-switch is initiated (synchronously, before <code>client.start()</code> returns), with a floating ProgressView showing the current phase: <strong>&quot;Spawning hermes acp…&quot;</strong> → <strong>&quot;Authenticating…&quot;</strong> → <strong>&quot;Loading session…&quot;</strong> → <strong>&quot;Loading history…&quot;</strong> → <strong>&quot;Ready&quot;</strong>. Pre-fix the sidebar looked engageable while the 5-7 second SSH+ACP boot was still in flight, and the user could queue up a second session-switch behind the first. New <code>isStartingSession</code> flag flips on user click for instant feedback.</p>
<h4>Partial-result + mismatch + pinned-model banners</h4>
<ul>
  <li><strong>Partial-result banner.</strong> When the skeleton fetch trips an SSH transport failure (rather than a clean empty result), the chat surfaces &quot;Couldn&#x27;t load full chat history — the connection to <em>server</em> timed out&quot; through the existing <code>acpError</code> triplet, plus forces <code>hasMoreHistory = true</code> so the &quot;Load earlier&quot; affordance shows up. Replaces the pre-fix silent empty transcript.</li>
  <li><strong>Model/provider mismatch banner.</strong> <a href="https://github.com/awizemann/scarf/blob/main/scarf/Packages/ScarfCore/Sources/ScarfCore/Services/ModelPreflight.swift"><code>ModelPreflight.detectMismatch</code></a> recognizes when <code>model.default</code> carries a <code>&lt;provider&gt;/...</code> prefix that disagrees with <code>model.provider</code> (e.g. <code>anthropic/claude-sonnet-4.6</code> + <code>provider: nous</code> after switching OAuth via Credential Pools). Banner offers one-click fix in either direction.</li>
  <li><strong>Pinned-model failure hint.</strong> ACP error classifier now recognizes <code>model_not_found</code> / <code>404 messages</code> / <code>model is not available</code> and surfaces &quot;This session was created with a model the provider no longer offers — start a new chat to use your current model&quot; so the pinned-model failure mode has a clear recovery path.</li>
  <li><strong>OAuth-completion provider swap.</strong> After a successful OAuth in Credential Pools, if the just-authed provider differs from <code>model.provider</code>, surface &quot;Switch active provider to <em>name</em>?&quot; with [Switch] / [Keep current] instead of auto-dismissing.</li>
</ul>
<hr>
<h3>New Project from Scratch wizard + Keychain-backed cron secrets</h3>
<p>A <strong>third project entry point</strong> alongside Browse Catalog and Add Existing Project: a wizard that scaffolds a Scarf-standard project skeleton (<code>&lt;project&gt;/.scarf/dashboard.json</code> + AGENTS.md marker block), registers it, and hands off to a chat session that auto-activates the bundled <code>scarf-template-author</code> skill. The skill drives the rest conversationally — widgets, optional config schema, optional cron — and writes the final files itself. Wizard stays minimal because the agent does configuration better than a multi-step form. The skill ships bundled inside <code>Scarf.app/Contents/Resources/BuiltinSkills.bundle/</code> and copies into <code>~/.hermes/skills/</code> on launch (idempotent + version-gated).</p>
<p><strong>Cron + Keychain — <code>$SCARF_&lt;SLUG&gt;_&lt;FIELD&gt;</code> env vars.</strong> Cron prompts that referenced <code>secret</code>-typed config fields used to get the literal <code>keychain://...</code> URI back when reading <code>config.json</code>, producing 401s. v2.7 mirrors resolved Keychain values into <code>~/.hermes/.env</code> under a marker-bounded block keyed by template slug:</p>
<pre><code># scarf-secrets:begin local-news-aggregator
SCARF_LOCAL_NEWS_AGGREGATOR_API_TOKEN=actual-value
SCARF_LOCAL_NEWS_AGGREGATOR_RSS_URL=https://example.com/feed
# scarf-secrets:end local-news-aggregator</code></pre>
<p>Hermes already reloads <code>~/.hermes/.env</code> per cron tick, so credential rotation is automatic — just edit the value in Configuration → next tick sees it. The mirror runs at every state-change point: install, post-install Configuration save, uninstall, &quot;Remove from List&quot;, and on app launch (reconciliation pass over registered projects). Source of truth stays in the Keychain — <code>config.json</code> keeps <code>keychain://</code> URIs unchanged. Mode 0600 enforced on <code>~/.hermes/.env</code>.</p>
<p>Cron prompts now reference these env vars directly:</p>
<pre><code>{
  &quot;prompt&quot;: &quot;Use the terminal: curl -sS -H \&quot;Authorization: Bearer $SCARF_LOCAL_NEWS_AGGREGATOR_API_TOKEN\&quot; \&quot;$SCARF_LOCAL_NEWS_AGGREGATOR_RSS_URL\&quot; -o {{PROJECT_DIR}}/.scarf/feed.xml&quot;
}</code></pre>
<p><strong>Migration.</strong> First launch of v2.7 walks the project registry and writes the managed block per schemaful project — automatic. Existing cron prompts you wrote against the old (broken) <code>config.json</code> pattern still need updating: open the cron job in Scarf&#x27;s Cron sidebar and edit the prompt, or ask the agent in chat (&quot;Update my Local News cron job&#x27;s prompt to use the new env var convention&quot;) — the bundled <code>scarf-template-author</code> skill (now v1.1.0) documents the convention with worked examples.</p>
<p>Also fixes <a href="https://github.com/awizemann/scarf/issues/75">#75</a> — <code>_NSDetectedLayoutRecursion</code> on the Configuration form for projects whose form transitioned between stages with different intrinsic heights.</p>
<hr>
<h3>Project dashboards — file-reading widgets, sparklines, typed status</h3>
<p>Five new widget types, project-wide auto-refresh, and a structured error card for unknown widgets. Backwards-compatible — every existing <code>dashboard.json</code> renders byte-identically.</p>
<ul>
  <li><strong>Project-wide auto-refresh.</strong> <a href="https://github.com/awizemann/scarf/blob/main/scarf/scarf/Core/Services/HermesFileWatcher.swift"><code>HermesFileWatcher</code></a> used to watch each project&#x27;s <code>dashboard.json</code> specifically. v2.7 promotes that to a watch on the entire <code>&lt;project&gt;/.scarf/</code> directory. A <code>markdown_file</code> or <code>log_tail</code> widget pointing at <code>&lt;project&gt;/.scarf/reports/foo.md</code> refreshes the moment a cron job rewrites the file. <strong>By convention, place files the dashboard reads inside <code>.scarf/</code></strong> so the watch picks them up.</li>
  <li><strong><code>markdown_file</code></strong> — renders a markdown file from disk through the same <code>MarkdownContentView</code> pipeline used by inline <code>text</code> widgets.</li>
  <li><strong><code>log_tail</code></strong> — last <code>lines</code> of a file (default 20, max 200), monospaced, ANSI codes stripped.</li>
  <li><strong><code>cron_status</code></strong> — last run / next run / state for one Hermes cron job by <code>jobId</code>, plus a small inline log tail. Read-only — Run/Pause/Resume controls stay on the Cron tab.</li>
  <li><strong><code>image</code></strong> — local file (<code>path</code> relative to project root) or remote <code>url</code>. Optional <code>height</code> cap. Useful for matplotlib/Plotly PNGs the cron job generates.</li>
  <li><strong><code>status_grid</code></strong> — compact NxM grid of colored cells, one per service / item, with hover labels.</li>
  <li><strong><code>stat</code> widget gains inline sparklines.</strong> Optional <code>sparkline: [Number]</code> field. SVG-only render, dozens per dashboard cost nothing.</li>
  <li><strong>Typed status badges.</strong> <code>list</code> items and <code>status_grid</code> cells share a typed enum (<code>success</code>, <code>warning</code>, <code>danger</code>, <code>info</code>, <code>pending</code>, <code>done</code>, <code>neutral</code>) with lenient decode for synonyms (<code>ok</code>/<code>up</code> → success, <code>down</code>/<code>error</code> → danger). Unknown strings render as plain text.</li>
  <li><strong>Structured widget error card.</strong> Replaces the legacy &quot;Unknown: \&lt;type\&gt;&quot; placeholder with a card surfacing the title, specific reason, and a hint.</li>
  <li><strong>Schema mirror.</strong> The widget vocabulary lives once at <a href="https://github.com/awizemann/scarf/blob/main/tools/widget-schema.json"><code>tools/widget-schema.json</code></a>; the catalog validator reads from it and enforces per-type required fields.</li>
</ul>
<hr>
<h3>OAuth resilience + Credential Pools</h3>
<ul>
  <li><strong>Daily OAuth keepalive cron.</strong> Prevents Anthropic OAuth refresh tokens from expiring after weeks of inactivity. New cron job <code>[scarf:oauth-keepalive]</code> (managed by Scarf) pings Hermes on a daily cadence; the in-app Refresh All Sessions action mirrors the same path on demand.</li>
  <li><strong>Remote re-auth.</strong> Re-authenticating against a remote droplet&#x27;s OAuth provider used to be blocked by the lack of a stdin path through SSHTransport. The OAuth flow now drives a remote <code>hermes auth add</code> correctly with stdin forwarded.</li>
  <li><strong>OAuth remove button.</strong> Per-provider remove action in Credential Pools (auth.json edit), with confirmation dialog. Companion auto-refresh of the view when <code>auth.json</code> changes externally (file-watcher).</li>
  <li><strong><code>resolve_provider_client</code> error classification.</strong> When an auxiliary task references a provider whose credentials aren&#x27;t loaded, Hermes prints <code>resolve_provider_client: &lt;name&gt; requested but &lt;Display Name&gt; not configured</code> to stderr — pre-fix this surfaced in chat as the opaque <code>-32603 Internal error</code> with no actionable detail. Now classified into a clear hint pointing at Settings → Aux Models.</li>
  <li><strong>Aux Tab unknown-task surface.</strong> When <code>config.yaml</code> has an <code>auxiliary.&lt;task&gt;</code> block for a task Scarf doesn&#x27;t know about (newer Hermes added it; Scarf hasn&#x27;t caught up), render it as a plain row with the raw provider/model values instead of dropping it silently.</li>
  <li><strong>Credential Pools refresh after OAuth sheet dismiss.</strong> Closing the OAuth sheet after a successful add now refreshes the list immediately instead of leaving the just-added pool hidden until the next file-watcher tick.</li>
</ul>
<hr>
<h3>ScarfMon — performance instrumentation harness</h3>
<p>The diagnostic surface that drove the bulk of the v2.7 perf work. Off by default; signpost-only mode (Instruments-friendly) is free; Full mode (4096-entry in-memory ring buffer + os.Logger) is a click away in Settings → Diagnostics → Performance. Wiki: https://github.com/awizemann/scarf/wiki/Performance-Monitoring</p>
<ul>
  <li><strong>Phases 1-3</strong> built the core: dispatcher + ring buffer + 3 backends, chat / transport / sqlite measure points, diagnostic counters for chat-render bursts, finalize-burst dampening.</li>
  <li><strong>Tier A + B</strong> added per-feature instrumentation: iOS file watcher, sessions list, model catalog, dashboard widgets, image encoder, message hydration.</li>
  <li><strong>Nous picker investigation</strong> localized a 60s + 120s beach-ball to a specific path (Nous catalog <code>readCache</code>), then killed the 120s one with dedupe + 5s timeout.</li>
  <li><strong>Tier C catch-up</strong> (this release): instrumented Memory / Skills / Cron / Curator load paths so future captures show how often these tabs cost multiple sequential SFTP RTTs on remote.</li>
  <li><strong>Per-call bytes recorded</strong> on transport + sqlite events so captures show payload sizes alongside latencies.</li>
  <li><strong><code>mac.emptyAssistantTurn</code> event</strong> documents the Nous quirk where the model returns a thought stream with no body (the bubble looks like Hermes is &quot;still thinking&quot; but the turn already finished).</li>
</ul>
<p>Adding a new measure point is two lines. The harness covers Mac and iOS uniformly. The &quot;Copy as JSON&quot; button exports the ring buffer for paste-into-issue diagnosis.</p>
<hr>
<h3>Other fixes + polish</h3>
<ul>
  <li><strong>Sessions sidebar reload debounce</strong> — file-watcher deltas during streaming used to flicker the sessions list. Coalesced into one trailing fetch ~500ms after the last tick.</li>
  <li><strong>Session-load pagination + race guard</strong> — switching to a small chat while a larger one is mid-fetch could last-write-wins the small chat away. Three race-checks against <code>self.sessionId</code> prevent the stale fetch from overwriting.</li>
  <li><strong>Sessions + previews batched</strong> — two separate SSH calls folded into one <code>queryBatch</code> round trip, halving the round-trips for every sidebar refresh.</li>
  <li><strong>Remote SQLite query timeout</strong> bumped 15→30s to better tolerate slow links; in-flight query coalescing dedupes concurrent identical queries.</li>
  <li><strong><code>Thread.sleep</code> spin replaced</strong> with a kernel-wait via <code>DispatchGroup</code> for <code>runLocal</code> timeout; under concurrent SSH load the old loop accumulated spin-blocked threads and produced 7-second outliers in <code>loadRecentSessions</code>.</li>
  <li><strong>Window position + size</strong> persists across launches.</li>
  <li><strong>Sidebar reorder</strong> — Projects promoted to first section; profile chip moved under server name.</li>
  <li><strong><code>stop</code> badge suppressed</strong> on metadata footer for normal turn ends (it was firing for every clean completion, looking like an error).</li>
  <li><strong>Nous picker search field</strong> + <code>model-picker</code> filter for the long Nous overlay model list.</li>
  <li><strong><code>oauth-keepalive</code> cron create</strong> — drop the <code>--silent</code> flag Hermes doesn&#x27;t accept.</li>
  <li><strong>Snapshot pipeline rewritten</strong> — replaced the <code>sqlite3 .backup</code>-then-download pipeline with direct SSH-streamed query execution (issue <a href="https://github.com/awizemann/scarf/issues/74">#74</a>). Eliminates the multi-minute snapshot wait on multi-GB state.db files. Companion fix: pre-expand <code>~/</code> in Swift via <code>resolvedUserHome</code> so sqlite3 finds the DB without depending on the remote shell&#x27;s tilde expansion.</li>
  <li><strong>Aux nested-YAML parser</strong> — corrected the parser so the unknown-task surface works on remote (was previously dropping aux blocks whose <code>provider:</code> value lived on a separate line).</li>
  <li><strong><code>ModelPreflight</code> newline trim bug</strong> — <code>.whitespaces</code> doesn&#x27;t strip newlines; switched both trims to <code>.whitespacesAndNewlines</code> so a stray <code>\n</code> in a hand-edited config.yaml doesn&#x27;t false-positive the mismatch banner.</li>
</ul>
<hr>
<h3>What&#x27;s measured today</h3>
<p>321 ScarfCore tests pass (302 prior + 19 new ModelPreflight). New ScarfMon events documented in the <a href="https://github.com/awizemann/scarf/wiki/Performance-Monitoring">Performance-Monitoring wiki</a>.</p>
<h3>Compatibility</h3>
<ul>
  <li>macOS 14+ (unchanged).</li>
  <li>Hermes target: still <strong>v2026.4.30 (v0.12.0)</strong>. No new Hermes capability gates added.</li>
  <li>Existing <code>dashboard.json</code> files render unchanged.</li>
  <li>Existing <code>.scarftemplate</code> bundles install unchanged. Catalog manifest schemaVersion stays at 1/2/3 — no bump.</li>
  <li>Existing <code>~/.hermes/.env</code> content is preserved byte-identically — Scarf only writes inside its <code># scarf-secrets:begin &lt;slug&gt;</code> / <code># scarf-secrets:end &lt;slug&gt;</code> regions.</li>
  <li>The skeleton-then-hydrate chat loader and SSH cancellation propagation are <strong>Mac-only</strong> in this release; ScarfGo (iOS) keeps its existing chat path.</li>
</ul>
<h3>What&#x27;s deferred</h3>
<ul>
  <li><strong>Per-widget data sources + per-widget refresh granularity.</strong> The general &quot;widget points at a typed data source&quot; abstraction is the next-largest win in dashboards but materially expands the model + JS mirror + validator surface. The project-wide watch covers the common cron-driven workflow without it.</li>
  <li><strong>Cross-project health digest sidebar rollup.</strong> Counting attention-needed projects across the registry — scoped but didn&#x27;t pull its weight. The typed status enum makes it cheap to add later.</li>
  <li><strong>Automatic cron-prompt rewriter on upgrade.</strong> Heuristic rewrites of free-form prompts are risky; the docs + agent-assisted path ships in v2.7. Revisit a &quot;scan + fix&quot; UI in v2.8 if real users miss the migration.</li>
  <li><strong>iOS New Project wizard + iOS Keychain-env mirror.</strong> ScarfGo&#x27;s project surface is read-only; the wizard&#x27;s chat-handoff pattern depends on Mac-only ACP plumbing.</li>
  <li><strong>iOS skeleton-then-hydrate loaders.</strong> Same data-service surfaces are public, but the iOS chat lifecycle is structured differently. Defer until iOS dogfooding shows the same payload-size pain.</li>
  <li><strong>Tier C redesigns (Memory/Skills/Cron/Curator).</strong> Instrumented in v2.7; redesign waits for capture data showing which path actually needs the skeleton-then-hydrate treatment.</li>
</ul>
</body></html>
      ]]></description>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.7.0/Scarf-v2.7.0-Universal.zip"
                 sparkle:edSignature="JfHK1sKbP3ubbznXx3uY/a3kY2szkWpTUQmtHJE54Uv970T5PxgIHCTwsimqtZCPepUTv2qK4TRQfqHJBKUmCA=="
                 length="18793474"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.6.5</title>
      <sparkle:version>31</sparkle:version>
      <sparkle:shortVersionString>2.6.5</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Sun, 03 May 2026 20:20:29 +0000</pubDate>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.6.5/Scarf-v2.6.5-Universal.zip"
                 sparkle:edSignature="2hYRNX/z+mQ7TjlHfDAiTHiP/Rk1BoJRjjefPvutooiptwOK6EyYq/9crnHYZYe8xvEQTDMmAvsPz4vYz823Cg=="
                 length="18257858"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.6.0</title>
      <sparkle:version>29</sparkle:version>
      <sparkle:shortVersionString>2.6.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Fri, 01 May 2026 13:48:15 +0000</pubDate>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.6.0/Scarf-v2.6.0-Universal.zip"
                 sparkle:edSignature="gI1uwJAvmwdlvngLcsSQFtMDsHyI6+DWN23ZjOX/BYmX+BqKu0XLuAXL1tztZV9RF1l5PG+vNVWGnq6ivqVQCQ=="
                 length="18031081"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.5.2</title>
      <sparkle:version>28</sparkle:version>
      <sparkle:shortVersionString>2.5.2</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Wed, 29 Apr 2026 11:47:40 +0000</pubDate>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.5.2/Scarf-v2.5.2-Universal.zip"
                 sparkle:edSignature="tPgE0ajkNs+OYuYGgB8jVLtY/tGTaUJlrJCf5I5AbwCU2R+Zu08D+pLB17q01A3AgwYfYTLZbnoTG+xO4ys8DQ=="
                 length="17367761"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.5.1</title>
      <sparkle:version>27</sparkle:version>
      <sparkle:shortVersionString>2.5.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Mon, 27 Apr 2026 13:38:41 +0000</pubDate>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.5.1/Scarf-v2.5.1-Universal.zip"
                 sparkle:edSignature="OArax2dY25Q7ZRFYGcviaGmCQCJsIugcBdjTET//mJ4XTT/FnnPQoSTIYaQrsV+mwFZU//G75q9PwPtnNsPiCA=="
                 length="17090983"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.5.0</title>
      <sparkle:version>26</sparkle:version>
      <sparkle:shortVersionString>2.5.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Sat, 25 Apr 2026 15:42:47 +0000</pubDate>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.5.0/Scarf-v2.5.0-Universal.zip"
                 sparkle:edSignature="YnHpGMIiL8jyDn3+h8B7Gqzrlz8SXDSyiUXGm9DD6BIkRfYfzi3AVatkxfBLMvoVhlqPGKIhKsB8ybqopgjpCw=="
                 length="16994785"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.3.0</title>
      <sparkle:version>25</sparkle:version>
      <sparkle:shortVersionString>2.3.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Fri, 24 Apr 2026 01:20:51 +0000</pubDate>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.3.0/Scarf-v2.3.0-Universal.zip"
                 sparkle:edSignature="vae/hUTU7UOSY/LYU/pt1A9wnbKgvp22+e2peGA/clmloaA22gxCnBX5JALT1w93eHYMLOtvdSf5OrNHyogHDQ=="
                 length="14783787"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.2.1</title>
      <sparkle:version>24</sparkle:version>
      <sparkle:shortVersionString>2.2.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Thu, 23 Apr 2026 20:10:10 +0000</pubDate>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.2.1/Scarf-v2.2.1-Universal.zip"
                 sparkle:edSignature="nFdO9t2wWWKeXehQCm7btr7kzCtmDDg4xsvrm4Z24fqOB+Y9ffYcIBr+e9pqnLSIJJ6r/lYcyz5FlkUMjYXyCw=="
                 length="17868308"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.2.0</title>
      <sparkle:version>23</sparkle:version>
      <sparkle:shortVersionString>2.2.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Thu, 23 Apr 2026 16:31:53 +0000</pubDate>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.2.0/Scarf-v2.2.0-Universal.zip"
                 sparkle:edSignature="mGuKLJbcugMTKdSlrkgYKjpVAaXY8CsMVhCiAo/O7b4K8S/fRaK7ZZNdbLtfSGndrbVWcSU0IIhaB1rBGaPOBQ=="
                 length="17867934"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.1.0</title>
      <sparkle:version>22</sparkle:version>
      <sparkle:shortVersionString>2.1.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Tue, 21 Apr 2026 01:50:30 +0000</pubDate>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.1.0/Scarf-v2.1.0-Universal.zip"
                 sparkle:edSignature="kllR3yC/Cze1W9fSM+WRIE5YVObubEGmV629hAvxzVhvVIJ9n+qa00WOAC3YakZLEKX46DmowEMQf5ikqQGODQ=="
                 length="17243337"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.0.2</title>
      <sparkle:version>21</sparkle:version>
      <sparkle:shortVersionString>2.0.2</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Mon, 20 Apr 2026 22:50:02 +0000</pubDate>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.0.2/Scarf-v2.0.2-Universal.zip"
                 sparkle:edSignature="3BF7PzLqO835wr+cCEA0Ls4kfp2hNwgDmM8YlvUKM9R/GwTs+tgtSPLTwfr1UyXNH/83uNRuSeZM8iNgV4dPDA=="
                 length="17063811"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 2.0.0</title>
      <sparkle:version>19</sparkle:version>
      <sparkle:shortVersionString>2.0.0</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Sun, 19 Apr 2026 20:11:43 +0000</pubDate>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v2.0.0/Scarf-v2.0.0-Universal.zip"
                 sparkle:edSignature="QTgPwFgHk5SbUYKrgg412o/Yc7ZhSiow/33EoWnN+julyEVw/0MwemjHNcwcX+aVQJV4arMp7xGVMzaVV/j1CQ=="
                 length="16906164"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 1.6.2</title>
      <sparkle:version>18</sparkle:version>
      <sparkle:shortVersionString>1.6.2</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Sat, 18 Apr 2026 00:22:28 +0000</pubDate>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v1.6.2/Scarf-v1.6.2-Universal.zip"
                 sparkle:edSignature="wtf0QjTKCvYq8BZW1meeWdWk8GMsbYopfM/DNiYw8ImdgmX6X8jN5+bIG9KvzYv0VgZ0la8ssSliiz7zdJA2CQ=="
                 length="16570465"
                 type="application/octet-stream" />
    </item>
    <item>
      <title>Version 1.6.1</title>
      <sparkle:version>17</sparkle:version>
      <sparkle:shortVersionString>1.6.1</sparkle:shortVersionString>
      <sparkle:minimumSystemVersion>14.6</sparkle:minimumSystemVersion>
      <pubDate>Fri, 17 Apr 2026 02:11:53 +0000</pubDate>
      <enclosure url="https://github.com/awizemann/scarf/releases/download/v1.6.1/Scarf-v1.6.1-Universal.zip"
                 sparkle:edSignature="hoYDb7VRQ+YDNUox1kf7eYbhckOJWYLEi8ZPfBZG59qK4L/5N2mmgV7jOCLriHkNx0F4mvM9UK8UQZIkxsX1DA=="
                 length="16566934"
                 type="application/octet-stream" />
    </item>
  </channel>
</rss>
